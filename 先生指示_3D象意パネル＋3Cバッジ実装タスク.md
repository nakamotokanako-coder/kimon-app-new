# 🎯 実装タスク：3-D 象意パネル ＋ 3-C 順利バッジ（まとめて実装）

**作成日**: 2026-05-24
**作成者**: Claude Web（指示書フェーズ）
**実行者**: Claude Code（kimon-app ローカル環境）
**前提**: 3-C（◎順利・秘伝判定ロジック）はマージ済み。本タスクはその **UI 表示** と 3-D 象意パネルを同時に行う。

> **⚠️ 実装先リポジトリ**: 正本は `nakamotokanako-coder/kimon-app-new`（Vercel本番接続・2026-05-19にクリーン履歴で移行）。
> 旧 `kimon-app` はバックアップなので触らない。

---

## 📨 このタスクの背景

引き継ぎメモ（5/22・3C完了版）の方針：

- 3-C はロジック完成済み。**UIバッジだけ意図的に切り出して保留**してある。
- 3-D（象意パネル）も同じ宮セルまわりを触るので、**表示作業はまとめてやる方が効率的**。

→ 本指示書で、3-D 象意パネルと 3-C 順利バッジを一度に実装する。

---

## 📦 新規データ（このタスクの入力）

`shoui_dict.json`（同フォルダに添付）を `kimon-app-new/data/` または `src/data/` に配置。

### 役割分担（重要・既存JSONと混同しない）

| ファイル | 役割 | 状態 |
|---|---|---|
| `jukkanKokuou.json` | 十干剋応の **判定**（天盤×地盤→どの名称が出るか） | 既存・流用 |
| `kakkyoku.json` | 格局の **判定**（条件→どの格局が出るか） | 既存・流用 |
| **`shoui_dict.json`** | 名称→ **解説テキスト**（原文/現代の読み替え/実務示唆/色） | **★新規** |

→ 判定は既存JSON、**解説は新JSON**。3-D は「判定で出た名称」を新JSONで引いて表示するだけ。

### shoui_dict.json の構造

```jsonc
{
  "meta": { "jukan_count": 81, "kakkyoku_count": 50, "color_legend": {...} },

  "jukan_kokuou": [        // 十干剋応 81件
    {
      "no": 62,
      "name": "凶蛇入獄",   // ← 記号なし。判定側の名称と完全一致（照合済み）
      "sign": "×",          // 〇△×
      "kikkyo": "凶",       // 吉/中立/凶
      "color": "red",       // green=吉 / yellow=中立 / red=凶（※後述の色注意）
      "kikkyo_text": "凶",
      "original": "三角関係。争いは負ける。先に動くと理が通らなくなる。",
      "modern": "三角関係のような複雑な利害が絡む配置。…",
      "practical": "自分から仕掛けるのは不利。…"
    }
  ],

  "kakkyoku": [            // 格局 50件（同じく original/modern/practical を持つ）
    { "no": 48, "name": "乙奇入墓", "original": "…", "modern": "…", "practical": "…" }
  ],

  "jukan_index_by_name":   { "凶蛇入獄": [62, 68], "華蓋蓬星": [9, 73], ... },
  "kakkyoku_index_by_name":{ "青龍返首": 1, "乙奇入墓": 48, ... }
}
```

### ⚠️ 名称引きの注意：同名で複数配置がある十干剋応

`jukan_index_by_name` で **no の配列** が返るのは次の3名称のみ：

- `華蓋蓬星` → [9, 73]
- `華蓋孛師` → [18, 74]
- `凶蛇入獄` → [62, 68]

これらは天盤×地盤の組合せが違う別配置。判定側（jukkanKokuou）は天干ペアから一意に決まるので、
**判定が返す No（または天干ペア）で該当 no を特定**して解説を選ぶこと。
それ以外の名称は配列要素1個なので `[0]` でよい。

---

## ⚠️ 色分けの食い違い（実装前に必ず確認）

ここだけ仕様に矛盾があるので Claude Code 側で統一すること。

| 出どころ | 吉 | 中立 | 凶 |
|---|---|---|---|
| 5/22メモ 3-D仕様（先生指定） | **青** | （指定なし） | **赤** |
| shoui_dict.json の `color` 値 | green | yellow | red |

**推奨対応**：JSONの `color`（green/yellow/red）は「意味ラベル」として持たせてあるだけ。
表示の実色は先生指定に合わせ、**吉=青・凶=赤・中立=黄/グレー** にマッピングする。
（例：CSSクラス `shoui-kichi`=青 / `shoui-kyo`=赤 / `shoui-chu`=黄 を当てる）

格局の吉凶は既存 `kakkyoku.json` の `kichi_kyo` を利用（5/22メモ記載）。
shoui_dict.json の格局側には sign/color を入れていないため、**格局の色は kakkyoku.json 側を正**とする。

---

## 🛠 実装タスク

### Part A：3-D 象意パネル（ShouiPanel.jsx 改修）

対象：`src/components/ShouiPanel.jsx`

#### A-1. データローダー
- [ ] `shoui_dict.json` を読み込むローダーを作る（既存 loadKoyomi.js 等と同じ流儀）
- [ ] 名称引きヘルパー：
  - `getJukanShoui(name, no?)` → 該当エントリ。no省略時、配列が複数なら no 必須（上記3名称）
  - `getKakkyokuShoui(name)` → 該当エントリ

#### A-2. 表形式の表示
- [ ] 5/22メモ仕様の表：**宮・方位・八門・十干剋応・格局・点数**
- [ ] 各宮について、scoreEngine の結果から十干剋応名・格局名を取得し、解説辞書を引く
- [ ] 点数は既存 `scorePalace().score` を表示

#### A-3. アコーディオン辞書
- [ ] 単語（十干剋応名・格局名）クリックで内容を開閉。**初期は全閉**（辞書イメージ、最初は全展開しない）
- [ ] 開いたときの中身：**原文 → 現代の読み替え → 実務での示唆** の3段
  - 先生指示：「実装時は **現代の読み替えと実務での示唆を入れる**」→ 必ず3段とも表示
- [ ] 表示順は 原文（伝統）→ 現代の読み替え → 実務での示唆 を推奨（伝統を上に）

#### A-4. 色分け
- [ ] 十干剋応：sign（〇△×）→ 吉=青/中立=黄/凶=赤（上記「色の食い違い」に従う）
- [ ] 格局：kakkyoku.json の kichi_kyo → 吉=青/凶=赤

### Part B：3-C 順利バッジ（PalaceCell.jsx）

3-C のロジックは実装済み。**表示だけ**を追加する。

接続キー（3-C完了メモより・実装済みの戻り値）：
- 各宮：`palaceScores[name].is_junri`（boolean）
- 盤全体：`scoreBoard().{ day_kan_palace, junri_palaces }`（英字キー配列）

対象：`src/components/PalaceCell.jsx`

- [ ] `is_junri === true` の宮に **◎順利バッジ** を表示
- [ ] `cell-junri` クラスを付与し、CSS で強調（最優先＝「◎◎効果最大」の見た目）
- [ ] バッジの文言・位置はUI担当の裁量。既存の評価ランク表示と整合させる
- [ ] **注意**：is_junri は 3-C で「検出」まで。評価ランク「◎◎効果最大」への昇格は 3-B 担当。
  本タスクは「is_junri な宮を視覚的に目立たせる」までで十分（3-B未完でも独立して出せる）

### Part C：CSS

対象：`src/styles.css`

- [ ] `.shoui-kichi`（青）/ `.shoui-kyo`（赤）/ `.shoui-chu`（黄）
- [ ] `.cell-junri`（順利の宮の強調）
- [ ] アコーディオンの開閉スタイル

---

## ✅ テスト観点

### データ整合（実装前のスモークテスト推奨）
- [ ] shoui_dict.json の jukan_kokuou が **81件**、kakkyoku が **50件**
- [ ] `jukan_index_by_name` の全 name が、判定側 jukkanKokuou の名称集合と一致
  （※ Web側で照合済み・過不足ゼロを確認。実装側でも assert を1本入れると安全）
- [ ] 同名3種（華蓋蓬星/華蓋孛師/凶蛇入獄）が no で正しく引き分けられる

### 表示テスト
- [ ] 任意の盤で、各宮の十干剋応・格局がアコーディオンで開き、3段（原文/読み替え/示唆）が出る
- [ ] 吉=青・凶=赤・中立=黄 で色が付く
- [ ] No.62「凶蛇入獄」を開くと、書き直し後の本文（三角関係…）が出る（No.68と別内容）
- [ ] 格局「乙奇入墓/丙奇入墓/丁奇入墓」が引ける（新規追加分）

### 順利バッジ
- [ ] is_junri な宮に ◎順利バッジ＋ cell-junri が付く
- [ ] is_junri が無い宮には付かない

---

## 📌 完了条件

1. `shoui_dict.json` が data/ に配置され、ローダー経由で読める
2. 象意パネルが 表＋アコーディオン辞書＋色分け で表示される
3. アコーディオンに 原文/現代の読み替え/実務での示唆 の3段が出る
4. 十干剋応=吉青/中立黄/凶赤、格局=吉青/凶赤 で色が付く
5. is_junri な宮に順利バッジが出る
6. `npm test` 全 green
7. `npm run dev` で UI 確認 → OK なら commit & push → PR → マージ

---

## 🗂️ 関連ファイル

- `src/components/ShouiPanel.jsx` ← 3-D 改修対象
- `src/components/PalaceCell.jsx` ← 3-C バッジ追加
- `src/kimon/scoreEngine.js` ← is_junri / junri_palaces / day_kan_palace を返す（3-Cで実装済）
- `src/kimon/jukkanKokuou.js` / `kakkyoku.js` ← 名称の **判定**（既存・流用）
- `data/shoui_dict.json` ← 名称の **解説**（★新規・本タスクで配置）
- `src/styles.css` ← 色分け・バッジCSS

---

## 🔭 このタスクの後（参考）

- 3-D の検索機能（3-G・期間検索・方位フィルター）は別タスク。chito_v2.csv 活用。
- 評価ランク「◎◎効果最大」への正式昇格は 3-B。is_junri を最優先キーに使う。
- 3-B 上位ランク（XX超危険）は Q2 の先生確認待ち（本タスクとは独立）。

---

## 📝 解説テキストについての補足（先生確認済み事項）

本データは先生監修ファイル `十干剋応と格局の象意_わかりやすい解説_v2.xlsx` から生成。
以下は先生回答（2026-05-24）を反映済み：

- 十干剋応 No.62「凶蛇入獄」：原文に合わせ読み替え・示唆を書き直し
- 十干剋応 No.82〜87（九遁等）：十干剋応ではない掲載ミスのため削除 → **81件確定**
- 格局「乙奇入墓・丙奇入墓・丁奇入墓」：解説を新規作成（原文「三奇の力が失われる」を反映）
- 誤字4件（因獄被傷→囚獄被傷 ほか）修正済み

→ そのまま実装してよい（先生承認済み）。
