# Codex指示：起点日を「今日」に＋テーマ4色化＋盤情報デザイン刷新

**作成**: 2026-05-25 / Claude Web
**実行**: Codex（`C:\dev\kimon-app`）
**対象**: `src/App.jsx`（初期日付）／`src/styles.css`（テーマCSS変数）／テーマ切替UI（ヘッダー）／`src/components/MetaPanel.jsx`（盤情報デザイン）
**参考**: `board_info_designs.html`（盤情報の案1）、`theme_color_candidates.html`（テーマ色）
**最重要制約**: 盤・象意パネルの**中身の配色（宮名の黄・三奇の青・八門の赤青ベタ・点数の緑赤・象意の色分け）は全テーマ共通で一切変えない**。判定・スコアロジックも不変。

---

## タスク1：起点日を「今日」にする

### 問題
アプリを開くと初期日付が **2026/05/18 に固定**されている。開いた日（今日）が初期値で入っていてほしい。

### 修正
- `src/App.jsx`（または日付state初期化箇所）で、初期日付を **new Date() ベースの「今日」** にする。
- タイムゾーンは**日本時間（JST）基準**で「今日」を算出（UTCずれで前日にならないよう注意）。
- 日付フォーマットは既存の表示形式（YYYY/MM/DD）に合わせる。
- 変更は初期値だけ。日付を変えたときの盤再計算ロジックは触らない。

---

## タスク2：テーマを4色にする

現状テーマは3つ（`dark-gold`=黒金 / `navy-silver`=紺銀 / `washi-vermillion`=和）。
**和をリニューアル**し、**くすみピンクを新規追加**して計4テーマにする。

### 2-1. 「和」テーマの背景を刷新（しみったれたベージュ→生成り）
`src/styles.css` の `[data-theme="washi-vermillion"]` の **背景キャンバス色のみ**変更：
- `--bg-main`：`#f4ecd8`（今のくすんだベージュ）→ **`#f7f3ea`（生成り・白寄りで明るく）**
- セル背景 `--bg-cell` は白（#ffffff〜#fffefb）で明るく保つ（既に白系ならそのまま）
- **外周十二支の色**を、薄いグレーではなく **墨 `#2b2b2b`** にする（明るい背景でグレーは沈むため）。
  - ※ 十二支の色指定箇所（`.zodiac-label` 等）がテーマ変数を見ていない場合は、テーマごとに切り替わるよう変数化する（例 `--zodiac-color`）。

### 2-2. 「くすみピンク」テーマを新規追加
`src/styles.css` に `[data-theme="dusty-pink"]` を新設（既存テーマと同じ変数セットを定義）：
- `--bg-main`：**`#f3e3e0`（くすみピンク・淡）**
- `--bg-cell`：白系（`#fdf8f7` 等、ほぼ白で宮セルを明るく）
- 外周十二支の色：**小豆 `#6b3f3a`**（くすみピンク背景で映える濃色）
- その他の変数（border, text系, success/danger, glow）は **和テーマ（washi）と同系の値**を流用してよい。文字が読める明るさを確保すること。

### 2-3. 切替UIに「くすみピンク」を追加
- ヘッダーのテーマ切替ボタン群に4つ目「くすみピンク」（ラベルは「桜」または「ピンク」など短く）を追加。
- クリックで `document.documentElement.dataset.theme = 'dusty-pink'`、`localStorage.setItem('kimon-theme','dusty-pink')`。
- 既存の切替ロジック（useState・localStorage読込）に新テーマを組み込む。

---

## タスク3：盤情報セクションのデザイン刷新（案1・柔らかカード型）

### 問題
盤情報セクション（`src/components/MetaPanel.jsx`）だけ古臭い。明朝っぽいフォント・項目間の余白が間延び・黒背景が重い。盤と象意パネルが現代的になったのに、ここだけ取り残されている。

### 対象セクション（3ブロックすべて統一）
`MetaPanel.jsx` の以下3ブロックを、同じ「柔らかカード型」に統一する：
1. **盤情報**（盤種・局数・キー干支・旬首・直符・直使・空亡・旬首天盤宮・旬首地盤宮）
2. **干支**（年・月・日）
3. **盤レベル判定**（空亡・伏吟・反吟・門迫・五不遇時など該当時表示）

### デザイン（案1：柔らかカード型）
- 各項目を**小さなカード**にして、**2列グリッド**で並べる（`grid-template-columns: repeat(2, 1fr); gap: 8px`）。
- 1項目カード：項目名（小さめ・薄い色）を上、値（大きめ・太字）を下に。`border-radius` 11px 前後、内側 padding 9〜11px。
- 各ブロックに見出し（「盤情報」「干支」「盤レベル判定」）。見出しは左に細い縦バー＋ゴシック太字（明朝をやめる）。
- **フォントは Noto Sans JP（ゴシック）に統一**。明朝は使わない。
- **余白を詰める**（今の間延びを解消）。
- 値が長い項目（旬首天盤宮など）も2列に収まるよう調整。収まらなければその項目だけ1列ぶち抜きにしてよい。

### 配色（テーマ変数で切り替わる）
- カード背景・項目名・値・見出しの色は、**テーマCSS変数**を使う（背景キャンバスと同じくテーマで変わる）。
  - 例：くすみピンク→カード白系/見出し小豆/項目名くすみ/値こげ茶。黒金→カード濃灰/見出し金/値白。
  - 既存テーマ変数（--bg-cell, --text-primary, --text-secondary, --text-accent 等）を流用してよい。
- **情報は一切減らさない**（項目を可愛くするだけ。盤種〜旬首地盤宮・年月日・盤レベル判定すべて残す）。

### 参考イメージ
`board_info_designs.html`（同梱）の **案1（柔らかカード型）** のとおり。他の案2〜4は不採用。
※ 参考HTMLはくすみピンク背景で描いているが、実装ではテーマ変数で4テーマすべてに対応させる。

---

## 変えてはいけないもの（全テーマ共通・厳守）
- 宮名の黄、三奇（乙丙丁）の青、八門の赤青ベタ塗り白抜き、点数の緑/赤、象意パネルの色分け（吉青/凶赤/中立）
  → これらは**盤・象意の中身の色**。テーマで変わってはいけない。今のまま。
- 盤レイアウト（aspect-ratio固定の9宮同一サイズ、2×2配置、八門下中央 等、今日刷新した内容）
- 判定エンジン・スコア計算

## 変えるもの（テーマで切り替わる）
- 背景キャンバス色（--bg-main）
- 外周十二支の色（明るいテーマは濃色：和=墨#2b2b2b / くすみピンク=小豆#6b3f3a）
- （必要なら）セル背景・枠・テーマ固有の地の色

---

## チェック
- [ ] アプリを開くと初期日付が「今日」になっている（JST基準・前日ずれなし）
- [ ] テーマが4つ（黒金・紺銀・和・くすみピンク）切り替えられる
- [ ] 「和」の背景が生成り #f7f3ea で、十二支が墨 #2b2b2b でくっきり読める
- [ ] 「くすみピンク」の背景が #f3e3e0 で、十二支が小豆 #6b3f3a で読める
- [ ] どのテーマでも宮セルの中身の色（黄/青/赤青/緑）が変わっていない
- [ ] 明るいテーマ（和・くすみピンク）で文字・十二支が薄すぎず読める
- [ ] 切替→リロードで前回テーマが復元される（localStorage）
- [ ] 盤情報・干支・盤レベル判定の3ブロックが案1（カード型2列）になり、Notoゴシック・余白が詰まっている
- [ ] 盤情報の項目が一切減っていない（盤種〜旬首地盤宮・年月日・盤レベル判定すべて表示）
- [ ] 盤情報セクションも4テーマで地色・文字色が切り替わる
- [ ] スマホ幅で4テーマとも盤が崩れない
- [ ] npm test 全green / npm run build 成功

## 作業
- main 最新化 → ブランチ `git checkout -b phase-today-theme-metapanel`
- 実装 → npm test → dev で4テーマ＋初期日付を実機（スマホ）確認
- commit → push → PR（マージは本人が行う）
