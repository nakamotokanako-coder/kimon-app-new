# 🎯 先生指示反映タスク（2026-05-18受領分）

**作成日**: 2026-05-18
**作成者**: Claude Web（指示書フェーズ）
**実行者**: Claude Code（kimon-app ローカル環境）
**プロジェクト**: kimon-app（C:\Users\nakam\OneDrive\ドキュメント\kimon-app）

---

## 📨 先生からの新情報サマリ

### ① 日局数の最終版データ提供

- 添付: `超神節気_日局数_.xlsx`（Sheet3）
- **期間**: 2026-01-01 〜 2044-01-31（6605日分）
- **形式**: `日付` / `日局数` の2列、毎日1行
- **状態**: **最終版・続きなし**（過去日付や2044-02-01以降は提供されない）
- **法則確認済み**:
  - 陽局期: 毎日「陽n局 → 陽(n+1)局」と順行、9局で1サイクル
  - 陰局期: 毎日「陰n局 → 陰(n-1)局」と逆行
  - 夏至（6/21頃）で陽→陰、冬至（12/22頃）で陰→陽に切替
  - 整合性チェック済: 抜け0・重複0・パース失敗0

→ これは講座まとめ第2章 p29「日盤局数（節気＋上中下元の表で決定）」の正統な実装をlookup表化したもの。

### ② 刑格の二重表記対応（ダブルカウント確定）

天盤=庚 × 地盤=己 の宮について：

| 算出 | 既存実装 | 修正 |
|---|---|---|
| 十干剋応 | `官符刑格`（−10） | そのまま |
| **格局** | **空欄** | **「刑格」を追加（−10）** |

→ **合計 −20 のダブル減点** が正しい挙動。

理由（先生）: 「算出の種類が異なる」=別概念として扱う。

---

## 📦 入力ファイル（このタスクの新規データ）

`day_kyokusu.csv`（このフォルダに添付）

- UTF-8 BOM付き
- 列: `date` (YYYY-MM-DD), `day_kyokusu` (例: `陽3局`, `陰7局`)
- 6605行、125KB

→ `kimon-app/data/day_kyokusu.csv` として配置。

---

## 🛠 実装タスク

### Phase 1: 日局数の差し替え

#### 1-1. データ配置
- [ ] `day_kyokusu.csv` を `kimon-app/data/` にコピー
- [ ] 既存の `koyomi_v3.csv` の `kyokusu` 列は **削除しない**（時盤や他用途で参照される可能性のため温存。仕様確定後に削除判断）

#### 1-2. ローダー新規作成
- [ ] `src/kimon/loadDayKyokusu.js` を作成
  - PapaParse で `data/day_kyokusu.csv` を読む
  - 戻り値: `{ [date]: day_kyokusu, ... }` の Map 形式（O(1)ルックアップ）
  - 既存 `loadKoyomi.js` を参考に同じ流儀で

#### 1-3. buildBoard.js の日盤分岐を修正
- [ ] `boardType === '日'` のとき、局数の取得元を
  - 旧: `koyomi[date].kyokusu`
  - 新: `dayKyokusu[date]` （新ローダーから取得）
- [ ] **範囲外日付のエラー処理**: 2026-01-01より前 or 2044-01-31より後の場合
  - 明示的エラー: `throw new Error(\`日盤データが提供されていない日付です: ${date}（対応期間 2026-01-01〜2044-01-31）\`)`
  - or 警告ログ + null返却（UI側でハンドル）
  - **方針はぶりちゃんと相談**（推奨: 明示エラー、UI側で「日盤は2026年以降のみ対応」と表示）

#### 1-4. テスト
- [ ] `test/buildBoard.test.js` に日盤の新ロジック検証ケースを追加
  - 例: 2026-01-01 (陽3局) の日盤が正しく組まれるか
  - 陰陽切替日 (2026-06-21=陰7局) の日盤も検証
  - **先生に「2026-01-01 戊午日の日盤」の正解を1つ送ってもらえると確実**
- [ ] 範囲外日付のエラーテスト

### Phase 2: 刑格の二重表記対応

#### 2-1. kakkyoku.json に「刑格」エントリ追加
- [ ] 条件: `tenban_kan === '庚' && chiban_kan === '己'`
- [ ] 種別: `凶格`
- [ ] スコア: `-10`
- [ ] 重複8格局リスト（青龍返首・飛鳥跌穴・刑格・大格・戦格・小格・焚入太白・太白入熒）から **「刑格」を除外**
  → 5/12時点で「重複として削除」していたのを **復活させる**

#### 2-2. kakkyoku.js で検出ロジック実装
- [ ] 複合表記（戊丁、丙己等）の場合は分解して個別判定
  - 「丙己」の宮で天盤=庚、地盤=己 → 該当しない
  - 「庚」の宮で地盤=「己丁」 → 「己」を含むので該当
  - splitKan ヘルパー流用想定

#### 2-3. CSV再生成
- [ ] `scripts/generate-kakkyoku-csv.mjs` を再実行
- [ ] `data/chito_v2_with_kakkyoku.csv` の kakkyoku 列に「刑格」が出るか確認
  - 例: `陽1局甲子` 〜 `陰9局癸亥` の中で、天盤庚×地盤己になる宮を抽出 → 「刑格」表記がついていればOK

#### 2-4. テスト
- [ ] `test/kakkyoku.test.js` に刑格検出ケースを追加
  - 天盤庚×地盤己 → kakkyoku に「刑格」が含まれる
  - スコア計算: 該当宮が **−20** になる（十干剋応−10 + 格局−10）
- [ ] `test/scoreEngine.test.js` でダブルカウントの検証

---

## ✅ 完了条件

1. `day_kyokusu.csv` が `data/` に配置され、ローダー経由で読める
2. `buildBoard({date: '2026-01-01', boardType: '日'})` が新局数 `陽3局` で動作
3. 範囲外日付で明示的エラー（仕様確定後）
4. 天盤庚×地盤己の宮で、十干剋応「官符刑格」+ 格局「刑格」の両表記が出る
5. 該当宮のスコアが −20（十干剋応−10 + 格局−10）になる
6. `npm test` で全テスト pass
7. `npm run dev` で UI 上、2026-01-01 日盤の盤が正しく表示される

---

## ⚠️ 注意点・後続課題

### A. 旧 `kyokusu` 列の今後
- 5/18時点で年盤・月盤は削除済
- 現状、旧 `kyokusu` 列は使用箇所が残っている可能性 → 全箇所洗い出して整理（別タスク）

### B. 過去日付（2026-01-01より前）の鑑定
- 先生「もう続きない」=提供しない
- 過去日鑑定の需要は薄いと想定（奇門遁甲は未来向き）
- UI側で「日盤は2026-01-01以降のみ」とアナウンス推奨

### C. 範囲外日付のエラー文言
- ぶりちゃんに最終確認: 「2026年以前の日盤は鑑定対象外」というメッセージで良いか

### D. 引き継ぎメモ更新
- 完了後に `引き継ぎメモ_2026-05-XX.md` を新規作成
- 反映内容:
  - 刑格の二重表記対応（5/12の「解決済み」は撤回）
  - 日局数の新データ運用開始
  - 残課題（A-1 UIレイアウト・A-2 残り格局修正）は持ち越し

---

## 🧪 TDD推奨ステップ

1. ローダーのユニットテスト書く
2. ローダー実装 → green
3. buildBoard の日盤ロジック差し替えテスト書く（先生提供の正解1ケース）
4. buildBoard 修正 → green
5. 範囲外エラーテスト書く → 実装 → green
6. kakkyoku.json に刑格追加 → kakkyoku.js でテスト書く → 実装 → green
7. CSV再生成 → 目視確認
8. scoreEngine のダブルカウントテスト書く → green
9. 全テスト → 全UI動作確認 → コミット → push（Netlify自動デプロイ）

---

**最終更新**: 2026-05-18
**次のアクション**: このファイルと `day_kyokusu.csv` を Claude Code に渡してフェーズ開始
