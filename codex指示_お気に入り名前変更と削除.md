# Codex指示：お気に入りに「名前変更」と「削除」を追加

> 対象：地図「吉方位」タブの **お気に入り一覧**（`src/reverseDirection/` 配下、DirectionMap.jsx等）。
> 目的：今は住所がそのまま名前になっているお気に入りに、ユーザーが**好きな別名（「実家」「会社」等）**を付けられるようにする。あわせて**削除**もできるようにする。
> 重要前提：お気に入りは localStorage 保存。**既存データ構造を壊さない・後方互換を保つ**こと（既存のお気に入りが消えたり表示崩れしたりしないこと）。

---

## 0. まず現状把握（実装前に必ず）
- お気に入りのデータ構造・localStorageのキー名・保存/読込関数の場所を確認する：
  - `rg -n "localStorage|favorite|お気に入り|favourites" src` で保存/読込箇所を特定。
  - 1件のお気に入りオブジェクトが現在どんなフィールドを持つか（例：住所文字列・lat・lon・方位・スコア等）をソースで確認。
- 既存フィールド名・型は**変更しない**。今回は表示名フィールドを**追加**する方針。

---

## 1. データ：表示名フィールドを追加（後方互換）
- お気に入り1件に **表示名フィールド `label`（任意・文字列）** を追加する。
  - 新規追加時：`label` は未設定（空 or undefined）でよい。
  - 名前変更時：ユーザー入力を `label` に保存。
- **後方互換**：`label` が無い既存お気に入りは、これまで通り住所を名前として表示する。
  - 表示名の決定ロジック（ヘルパー）：`displayName = fav.label?.trim() ? fav.label : fav.address`（住所フィールド名は現状に合わせる）。
- localStorageの保存/読込はそのまま（フィールドが1つ増えるだけ。JSON.stringify/parseで自然に通る）。古いデータに `label` が無くても読込でエラーにならないこと。

---

## 2. 表示：別名＋住所を小さく残す（2段表示）
お気に入り一覧の各行を、次の2段にする：
- **1段目（大きめ）**：表示名 `displayName`（labelがあれば別名、無ければ住所）。
- **2段目（小さめ・薄め）**：常に**住所**を表示（既に「家から約○km」を出している行は、住所＋距離を小さく併記）。
  - こうすることで「実家」と名付けても、その下に「群馬県渋川市…」が小さく残り、どこか分かる。
- 右側の方位バッジ（例：北西 +60 / 南 -20）と色（吉=青/凶=赤ドット）は**現状のまま変更しない**。

例（イメージ）：
```
● 実家                                   北西 +60
  群馬県渋川市赤城町津久田291番地 ・ 家から約106.3km
```
labelが無い場合（従来どおり）：
```
● 東京都板橋区幸町66番4号                南西 +20
  家から約2.4km
```

---

## 3. 編集UI：各行に鉛筆アイコン（行タップの地図遷移と分離）
- 各お気に入り行に、小さい**鉛筆（編集）アイコン**を置く（右端、方位バッジの近く or 行の端）。
- **行本体のタップは従来動作（その場所の地図/方位へ）を維持**。編集は鉛筆アイコンのタップで起動する。
  - 理由：行タップに編集を載せると「地図に飛びたいのに編集が開く」誤爆が起きる。操作を分ける。
- 鉛筆アイコンをタップ → **編集モーダル/シート**を開く。中身：
  - テキスト入力（現在の `displayName` を初期値に。プレースホルダ「お気に入りの名前」）。
  - 「保存」ボタン：入力を `label` に保存し、一覧を即時更新。空のまま保存したら `label` をクリア（＝住所表示に戻る）。
  - 「キャンセル」。
  - **「削除」ボタン**（赤系・§4）。

---

## 4. 削除
- 編集シート内に「このお気に入りを削除」ボタンを置く。
- 押下時は**確認**を出す（「『実家』を削除しますか?」的な簡易confirm or 確認ダイアログ）。即削除しない（誤爆防止）。
- 確認OKで、そのお気に入りを localStorage から除去し、一覧から消す。
- ※将来「並べ替え」も足したくなる可能性があるが、**今回はやらない**（名前変更＋削除のみ）。

---

## 5. 触ってよい / 触ってはいけない
### 触ってよい
- お気に入り一覧の表示コンポーネント（行のレイアウト・鉛筆アイコン・編集シート）。
- お気に入りの保存/読込関数に **labelフィールドの追加・名前更新・削除** を足す。

### 触ってはいけない
- 方位スコア計算・方位バッジの値/色・地図の扇/ピン・距離計算。
- 既存お気に入りの **lat/lon/住所/スコア等のフィールド名と型**（labelを足すだけ）。
- localStorageのキー名（既存キーをそのまま使う。新キーを作って二重管理しない）。
- src/kimon/ エンジン群、`/api/overpass`、施設検索ロジック。

---

## 6. テスト
- 表示名ヘルパー：labelあり→label、labelなし→住所、label空白のみ→住所。
- 名前変更：labelが保存され、displayNameが切り替わる。空保存でlabelクリア＝住所表示に戻る。
- 削除：対象が一覧とlocalStorageから消える。他のお気に入りは残る。
- **後方互換**：labelフィールドが無い既存形式のデータ（旧JSON）を読み込んでもエラーにならず住所表示される、を1ケース入れる。
- 既存テスト全green維持。

---

## 7. 進め方
1. `git checkout main && git pull` → `git checkout -b feat-favorite-rename`（mainから新規ブランチ）。
2. §0で現状構造を把握 → §1〜4実装 → §6テスト。
3. `npm test`（全green）＋ `npm run build`（成功）。
4. push前に `git log --oneline origin/main..feat-favorite-rename` で本機能のコミットだけか確認。
5. **名指し add**（お気に入り関連の変更ファイルのみ。`git add .` 禁止＝先生資料の未追跡ファイルを巻き込まない）。
6. push → PR → **Vercelプレビューで実機目視**：
   - お気に入りの鉛筆→名前変更→一覧に別名＋住所小表示。
   - 空保存で住所表示に戻る。
   - 削除→確認→消える。他は残る。
   - 既存お気に入り（旧データ）が崩れず表示される。
7. 全部OKで手動マージ → **Vercel概要で本番展開コミットが最新か確認**（古ければ「プロダクションへの昇格」）。
