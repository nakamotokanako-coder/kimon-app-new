# Codex指示：宮セルの象意が5件で枠から漏れる問題の修正

**作成**: 2026-05-25 / Claude Web
**実行**: Codex（`C:\dev\kimon-app`）
**対象**: `src/styles.css`（宮セルの高さ・象意エリア）／必要なら `src/components/PalaceCell.jsx`
**最重要**: 盤の配色・判定ロジック・テーマは触らない。**9宮の同一サイズは絶対維持**。

---

## 🔴 問題

本番（今日 2026/05/25 の盤）で、**左下の艮宮の象意が枠から漏れている**。
艮宮は象意が **5件**：
- △華蓋蓬星 / ×反吟大格 / ○鬼遁 / ×月格 / ×六儀撃刑格

現状の `aspect-ratio: 1/1.28` は「象意4件」を基準に決めたため、**5件目（×六儀撃刑格）がセル下枠から漏れる**。
※ お手本HTML上は収まって見えても、実機のフォントレンダリングと余白差で漏れる。実機基準で直すこと。

---

## ✅ 直し方（方針）

**「象意5件が確実に枠内に収まる」かつ「9宮は全て同一サイズ」を両立させる。**

比率の数値そのものより、「5件が漏れない作りにする」ことが目的。以下を組み合わせて対応：

### 1. セル比率を少し縦に伸ばす
- `aspect-ratio` を `1/1.28` → **`1/1.34` 目安**に。
- 9宮すべて同じ比率（順利宮・通常宮・中宮すべて同一）。**高さは必ず一致**。
- 1/1.34 で足りなければ 1/1.36 程度まで可。ただし伸ばしすぎると間延びするので、下記2・3と組み合わせて最小限に。

### 2. 八門と象意の間の余白を詰める
- 現状、八門（.mon-row 等）の下と象意（.info 等）の間に余白がありすぎて、その分5件目が押し出されている。
- 八門の下マージン・象意の padding-top を詰める。

### 3. 象意の行間を少し詰める
- 象意（.info の各行）の line-height をやや詰める（例 1.5 → 1.4 程度）。
- font-size は読める範囲で微調整可（小さくしすぎない）。

### 4. あふれ保険（将来6件以上でも漏れないように）
- 5件を基準に収めた上で、**万一それ以上の件数が出ても枠外に漏れない**ようにする。
- 方法：象意エリアに `overflow` 制御を入れる、または象意エリアを `flex:1; min-height:0;` にして、はみ出す場合はエリア内で吸収（必要なら内部スクロール）。
- 「漏れて隣の宮や枠外に重なる」状態だけは絶対に避ける。普段（1〜5件）は全部見えること。

---

## 検証（実機 or DevTools スマホ幅・必ず今日の盤で）
- [ ] 2026/05/25 日盤の **艮宮の象意5件が全部、枠内に収まって見える**（×六儀撃刑格まで）
- [ ] 9宮すべて同一サイズ（順利宮・通常宮・艮宮の高さがピクセル一致）
- [ ] 360 / 375 / 390 / 414px で艮宮の象意が漏れない
- [ ] 八門が枠から漏れていない（前回直した状態を維持）
- [ ] 他の宮（象意1〜3件）も不自然な余白なく自然に見える
- [ ] 4テーマすべてで崩れない
- [ ] npm test 全green / npm run build 成功

## 変えないもの
- 宮名の黄・三奇の青・八門の赤青ベタ・点数の緑赤・象意の色分け
- 2×2配置（天盤干=八神 / 地盤干=九星）・八門下中央・順利の青枠
- 判定エンジン・スコア計算・テーマ

## 作業
- main 最新化 → ブランチ（例 `phase-cell-overflow-fix`。直近の作業ブランチに続けてもよい）
- 実装 → npm test → dev で**今日の盤の艮宮**を実機（スマホ）確認
- commit → push → PR（マージは本人）
