# Codex実装指示：日盤モード（遠出）本実装 ― 段階6

> 対象：設計図「設計図_逆引き方位検索.md」§4-2・§8の **段階6「日盤モード」の本実装**。
> 現状：吉方位タブ内に「日盤 遠出」のタブ枠はあるがプレビュー止まり。これを本番化する。
> 見た目の確定モック：**`nichiban_map_proto.html`**（このファイルと同じ場所に置く。実機=Vercelプレビューで確認した「見た目の正解」）。
>  ※検証用の `nichiban_map_proto_TEST.html` はLeaflet同梱＋タイルをダミーグリッドに差し替えたもの。実装の参照は本番版 `nichiban_map_proto.html` を使う。
> 前提：段階1〜5（メニュー化・時盤逆引き・時間帯一覧・地図扇・全画面/引き方トグル・目的フィルタ）は実装済み・本番反映済み。mainは安定最新。テスト全green維持が必須。

---

## 0. 大原則（必読・前指示書から継続）

- **既存の判定エンジンには一切手を入れない**。盤組立・点数計算（buildBoard / scoreEngine / kakkyoku / jukkanKokuou 等）は**呼ぶだけ**。
- **AIは使わない**。判定は全て既存の自前エンジン。
- **課金ゼロ・APIキー不要**を厳守。地図=Leaflet、タイル=既存と同じ淡色タイル（CartoDB Positron）。Google Mapsは使わない。
- 作業は新しいブランチで。`git checkout main && git pull && git checkout -b phase-nichiban`。
- 各ステップ後に `npm test`（全green維持）と `npm run build`（成功）を必ず確認。
- 実機確認は**Vercelプレビュー**で（位置情報API・地図タイルはHTTPS必須。LAN IPでは不可）。
- **この指示書の範囲を超えて実装しない**（§7「やらないこと」を厳守）。

---

## 1. この機能は何か（1行）

**「日盤 遠出」タブで、指定した“日付”について、家（基準点）から見た8方位の吉凶ランキングと地図の扇を出す。** 時盤モードとの違いは「単位が時刻ではなく日付」「距離スケールが徒歩圏でなく遠方（反転フェード）」の2点だけ。盤の組み方・点数化は既存エンジンを再利用する。

---

## 2. 時盤モードとの違い（実装すべき差分はこの2つだけ）

| | 時盤（既存・お散歩） | 日盤（本実装・遠出） |
|---|---|---|
| 入力単位 | 時刻（時辰・2時間ごと） | **日付**（1日単位） |
| 自然時補正 | あり（経度→補正分） | **基本不要**（1日単位） |
| 盤の組み方 | 時辰→buildBoard | **日付→日盤を組む**（§3） |
| 距離スケール | 確定500m＋外ほど薄いフェード | **確定50km＋外ほど濃い反転フェード**（§4） |
| 効果の目安 | 500m以上＋5分滞在／効果5日 | **50km以上＋3時間滞在／効果60日** |

> 上記以外（基準点GPS/場所検索、8方位45度均等・定位、目的フィルタ、吉/凶切替、円盤、全画面、引き方トグル、色テーマ）は**時盤と完全共通＝既存部品を使い回す**。新規に作り直さない。

---

## 3. 日付 → 日盤を組む経路（エンジンは呼ぶだけ）

- **まず既存の作盤経路を調査**：盤タブ（BoardGrid）は「今日の盤」を既に表示している。そこで**日盤（その日の日干支）から盤を組む関数が既にあるはず**。`src/kimon/` 配下で日干支→局→buildBoard に至る経路を確認し、**それを呼ぶ**。
  - 時盤逆引き（`reverseDirection.js`）が「時辰→盤→8方位スコア」をやっているので、**その時辰指定を日盤指定に差し替えた版**を作るのが最短。関数名は既存に合わせる（例：`rankDirectionsForDay(date, origin, purpose)`）。
- **日付の決め方**：UIの日付ピッカーで選んだ日付（ローカル日付）。その日の**日干支**を既存のこよみ参照（`koyomi_v3.csv` 等、既存ローダ経由）で引き、日盤の局を決めて buildBoard に渡す。**こよみ参照・局決定のロジックは既存のものを使う**（新規に暦計算を書かない）。
- **自然時補正は使わない**（日盤は1日単位）。時盤の補正コードを日盤に流用しない＝補正分の表示も日盤画面では出さない。
- 出力は時盤と同じ形：**8方位 ×（方位名・点数・吉凶レベル・代表象意）** の配列。これを地図とランキングが共有する（時盤と同じ）。

> 注意：日付境界（深夜0時前後）・節入りの厳密な扱いは**先生確認事項**（§8）。今回は「選んだ日付の日干支で1枚組む」で実装し、境界の特殊処理は入れない（仮）。

---

## 4. 距離プロファイルの切替（この機能の見た目の肝・モックのとおり）

時盤の扇描画ロジック（`mapFan.js` / `DirectionMap`）を**そのまま使い**、**距離プロファイルだけ時盤/日盤で差し替える**。扇の角度計算・色・引き方トグルは一切変えない。

### 4-1. 距離プロファイルを定数化して外出し
`mapFan.js`（または近接の定数ファイル）に、モード別の距離プロファイルを定義する。**時盤の既存値も同じ構造にまとめ直してよい**（リファクタはこの範囲に限定）。

```js
// 距離プロファイル（モック nichiban_map_proto.html の確定値）
export const DISTANCE_PROFILE = {
  jiban: {                 // 時盤（既存・お散歩）＝内が濃い → 外が薄い
    confirmKm: 0.5,        // 確定ライン 500m
    fadeMaxKm: 10,
    initialZoom: 13,
    // 内ほど濃い（徒歩圏を出ると効果が続くか不確か）
    fadeBands: [
      { from: 0,   to: 0.5, op: 0.55 },
      { from: 0.5, to: 2,   op: 0.40 },
      { from: 2,   to: 4,   op: 0.28 },
      { from: 4,   to: 7,   op: 0.18 },
      { from: 7,   to: 10,  op: 0.10 },
    ],
    rings: [ { km: 0.5, label: '500m 確定' }, { km: 10, label: '10km' } ],
  },
  nichiban: {              // 日盤（本実装・遠出）＝内が薄い → 外が濃い（★反転）
    confirmKm: 50,         // 確定（有効）ライン 50km
    fadeMaxKm: 250,        // 表示上限（実際は「遠いほど良い」）
    initialZoom: 7,
    // ★反転：50km未満は薄く、遠いほど濃い（効果が増す）
    fadeBands: [
      { from: 0,   to: 50,  op: 0.10 },   // 50km未満＝近すぎ・効果弱
      { from: 50,  to: 100, op: 0.28 },   // 有効ゾーン入口
      { from: 100, to: 160, op: 0.42 },
      { from: 160, to: 250, op: 0.55 },   // 遠いほど濃い＝強い
    ],
    rings: [ { km: 50, label: '50km 有効ライン' }, { km: 100, label: '100km' }, { km: 200, label: '200km' } ],
  },
};
```

- **凶のフェード係数**：時盤と同じ扱い（凶は吉より控えめ base=0.75 等、既存ルールを踏襲）。日盤でも同じ係数を適用。
- **中立（0点）**：時盤同様フェードは出さない（確定ゾーンのみ・控えめ）。

### 4-2. 確定（有効）ラインのリングを強調
- 確定ライン（時盤=500m / 日盤=50km）の円は**金 `#e6c34a`** で強調。モックでは「下地に太い半透明の金（weight 6, opacity 0.25）＋上に金の実線（weight 2.5）」の二重でグロー風にした。これを踏襲。
- 目盛りリング（100km/200km・時盤の10km等）は**淡いグレー破線**（`#c9c4b0`, dashArray '4 6'）。
- 各リングに小さなラベル（'50km 有効ライン' 等）を中心の北東寄りに置く（モック参照）。

### 4-3. 「反転」の意味をUIに明記（重要・先生にも一般ユーザーにも伝える）
時盤と日盤で**フェードの向きが逆**なのは奇門の作法に基づく仕様。誤解されないよう、日盤画面に短い説明を出す：
- 地図キャプション：「50km以上＋3時間滞在で効果（効果60日）。50km未満は近すぎ＝薄く、**遠いほど効果が増す**ので外ほど濃い（時盤と逆）。」
- 距離スケールのミニ凡例（モックの「ルーラー」）：日盤は「0 近すぎ → 50km 有効ライン → 200km〜 遠いほど強」。時盤は「0 起点 → 500m 確定 → 10kmへフェード」。
- ※このルーラーUIはモックにあるが、**実装の必須要件ではない**（地図＋キャプションで意図が伝われば可）。入れる場合はモックの作りを踏襲。

---

## 5. 画面構成（日盤タブ／モック `nichiban_map_proto.html` のとおり）

```
[基準点] 📍東京駅 / 現在地  （※自然時補正の行は日盤では出さない）
[行く日] 📅 2026年6月1日（月）   ← 日付ピッカー
🗺 地図：50km有効ライン＋反転フェードの扇（時盤の扇UIを距離プロファイル違いで再利用）
🏆 この日の方位ランキング（1位=金枠・地図のベスト扇と一致）
🔖 目的フィルタ ／ ☑吉のみ（時盤と共通部品）
（補助）出発時刻の時盤吉方位 ※先生確認後に出すか決める（今は文言のみ・実データは出さない）
```

- 基準点・目的フィルタ・吉/凶切替・円盤・全画面ボタン・引き方トグルは**時盤タブと同じ共通部品**。日付以外のUIを日盤用に作り直さない。
- 日付・基準点・目的フィルタのいずれかが変わったら、ランキングを再計算して地図にも反映（時盤と同じ連動）。
- ベスト方位（1位）＝地図の金枠の扇＝1位カード、が常に一致すること（時盤で踏んだ色/方位ズレバグの再発防止。**1位カードと地図を同じ配列から描く**）。

---

## 6. 触ってよい / 触ってはいけない

### 触ってよい（新規 or 追加）
- 新規：日盤ランキング関数（例 `rankDirectionsForDay`。中身は既存の作盤＋scoreEngineを呼ぶだけ）。
- 新規：日付ピッカーUL、日盤タブの本体配線。
- 追加：`mapFan.js` の距離プロファイル定数（§4-1）と、`DirectionMap` がプロファイルを prop で受け取って描き分ける最小の改修。
- 追加：上記の単体テスト。
- リファクタ可：時盤の既存距離値を §4-1 の `DISTANCE_PROFILE.jiban` に集約し直す（**見た目が変わらないこと**をテストで担保）。

### 触ってはいけない
- `src/kimon/` 配下のエンジン群（buildBoard, scoreEngine, kakkyoku, jukkanKokuou, banLevel, junri, timeKanshi, こよみ/局決定）＝**呼ぶだけ・出力を変えない**。
- 扇の角度計算（`bearingFor`・球面方位・西偏角）＝**変えない**。距離プロファイルだけ差し替える。
- 時盤モードの見た目・距離（500m/10km）＝**変えない**（プロファイル集約後も同じ描画になること）。
- 既存の盤UI（BoardGrid / PalaceCell）、円盤（CompassWheel）の点数ロジック。
- データ（shoui_dict.json / chito_v2.csv / koyomi_v3.csv 等）＝**読むだけ**。

---

## 7. やらないこと（範囲外）

- **道・経路の描画、目的地ピンの自動配置**はしない（時盤と同じ方針＝方角の扇＋距離の範囲を示すまで。どの道・どの場所へ行くかは使う人が地図を見て選ぶ）。
- **出発時刻の時盤併用の実データ表示**は今回入れない（文言・枠のみ。出すかは先生確認＝§8）。
- **日付境界・節入りの厳密処理**は入れない（選んだ日付の日干支で1枚組む・仮）。
- **期間スキャン系**（今月の最強ランキング・奇門三盤ルート・方角固定の日探し）は将来機能（設計図§10/§10-2）。今回作らない。
- **距離の最終確定はしない**：50km/3時間は講座資料＋一般通説で裏取り済み（下記§8参照）だが、定数化して後から差し替え可能にしておく。
- 24方位/12方位、精密地磁気モデル＝将来。

---

## 8. 完成後に先生へ確認（チェックリスト追記分）

- **距離（報告ベース）**：日盤は「**50km以上＋3時間滞在／効果60日**」で実装した（先生の講座資料『効果期間の表』のとおり。一般の複数流派とも一致＝日帰り行軍≒39km・50kmで無難・50〜100km以上が理想）。この採用でよいか念のため確認。
- **日付境界**：深夜0時前後・節入りまたぎの日の扱い（前日の盤を引きずるか等）。今は「選んだ日付の日干支で1枚」。
- **出発時刻の時盤併用**：遠出日に出発時刻の時盤吉方位を補助表示するか（本格作法では出発の瞬間に時盤の吉を5〜10分取る）。
- **球面方位の既定**：日盤は遠距離なので球面方位の影響が出る（数百km・海外で平面とズレる）。引き方トグルの既定を日盤では球面にするか／平面のままか。先生の流派の作法を確認。
- （継続）方位は定位・立向盤でよいか、五不遇時の扱い、目的フィルタ吉要素の○×、ランキング同点処理。

---

## 9. 進め方

1. **既存の作盤経路を調査**：盤タブが日付からどう盤を組んでいるかを確認し、日盤ランキング関数 `rankDirectionsForDay(date, origin, purpose)` を**既存関数を呼ぶだけ**で実装。時盤の `reverseDirection.js` を雛形にする。
2. `mapFan.js` に距離プロファイル定数（§4-1）を追加。時盤の既存値も同構造に集約（見た目不変をテスト）。
3. `DirectionMap` がプロファイルを prop で受け取り、確定ライン・反転フェード・リングを描き分けるよう最小改修。日盤プロファイルで反転フェード＋50km金リングが出ることを確認。
4. 日盤タブを本体配線：日付ピッカー → `rankDirectionsForDay` → ランキング＋地図に反映。基準点/目的フィルタ/日付変更で再描画。
5. 1位カード＝地図の金枠扇＝円盤の一致を実機で必ず確認（色/方位ズレの再発防止）。
6. `npm test`（全green維持）/ `npm run build`（成功）→ Vercelプレビューで実機（iPhone）確認 → PR。**マージはユーザー（かなこ）が手動**。Codexはpushまで。

*見た目の正解は `nichiban_map_proto.html`。距離の確定値（50km/反転フェード）はそこから取った。時盤との違いは「日付入力」と「距離プロファイル（反転）」の2点だけで、他は全部既存部品の使い回し。エンジンには触らない。*
