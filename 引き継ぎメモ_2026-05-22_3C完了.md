# 引き継ぎメモ 2026-05-22（3-C完了版）

**作成者**: Claude (web chat)
**作成日時**: 2026-05-22 夜
**前回メモ**: 引き継ぎメモ_2026-05-22.md（点数仕様確定・新リクエスト3件・App Store方針）

> このメモは前回 5/22メモの続き。前回メモの「点数判定仕様」「新リクエスト3件」
> 「Phase 4」セクションは引き続き有効。本メモは **3-C の確定・完了** を追記する。

---

## 🎯 本日この後の総括（3-C 一気通貫で完了）

| # | トピック | 成果 |
|---|---|---|
| 1 | **3-C 仕様を先生監修で完全確定** | 質問書→先生回答で不明点ゼロに |
| 2 | **3-C（◎順利・秘伝判定）実装・マージ完了** | `junri.js` 新規・302件テスト全 green |
| 3 | **3-A のテスト追従漏れを是正** | 死門0→-40 の更新漏れを 3-C作業中に修正 |

---

## ✅ 3-C 確定仕様（先生監修済み・実装反映済み）

「◎順利・秘伝判定」。実装はこの通りで完了している。

1. **日干のある宮の特定**：その日の日干（日干支の天干）が配置されている宮を
   **天盤の干**で探す（先生確定。地盤ではない）。
   - 日干が天盤のどこにも無い場合 → **旬首（junshu）の宮を代替に使う**（先生指示・5/11受領）
   - 複合表記（中宮処理で2干が並ぶ宮）は **片方ずつ分けて**判定し、どちらかが日干ならその宮
2. **対象宮テーブル**（日干のある宮 → ◎順利になりうる宮）:

   | 日干のある宮 | 対象宮 |
   |---|---|
   | 坎 | 坎・兌・乾 |
   | 艮 | 艮・離 |
   | 震 | 震・巽・坎 |
   | 巽 | 震・巽・坎 |
   | 離 | 離・震・巽 |
   | 坤 | 坤・離 |
   | 兌 | 兌・乾・艮・坤 |
   | 乾 | 兌・乾・艮・坤 |

   ※兌・乾は同じ4宮セット（先生確定・意図通り）
3. **◎順利の条件**：対象宮が「吉門（生・休・開・景）」かつ「合計40点以上」の両方
   - 「合計40点以上」= `scorePalace().score`（盤レベル減点込みの最終値）。usable閾値と一致
4. **表示**：該当宮は評価ランクを **「◎◎効果最大」に格上げ**（最優先）

---

## 🛠 3-C 実装の中身（マージ済み）

### 追加・変更ファイル
- **新規** `src/kimon/junri.js`
  - `JUNRI_KICHIMON`（吉門セット）/ `JUNRI_TARGET_TABLE`（対象宮テーブル）
  - `findDayKanPalace`（日干の宮特定。天盤優先→旬首フォールバック）
  - `detectJunri`（◎順利判定本体）
- **修正** `src/kimon/scoreEngine.js`
  - `scoreBoard()` の戻り値に `day_kan_palace` / `junri_palaces`（英字キー配列）を追加
  - 各 `palaceScores[name]` に `is_junri: boolean` を付与
  - **点数計算ロジックは無変更**（3-Aの検算を崩さないため）
- **新規** `test/junri.test.js`（20ケース）
- **追記** `test/scoreEngine.test.js` Phase 3-C describe（5ケース）

### 旬首フォールバックの順序（実装メモ）
1. `tenban_junshu_p`（日本語名→英字キー）を優先
2. 未設定なら `meta.junshu` を天盤スキャン
- 実盤の chito_v2.csv では `tenban_junshu_p` が常設なので通常1段目で確定

### テスト結果
- **9ファイル / 302ケース 全 green**
- 3-C新規25ケース＋pre-existing修正1件、すべて通過

### pre-existing テストの是正（3-C作業中に発見・修正）
- `test/scoreEngine.test.js:116`「完全な凶宮（庚+死門…）」が `expect(-50)` だが実測 `-90`
- 原因：**3-A（死門 0→-40）実装時に、この旧テストの期待値を更新し忘れていた**
- 対応：**テスト期待値を -50→-90 に修正**・コメントを「死門 0→-40」に更新
- `SCORE_MON` は無変更（触ると 3-A の検算が崩れるため）。3-C の不具合ではない

---

## 🔗 3-B への接続（重要・次フェーズの起点）

3-C は「◎順利フラグの検出」までで、評価ランク決定は 3-B の担当。

- 各宮：`palaceScores[name].is_junri`（boolean）
- 盤全体：`scoreBoard().{ day_kan_palace, junri_palaces }`（英字キー配列）

→ **3-B の評価ランク決定ロジックで `is_junri === true` を「◎◎効果最大」の
   最優先キーとして参照**すれば、そのまま接続できる。二度手間なし。

---

## 📌 3-C の残タスク（UIのみ・後回しでOK）

- ロジックは完了。**UIバッジ表示は未着手**（仕様通り意図的に切り出し）
  - PalaceCell の◎順利バッジ / `cell-junri` クラス / CSS
- → 3-D（象意パネル）や中宮表示まわりの**表示作業とまとめてやる方が効率的**

---

## 🎯 次に着手すべきもの

### 最有力：3-B（評価ランク判定）
- ただし **上位ランクは先生確認待ち**：
  - **Q2＝「XX超危険」の定義**（"伏吟反吟が3つ" の解釈、A〜D未回答・保留中）
  - **Q4 の五不遇時の扱い**（△注意の条件に五不遇時を含むか）
- 下位ランク（◎とても良い・〇良い・－無難）と ◎◎効果最大（is_junri接続）は
  先生確認なしで着手可。XX超危険だけ仮置きにすれば部分着手できる
- ◎◎効果最大は 3-C の `is_junri` を最優先キーに使う

### その他の着手可タスク
- **3-D（象意パネル改修）**：枠は着手可・解説テキストは後日先生受領
- **3-G（期間検索）**：着手可・chito_v2.csv 活用
- **3-F（中宮表示）追加分**：門迫を中宮に出すかは先生確認待ち

---

## ❓ 次回先生に確認すること（3点）

3-B を完全着手するために、まとめて聞くのが効率的：

1. **Q2：XX超危険の定義** — "庚・凶門・五不遇時・伏吟反吟が3つ、すべて揃う" の
   「伏吟反吟が3つ」とは？（5種中3つ以上／ちょうど3つ／宮単位で3重 など）
2. **Q4：△注意の五不遇時の扱い** — △注意＝「凶門あり庚なし」で確定済みだが、
   「五不遇時も無い」条件を含むか先生未言及。含む／含まない を確認
3. **門迫を中宮に出すか** — 3-F の中宮表示は伏吟反吟5種＋五不遇時の6項目のみ。
   門迫は現状中宮に出ない（盤情報パネルには出る）。これでよいか

> ※質問は必ず「選択肢化」して先生が○×で答えられる形にする（質問力PM運用ルール）

---

## 💬 次チャットへの引き継ぎポイント

1. **3-C は完了**（ロジック・マージ・テスト302件green）。残はUIバッジのみ。
2. 完了済みフェーズ：3-A・3-E（PR#7）・3-F・**3-C**。
3. 3-C の成果物 `is_junri` / `junri_palaces` を **3-B で最優先キーに使う**。
4. 次の着手最有力は **3-B**。ただし Q2・Q4五不遇時の先生確認が上位ランクの前提。
5. 先生確認3点（Q2／Q4五不遇時／門迫中宮）を選択肢化してまとめて送る。
6. 3-C のUIバッジは 3-D・中宮表示の表示作業とまとめる方針。
7. 3-G（検索）は chito_v2.csv 活用（盤の再計算を避ける）。
8. App Store化は Phase 4（Web完成後）。今は触らない。
9. ブランチ運用：3-C も main マージ済。次は最新 main から
   `git checkout main && git pull && git checkout -b phase-XX-...`。

---

**最終更新**: 2026-05-22 夜（3-C完了反映）
**監修・共同開発**: ◯◯先生
