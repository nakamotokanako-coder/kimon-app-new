# 先生指示_盤デザイン刷新_実装指示書（スマホ/タブレット専用化）

**作成**: Claude (web chat)
**日付**: 2026-05-25
**対象リポジトリ**: `nakamotokanako-coder/kimon-app-new`
**正本ローカル**: `C:\dev\kimon-app`
**実装担当**: Codex / Claude Code
**お手本HTML**: `board_design_v4.html`（このリポジトリに同梱。実際に動く完成イメージ。CSSの値はここを正とする）

---

## 0. このタスクの本質（最初に必ず読む）

今の本番（kimon-app-new.vercel.app）は、**端末によって盤の見え方が崩れる**。
具体的には、順利バッジが付く宮（震・坎）だけ縦に伸び、つられて隣の宮もサイズがバラバラになる。
iPhone では偶然きれいに見えるが、別端末（Android 等）では順利宮が間延びして崩れる。

**根本原因**: 宮セルの高さが「中身の量」で決まる作りになっている。
**根本対策**: 宮セルを `aspect-ratio` で外形固定し、中身・端末・画面幅によらず必ず同一サイズにする。

これが今回の **最重要要件①**。デザインの綺麗さより先に、まずこの「崩れない」を達成すること。

あわせて、**このアプリは PC では使わない。スマホ／タブレット専用にする**（最重要要件②）。
PC 幅対応は考慮不要。その代わり盤に最大幅を設け、PC で開いてもスマホ幅で中央表示されるようにする。

---

## 1. 最重要要件（絶対に守る）

### 要件① 9宮セルのサイズを固定（端末・中身によらず同一サイズ）
- 各宮セルは `aspect-ratio: 1 / 1.12` で外形を固定する。幅が決まれば高さは比率で自動決定。
- **順利バッジの有無、象意の件数（1〜4件）、干の数（1〜3個）に関わらず、9マスすべてが必ず同じ形・同じ大きさになること。**
- 中身がセルからあふれそうな場合は、外形を変えずに以下のいずれかで吸収する:
  - 象意が多い宮 → 象意のフォントを少し縮める / `overflow` で抑える
  - 干が3つの宮（巽=丁戊乙、坤=己丁戊 等）→ 干エリア内で収める
- グリッドは `display:grid; grid-template-columns:repeat(3,1fr); gap:9px;`。中宮（空マス）も同じ aspect-ratio で同サイズ。
- **検証必須**: 順利あり宮（震・坎）と順利なし宮（離・乾 等）の高さがピクセル単位で一致すること。

### 要件② スマホ／タブレット専用化
- 盤全体に `max-width` を設定し、中央寄せ（`margin:0 auto`）する。推奨 `max-width: 480px`。
- 想定端末は **スマホ＋タブレット**。タブレットでも盤はこの最大幅内に収まり、画面中央に表示される。
- PC で開いても崩れず、スマホ幅の盤が中央に出るだけ（PC専用レイアウトは作らない）。
- 想定幅レンジ: 360px〜480px で完璧に。それ以上の幅では中央固定表示。
- viewport: `<meta name="viewport" content="width=device-width, initial-scale=1.0">`

---

## 2. デザイン刷新の変更点（現状からの差分）

> 今の本番は「宮名=黄・三奇=青・八門ベタ塗り・順利バッジ・十二支配置」は既に実装済み。
> 以下は **そこからの差分**。既に正しい部分は触らない。

### 2-1. フォントを Noto ゴシックに統一（明朝をやめる）
- 現状の明朝体が古臭く見えるのが問題。
- **宮名（巽・離・坤・震・兌・艮・坎・乾）だけ `Noto Serif JP`（明朝）のまま**。色は黄 `#e6c34a`。
- それ以外すべて（天地盤干・八神・九星・八門・象意名・点数・五行・十二支）は **`Noto Sans JP`（ゴシック）**。
- 八門は `Noto Sans JP` の **900（Black）**。

### 2-2. 宮セルを正方形寄り・角丸大きめに（現代風）
- `aspect-ratio: 1 / 1.12`（要件①と同じ。縦長を解消）
- `border-radius: 16px`（角丸を大きく＝丸っこく現代的に。スマホ最小幅では 14px に縮小可）

### 2-3. ❹❺ 天盤干=八神 / 地盤干=九星 を同じ高さ・同じ行に揃える
- 現状は右の八神九星が上に出て、左の干が下にずれている。これを揃える。
- 構造: 干エリアと八神九星エリアを **2列×2行のグリッド**にする。
  ```
  [天盤干] [八神]   ← 同じ行・同じ高さ
  [地盤干] [九星]   ← 同じ行・同じ高さ
  ```
- 実装例（お手本HTML `.kanmeta`）:
  ```css
  .kanmeta{display:grid;grid-template-columns:1fr 1fr;align-items:center;row-gap:2px;}
  .kan{font-size:21px;font-weight:900;line-height:1.12;}   /* 干 */
  .meta{font-size:16px;font-weight:700;text-align:right;line-height:1.12;} /* 八神/九星 */
  ```
- 干（左）と八神九星（右）はそれぞれ左寄せ／右寄せ。上端を揃える。

### 2-4. 八門ベタ塗りの色を指定値ぴったりに
- 吉門＝青 **`#4472C4`** / 凶門＝赤 **`#FF0000`**、文字は白 `#fff` 抜き。
- 現状はくすんだ青・赤茶になっているので、この値に合わせる。
- 配置は **干・八神九星の下に中央寄せ**（案A）。`text-align:center`。
- サイズは控えめ（他要素と重ならない自然なサイズ。お手本では 20px）。
- 色分けロジック（どの門が青/赤か）は**現状維持**。色の値だけ差し替え。
- 適用は **盤の9宮セル内のみ**（象意パネル等は対象外）。

### 2-5. 点数を枠内で大きく
- 各宮ヘッダー右上の点数（5, -80 等）を大きく。お手本では `font-size:25px; font-weight:900;`。
- プラス＝緑 `#6cd06c` / マイナス＝赤 `#ff7a7a`（現状の色分けロジック維持）。

### 2-6. 順利は青枠のみ（文字は出さない）
- 順利宮は青枠 `border:3px solid #4472C4` だけにする。
- **現状の「◎順利」バッジ（文字表記）は削除**。青枠だけで順利と分かればよい（先生指示）。
- ※ ロジック（is_junri 判定）はそのまま。表示の「◎順利」テキストのみ消す。

### 2-7. 外周十二支：配置はそのまま・Noto化・サイズアップ
- **★ 十二支の配置（どの支がどこに来るか）は前回かなり時間をかけて作った資産。絶対に変更しない。**
- 変えるのは2点だけ:
  1. フォントを `Noto Sans JP`（ゴシック）に
  2. サイズを大きく（お手本では 20px。スマホ最小幅では 17px に縮小可）
- 方位（東西南北）の表示は不要（現状なければそのまま無し）。
- 十二支を丸で囲む案は **不採用**（やらない）。

---

## 3. 現状維持（変更しない・触らない）

- 外周十二支の**配置**（位置）
- 旬首の表記 = **`(庚)` のカッコ表記のまま**（本番では既にズレずに揃っている。緑背景化は不要・不採用）
- 三奇（乙・丙・丁）= 青文字。それ以外の干（甲戊己庚辛壬癸）= 白。※既に実装済み
- 宮名 = 黄色・明朝。五行（木火土金水）= 小さくグレー。※既に実装済み
- 八神九星の色分けロジック（吉=青文字 / 凶=赤文字）
- 点数・八門・象意の**色分けロジック全般**
- **吉凶判定エンジン・スコア計算ロジックは一切変更しない**（表示だけの改修）
- 中宮（戊の特殊配置等、既存仕様）

---

## 4. 配色リファレンス（お手本HTMLの :root より）

```
--bg:#0f0d08            画面背景
--board-border:#b8923a  盤外枠の金
--card:#15140f          宮セル背景
--card-empty:#100e09    中宮背景
--line:#4a4636          セル枠
--gong:#e6c34a          宮名（黄・明朝）
--elem:#8a8678          五行（小・グレー）
--txt:#e8e8ea           通常の干（白）
--sanki:#7fb0f0         三奇 乙丙丁（青）
--dim:#9aa0ab           旬首カッコ
--blue:#4472C4          八門 吉ベタ / 順利枠
--red:#FF0000           八門 凶ベタ
--green:#6cd06c         プラス点数
--blue-tx:#7fb0f0       八神九星 吉文字
--red-tx:#ff7a7a        八神九星 凶 / マイナス点数
--zod:#c2c8d2           十二支
```

---

## 5. 実装対象ファイル

- `src/components/PalaceCell.jsx` … 宮セルの中身（干・八神九星・八門・象意・点数・順利枠）
- `src/components/BoardGrid.jsx` … 9宮＋中宮の配置、外周十二支
- `src/styles.css` … レイアウトCSS
  - `.board-grid` / `.zodiac-label` / `.cell` / ヘッダー
  - 天地盤干・八神九星・八門・象意・中宮
  - `@media` クエリ（スマホ／タブレット幅）
- ※ 直近 e050a10「スマホ盤崩れ修正」の @media 調整と整合させること

---

## 6. 制約（守らないと差し戻し）

1. **元配置を崩さない**: 「左＝天地盤干 / 右＝八神九星（同じ高さで横並び）」という上半分の骨格は変えない。八門を中央に巨大化して全要素を組み替える案は不採用。
2. **吉凶判定ロジック・スコア計算は不変**（表示だけの改修）。
3. **PC幅対応は不要**（スマホ/タブレット専用）。ただし PC で開いても崩れず中央表示されること。
4. **既存テスト全 green を維持**（`npm test`）。
5. 順利・三奇・宮名色・八門色分けなどの**判定ロジックは触らず、表示・スタイルのみ**変更。

---

## 7. チェックリスト（実機確認）

- [ ] 9宮セルが**全て同一サイズ**（順利あり震・坎と、順利なし離・乾の高さがピクセル一致）
- [ ] 360 / 375 / 390 / 414 / 480px の各幅で崩れない・重ならない
- [ ] タブレット幅（768px 等）で盤が中央に最大幅で表示される
- [ ] PC幅で開いても崩れず中央表示（スマホ幅の盤が出るだけ）
- [ ] 明朝が宮名だけ。他は全部 Noto ゴシック
- [ ] 天盤干と八神、地盤干と九星が同じ高さで2段に揃う
- [ ] 八門が #4472C4 / #FF0000 のベタ塗り白抜き・干八神の下中央
- [ ] 点数が右上に大きく表示
- [ ] 順利宮は青枠のみ（「◎順利」テキストが消えている）
- [ ] 外周十二支の配置が現状と同じ（位置不変）・Notoゴシック・サイズアップ
- [ ] 旬首 `(庚)` がズレずに表示（離・兌）
- [ ] 三奇 乙丙丁が青、他の干が白
- [ ] `npm test` 全 green

---

## 8. 作業フロー

1. main 最新化: `git checkout main && git pull`
2. ブランチ作成: `git checkout -b phase-board-redesign`
3. お手本 `board_design_v4.html` をブラウザで開いて目標の見た目を把握
4. 上記 2〜3 を実装
5. `npm test` で全 green 確認
6. `npm run dev` で実機（スマホ実機 or DevTools のスマホ幅）で 7 のチェックリスト確認
7. commit / push → PR 作成
8. ぶりちゃんが実機確認 → OK なら先生に見せて最終確認 → マージ
