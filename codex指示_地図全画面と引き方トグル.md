# Codex実装指示：地図の全画面表示 ＋ 役割分け ＋ 方位線の引き方トグル

> 対象：段階3「地図連動」の続き。マージ済みの地図扇表示（DirectionMap）を拡張する。
> 参考にした先行ツール：「あちこち方位」(h200.com/houi)。本家は方位線を「平面/球面」「西偏角あり/なし」で切り替えられる。これをうちにも入れる。**ただし吉凶判定は本家にない我々の強み＝先生エンジンで色を出す**。
> 前提：地図扇表示は本番反映済み・mainは安定最新。テスト全green維持が必須。

---

## 0. 大原則（必読・継続）

- **既存の判定エンジンには一切手を入れない**。宮ごとの点数は今までどおりエンジンが出す。本指示で変えるのは「その点数を地図の扇に乗せるときの“角度の引き方”と“表示サイズ”」だけ。
- **AIは使わない**。**課金ゼロ・APIキー不要**を厳守。
- 作業は新しいブランチで（例：`phase-map-fullscreen`）。`git checkout main && git pull && git checkout -b phase-map-fullscreen`。
- 各ステップ後に `npm test`（全green維持）と `npm run build`（成功）を必ず確認。
- 実機確認は**Vercelプレビュー**で。
- **この指示書の範囲を超えて実装しない**（§6「やらないこと」厳守）。

---

## 1. やること（3つ）

### A. 地図の全画面表示
- 吉方位画面の地図（小枠）に **「拡大」ボタン**を付ける。押すと**地図だけが全画面**になり、グリグリ操作できる（「あちこち方位」のような専用ビュー）。
- 全画面には**「閉じる／戻る」ボタン**。押すと元の小枠レイアウトに戻る。
- 全画面でも扇・確定ライン・フェード・凡例は同じ仕様で表示。広い画面でズーム/パンしやすくする。
- **普段（小枠）の起動時レイアウトは変えない**＝吉方位タブを開いた最初は今までどおり（円盤・1位カードが見える）。全画面はボタンを押したときだけ。

### B. 役割分け（地図と円盤のダブり解消）
今、地図の扇と円盤が同じ点数を大きく出していて二重に見える。役割を分ける：
- **小枠の地図**＝「位置どり（方角の扇＋距離）」に専念。**点数ラベルは小さく/控えめに**（方位名は残す、点数は小さめ表示。完全に消すかは要調整＝定数で出し分けできるように）。
- **円盤**＝「点数の主役」。今のまま（くっきり見やすい）。
- **全画面の地図**では点数ラベルをしっかり出してOK（地図が主役になるので）。＝小枠と全画面で点数ラベルの大きさを変える。

### C. 方位線の引き方トグル（全画面時に表示）
本家「あちこち方位」と同等の切り替えを、**全画面ビューの中に**置く（マニア向け設定なので普段の小枠には出さない）。
- **① 平面 / 球面**：扇の方位角の引き方を切り替える。
  - 平面＝地図上で素直に「北東=45度」（現状の実装）。
  - 球面＝球面三角法で方位角を計算（遠距離ほど平面とズレる）。近距離（お散歩）はほぼ同じ、遠出（日盤・遠方）で効く。
- **② 西偏角（磁北のズレ）あり / なし**：
  - 真北基準（地図の北）＝なし（現状）。
  - 磁北基準＝あり。扇全体が西偏角ぶん回転する。日本は西に約5〜9度。
  - **西偏角は簡易近似でよい**（緯度経度から日本向けの近似式でざっくり算出）。**精密な地磁気モデル（WMM/国土地理院偏角データ、本家のD2000/D2010/D2015相当）は将来**。
- **初期値は現状維持＝「平面・西偏角なし」**（先生確認前の仮）。後でデフォルトを変えられるよう定数化。

---

## 2. 重要な切り分け（エンジンに触らない）

- 切り替えるのは**扇ポリゴンを描くときの方位角の計算だけ**（`destPoint`/`sectorPolygon` に渡す角度をどう作るか）。
- **どの宮が何点か＝先生エンジンの出力は一切変えない**。平面だろうと球面だろうと、北東の点数は北東の点数のまま。「北東をどの角度に描くか」だけが変わる。
- これにより先生ロジックの独自性・正確さは完全に保たれる。

---

## 3. 実装の指針

### 全画面
- `position: fixed` で全画面オーバーレイにする場合、**ボトムタブ（盤/吉方位/設定）と干渉しないこと**。全画面中はタブを隠す or 全画面をタブの上に重ねる。閉じたら元に戻す。
- 全画面の開閉は state で管理（重いライブラリ不要）。地図インスタンスは全画面/小枠でサイズが変わるので、切替時に Leaflet の `map.invalidateSize()` を呼んでタイル欠けを防ぐ。
- スマホ縦で全画面 → 地図が画面いっぱい、上部に「閉じる」、引き方トグルは上 or 下に小さく配置。

### 引き方の角度計算（定数化・テスト可能に）
- 方位角を出す関数を**切り替え可能**に：`bearingFor(dirIndex, {mode:'plane'|'sphere', declination:true|false})` のような形。
- 平面：`dirIndex * 45`（現状）。
- 球面：基準点から各方位の遠方点への球面方位角（近距離では45度に収束）。
- 西偏角：簡易近似で得た角度を全方位に加算（回転）。近似式は日本域でそれっぽい値が出ればよい。
- これらは**純粋関数として切り出し、単体テスト**を付ける。

### 点数ラベルの大小
- 小枠/全画面で点数ラベルのサイズ・表示有無を出し分け。**定数で管理**（後でかなこ/先生が調整しやすいように）。

---

## 4. 触ってよい / 触ってはいけない

### 触ってよい（新規 or 追加）
- 地図コンポーネント（DirectionMap）への：全画面state、拡大/閉じるボタン、引き方トグルUI。
- 方位角計算ユーティリティ（平面/球面/西偏角）＝新規・純粋関数。
- 点数ラベルのサイズ定数、引き方の初期値定数。
- 上記の単体テスト。

### 触ってはいけない
- `src/kimon/` 配下のエンジン群＝**呼ぶだけ・出力を変えない**。
- 宮スコア→方位ランキングのロジック＝**結果を受け取るだけ**。
- 円盤（CompassWheel）の点数表示ロジック＝**役割分けで主役にするが、点数の出し方は変えない**。
- 既存の盤UI（BoardGrid/PalaceCell）。

---

## 5. テスト
- `bearingFor` が平面モードで `dirIndex*45` を返すこと。
- 球面モードが近距離で平面とほぼ一致し、遠距離でズレること（向きの符号が妥当）。
- 西偏角ありで全方位が一定角度回転すること（日本域の近似値が妥当な範囲）。
- 全画面state切替で `invalidateSize` 相当が呼ばれる経路があること（描画崩れ防止）。
- 既存テストは**全green維持**。

---

## 6. やらないこと（範囲外）
- **吉凶判定の変更**は一切しない。引き方は表示角度のみ。
- **精密な地磁気モデル**（WMM/偏角データ/D2000系）は入れない。簡易近似まで。
- **24方位・12方位**への対応は将来（本家にはあるが今回は8方位のまま。枠だけ意識して定数化しておくのは可）。
- 道・経路描画、目的地ピン自動配置、奇門三盤ルートは将来。
- 距離（500m/10km）の最終確定はしない（仮のまま）。

---

## 7. 完成後に先生へ確認（チェックリスト追記分）
- 方位線の引き方：**平面/球面、西偏角あり/なし、どれをデフォルト**にするか。先生の流派はどれを使うか。
- 西偏角を採用する場合、簡易近似で許容か／精密版が要るか。
- 将来の24方位・12方位対応の要否（気学・奇門でよく使う特殊8方位を含む）。
- 小枠地図の点数ラベルは「小さく表示」「消す」どちらがよいか。

---

## 8. 進め方
1. 全画面state＋拡大/閉じるボタン → `invalidateSize` で描画確認。
2. 小枠/全画面で点数ラベルの大小を出し分け（役割分け）。
3. `bearingFor`（平面/球面/西偏角）を純粋関数で実装＋テスト。初期値=平面・偏角なし。
4. 全画面に引き方トグルUIを配置。切替で扇が再描画されること。
5. `npm test` / `npm run build` → Vercelプレビュー実機確認 → PR。**マージはかなこが手動**。

*本家「あちこち方位」が線を引くだけなのに対し、我々は先生エンジンで吉凶を色で出すのが差別化。引き方トグルは初期値=現状（平面・偏角なし）で枠だけ作り、デフォルトの確定は完成後に先生へ。エンジンには触らない。*
