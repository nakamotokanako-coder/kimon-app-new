# Codex実装指示：最強ランキング（日盤・期間スキャン）＝検索機能ビューA

> 対象：設計図「設計図_逆引き方位検索.md」§10-2 ビューA（今月の最強ランキング）。
> レイアウトの目標：モック **`saikyo_ranking_mock.html`**（スマホ縦・期間プリセット・5段階同点処理バッジ・月別ベスト）。
> 前提：日盤モード本実装（PR #19）はマージ済み。`buildBoard({boardType:'日'})` で日盤が組める状態。テスト340件green維持が必須。

---

## 0. 大原則（必読）

- **既存の判定エンジンには一切手を入れない**。盤組立・点数計算は既存関数を呼ぶだけ。先生ロジックの独自性・正確さを守るため。
- **点数（合計スコア）は scoreEngine の出力そのまま**。配点を変える・小数で刻むなどは**禁止**。同点は§3の二次ソートでほどく。
- **AIは使わない**。判定は全て既存の自前エンジン。
- 作業は**新しいブランチ**で。必ず最新mainから切る：`git checkout main && git pull && git checkout -b phase-ranking`
  - ※前回ブランチ混線の教訓。push前に `git log --oneline origin/main..phase-ranking` で差分が想定どおりか確認すること。
- 各ステップ後に `npm test`（340件green維持）と `npm run build`（成功）を必ず確認。
- 実機確認は**Vercelプレビュー**で行う。
- **この指示書の範囲を超えて実装しない**（§6の「やらないこと」を厳守）。

---

## 1. 再利用する既存エンジン（呼ぶだけ・改変禁止）

| 役割 | 場所 |
|---|---|
| 日盤の組立（日干支=koyomi.eto_day / 局数=lookupDayKyokusu(date)） | `buildBoard({date, boardType:'日'})`（src/kimon/buildBoard.js） |
| 局数の引き（範囲外日付は明示エラーを投げる） | `lookupDayKyokusu(date)`（day_kyokusu.csv 参照） |
| 点数判定（宮ごとのscore・usable・breakdown・detected_kakkyoku） | `scoreBoard(board)`（src/kimon/scoreEngine.js） |
| 格局判定 | `src/kimon/kakkyoku.js` |
| 十干剋応判定 | `src/kimon/jukkanKokuou.js` |
| 盤レベル・門迫・空亡・五不遇時 | `src/kimon/banLevel.js` |

- まず**これらの現状を読んで報告**してから実装に入ること。特に確認すべき点：
  - `scoreBoard` が各宮について返す構造（`score` / `breakdown`〔tenban_kan・hachimon・kyusei・hasshin・jukkan_kokuou・kakkyoku・ban_level_minus・空亡〕/ `detected_kakkyoku`〔name・kichi_kyo〕）。
  - 八門が breakdown のどこから取れるか（同点ソート③で「休/生/開/景」を判別するのに必要）。各宮の八門名そのもの（休門・生門…）を board から読めるか。
  - 凶要素（凶門・庚・凶神・空亡・盤レベル減点）が breakdown 上でどう表現されるか（同点ソート②の判定に必要）。

---

## 2. 機能の中身（日盤・期間スキャン）

### 2-1. やること
基準点から見て、**指定期間内の各日について日盤を組み、全8方位を点数化 → §3のルールで並べてランキング表示**する。

- 期間プリセット：**今週 / 今月 / 3ヶ月 / 半年 / 1年**（最長1年）。
- 各日：`buildBoard({date, boardType:'日'})` → `scoreBoard()` → 中宮を除く8宮の score を取得。
- **各日の代表＝その日の最高スコアの方位1つ**を採用してランキングに載せる（1日1行）。
  - ※「同じ日の複数方位」を全部出すと行が膨らむため、まず最高1方位/日。これで先生のツアー用途（有給1日の遠出先）に合う。
- 方角は**定位**（坎=北・離=南・震=東・兌=西・艮=北東・巽=南東・坤=南西・乾=北西）。8方位45度均等（確定済み）。

### 2-2. 計算量（軽い・心配不要）
- 1年 ≒ 365日 × 8方位 = 約2,920回の点数化。日盤は1日1枚で軽い。同期処理で一瞬。
- ただしUIブロックを避けるため、長期間（半年・1年）は**計算中インジケータ**を出すか、`requestIdleCallback`等で分割してよい（任意）。

### 2-3. 表示件数（モック準拠）
- **総合トップ10件**を基本表示。
- **「もっと見る」**で 20件 → 30件 に展開（それ以上は出さない）。
- **長期間（3ヶ月以上）は加えて「月別ベスト」**を併記：各月の最強1日を**月順（時系列）**に並べる。「6月→7月→…」と俯瞰でき、有給計画に使える。
  - 月別ベストの抽出も§3の同じソートで各月の1位を取る。

### 2-4. 自然時補正は不要
- 日盤は1日単位。**自然時補正なし**（補正は時盤お散歩モードのみ）。画面に「日盤は自然時補正なし」を明示してよい（既存日盤タブと揃える）。

---

## 3. ★同点処理＝5段階ソート（この機能の肝・確定仕様）

点数が10刻みのため同点が多発する。**合計スコアを一次キーにしつつ、同点を以下の順でほどく**。
②〜⑤の判定に必要な値は全て scoreBoard の出力／board から読める（新規計算なし）。

```
sortRanking(candidates):
  ① 合計スコア      … score 降順                         ※scoreEngineそのまま・不変
  ② 凶要素の有無    … 凶門・庚・凶神・空亡・盤レベル減点を含む方位は後ろへ（凶なしを上）
  ③ 八門の格        … 休門 > 生門 > 開門 > 景門 の順（MON_RANK = {休:0,生:1,開:2,景:3}）
  ④ 吉格局の質      … detected_kakkyoku に吉格(kichi)がある方位を上へ。
                       強い格（青龍返首・飛鳥跌穴 …）を優先（KAKU_RANK 定数で順位づけ・後から調整可に）
  ⑤ 日付の早さ      … ここまで同じなら date 昇順（直近で行ける日を優先）
```

- **②の「凶要素」の定義**：breakdown で減点が立っている要素（凶門=八門が傷杜死驚で-40 / 庚=-20 / 凶神=螣蛇勾陳朱雀で-20 / 空亡=-10 / 盤レベル減点）。1つでもあれば「凶要素あり」とする。判定は scoreBoard の breakdown を読むだけ。
- **③の八門**：その宮の八門名から休/生/開/景を取り出してランク化。四凶門は②で既に後ろなので③の対象外でよい。
- **④の格局ランク**：`KAKU_RANK` を定数ファイルに置き、後から先生回答で順序を差し替えられるようにする（コード直書き禁止＝重み付けは外部定数）。
- 実装はモック `saikyo_ranking_mock.html` の `sortRanking()` がそのまま参照実装。**この比較順を厳守**。

### バッジ表示（順位の根拠を可視化）
各ランキング行に、なぜその順位かを示すバッジを出す（モック準拠）：
- 「凶なし」/「⚠ 凶要素あり」（②）
- 八門名（休門/生門/開門/景門）（③）
- 吉格局名（あれば）（④）

---

## 4. 画面構成（モック `saikyo_ranking_mock.html` のとおり）

上から：ヘッダー（最強ランキング）→ 基準点＋「日盤/遠出 50km〜」バッジ → 期間プリセット → 総合トップ10（カード）→ もっと見る → 月別ベスト（長期のみ）→ 同点ルール凡例（折りたたみ）。

- カード：順位 / 方位 / 日付・曜日 / バッジ群 / 点数。1位は金枠強調。同点グループの区切りに「+◯点グループ」のラベル線（モック準拠）。
- **タップで日盤地図へ**：その日付の日盤地図（反転フェード・50km金リング・既存 DirectionMap）へ遷移する導線（既存日盤ビューを date 指定で開く）。
- 既存の盤デザイン（黒地・金枠・吉青/凶赤・Noto Sans/Serif JP）と統一。
- 「吉のみ表示」既定ON（凶方位は隠す）。プロ向け「凶も見る」トグルは残す（凶要素ありも表示対象に含める）。

### 配置（メニュー）
- 既存の「吉方位」タブ内の画面内タブに **「最強ランキング」** を追加（時盤お散歩／日盤遠出 と並ぶ位置）。設計図§4-3のメニュー構成に沿う。

---

## 5. 触ってよいファイル / 触ってはいけないファイル

### 触ってよい（新規 or 追加）
- 新規：最強ランキング画面コンポーネント、期間スキャンのユーティリティ（buildBoard/scoreBoardを日付ループで回す）、`sortRanking` ユーティリティ、`KAKU_RANK` 等の定数ファイル。
- 追加：上記に対応するテスト。
- 既存の「吉方位」タブに画面内タブを1つ追加（ルーティングの最小追記）。

### 触ってはいけない
- `src/kimon/` 配下のエンジン群（buildBoard, scoreEngine, kakkyoku, jukkanKokuou, banLevel, junri, timeKanshi）＝**呼ぶだけ**。
- 各種データCSV / `shoui_dict.json` ＝**読むだけ**。
- 既存の盤UI・日盤地図（DirectionMap / mapFan）の中身＝**遷移先として呼ぶだけ**、表示ロジックは変えない。

---

## 6. テスト
- **sortRanking の単体テスト（最重要）**：同点データを食わせて①〜⑤の順に並ぶこと。具体的に：
  - 同スコアで「凶あり vs 凶なし」→ 凶なしが上（②）。
  - 同スコア・凶なしで「景門 vs 休門」→ 休門が上（③）。
  - 同スコア・同八門で「吉格あり vs なし」→ 吉格ありが上（④）。
  - 全部同じなら日付が早い方が上（⑤）。
- 期間スキャンが各日1方位（最高スコア）を返すこと。
- 月別ベストが月順（時系列）で各月1件になること。
- 範囲外日付（day_kyokusu.csv の範囲外）で `lookupDayKyokusu` のエラーを**穏当に処理**（クラッシュせず「この期間は範囲外」表示）。
- 既存340件は**全green維持**（エンジンに触らないので壊れないはず）。

---

## 7. やらないこと（範囲外）
- scoreEngineの配点変更（厳禁）。
- 目的別フィルタの中身（UIのプレースホルダのみ可。対応定義JSONの接続は別段階）。
- ビューB（奇門三盤ルート）・ビューC（方角固定の日探し）。期間スキャン基盤は流用できるが今回は作らない。
- 1日複数方位の表示（まず最高1方位/日）。
- 出発時刻の時盤併用（先生確認事項）。

---

## 8. 先生確認待ち（実装はこの前提で先行・完成後に確認）
以下はPDF＋LINEで先生に送付済み。**回答を待たず本指示書の仕様で実装しきる**。流派で違えば後から直す（NGなら直す方式）。
- 同点ソートの順序（②凶→③八門→④格局）。八門の序列（休>生>開>景）。吉格局の格の高さ（KAKU_RANK）。
- 表示件数（トップ10＋長期は月別ベスト併記）。遠出の目安（50km/3時間）。

---

## 9. 進め方
1. まず§1の既存エンジン（特に scoreBoard の宮ごと出力構造・八門の取り出し方・凶要素の表現）を読んで**現状報告**（実装前）。ここで②③の判定にどのフィールドを使うか確定させる。
2. 期間スキャン＋sortRanking のユーティリティを実装 → **単体テスト（§6）を先に通す**。
3. 画面（モック準拠）を実装 → test/build → Vercelプレビューで実機確認。
4. 確認ポイント（実機）：①期間切替でランキングが変わる ②同点グループ内でバッジ順（凶なし>休>生>開>景・吉格）に並ぶ ③長期で月別ベストが月順に出る ④カードタップで該当日の日盤地図へ飛ぶ ⑤範囲外日付でcrashしない。
5. PR作成。**マージはユーザー（かなこ）が手動**。

*同点ソート（§3）はモック `saikyo_ranking_mock.html` の `sortRanking()` が参照実装。比較順を厳守すること。*
