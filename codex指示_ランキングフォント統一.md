# Codex実装指示：最強ランキングのフォント統一（明朝→ゴシック）

> 対象：phase-ranking でマージ済みの最強ランキング画面。
> 問題：最強ランキング画面だけ Noto Serif JP（明朝）になっており、盤など他画面（Noto Sans JP＝ゴシック）と不統一で浮いている。
> 修正：**盤デザインの確定ルールに合わせ、ゴシックに統一する**。

---

## 0. 大原則
- phase-ranking ブランチ（または main から新ブランチ）で作業。**push前に差分確認**。
- 触るのは最強ランキング画面のスタイルのみ（`src/reverseDirection/` 配下＋必要なら `src/styles.css` の該当クラス）。
- エンジン（src/kimon/）・他画面は触らない。
- `npm test` 350green維持・`npm run build` 成功を確認。

---

## 1. 修正内容：明朝指定をゴシックに置換

最強ランキング画面で `Noto Serif JP` / `serif` を指定している箇所を**すべて `Noto Sans JP`（ゴシック）に変更**する。

### 盤デザインの確定ルール（hikitsugi_board_design.md より・遵守事項）
> 宮名（離・巽・坤など）だけ現状フォント（明朝系=Serif）のまま。**それ以外（天地盤干・八神・九星・八門・象意名・点数）は Noto Sans（ゴシック）に統一**。

→ アプリ全体の基調は**ゴシック**。明朝は「盤の宮名」だけの例外。
→ **最強ランキング画面には宮名表示はない**ので、**例外なく全部ゴシックでよい**。

### 具体的に直す要素（モック由来で明朝になっている想定箇所）
- タイトル「最強ランキング」
- セクション見出し（総合トップ / 月別ベスト など）
- 順位の数字（1位・2位…）
- 方位名（東・北西 など）
- 点数の数字（+90点 など）
- 日付・バッジ・ボタン・その他テキスト

上記を含め、この画面内の `font-family` から `Noto Serif JP` と `serif` を除き、`'Noto Sans JP', sans-serif` に統一する。

---

## 2. やらないこと
- 盤画面（BoardGrid / PalaceCell）の宮名フォント＝**明朝のまま維持**（触らない）。
- 色・レイアウト・点数の値・並び順は変更しない。**フォントファミリーだけ**の修正。
- 他画面（時盤お散歩・日盤遠出・設定）のフォントは現状維持。

---

## 3. 確認
- 最強ランキング画面の文字がすべてゴシックになり、盤・他タブと統一感があること。
- レイアウト崩れ（フォント変更で行高・幅が変わる）がないこと。特に§前回直した点数の右端揃えが維持されていること。
- `npm test` 350green / `npm run build` 成功。

---

## 4. 進め方
1. 最強ランキング画面の `Serif`/`serif` 指定箇所を洗い出して報告。
2. ゴシックに置換 → test/build → Vercelプレビューで実機確認（盤タブと見比べて統一されているか）。
3. push前 `git log --oneline origin/main..ブランチ` で差分確認 → push → PR → **マージはかなこ手動**。

*背景：参照モック saikyo_ranking_mock.html が見出し・数字に Noto Serif JP を使っていたため、ランキング画面全体が明朝になった。アプリ基調はゴシックなので、この画面は明朝を使わない。*
