# Codex実装指示：地図連動（段階3）― 吉方位の扇表示

> 対象：設計図「設計図_逆引き方位検索.md」§4-1・§8の **段階3「地図連動」の本実装**。
> 前段の指示書「codex指示_メニュー化と時盤逆引き.md」では地図を「最小限」としていたが、見せ方の仕様がモックで確定したので**この指示書で正式版に差し替える**。
> 見た目の確定モック：**`map_fan_proto.html`**（このファイルと同じ場所に置く）。実機（iPhone）で確認済み・OK。
> 前提：段階0.5メニュー化＋段階1時盤逆引き（8方位ランキング）は実装済み・mainは安定。テスト全green維持が必須。

---

## 0. 大原則（必読・前指示書から継続）

- **既存の判定エンジンには一切手を入れない**。盤組立・点数計算は既存関数を呼ぶだけ。
- **AIは使わない**。判定は全て既存の自前エンジン。
- **課金ゼロ・APIキー不要**を厳守。地図=Leaflet、タイル=後述の無料タイル。Google Mapsは使わない。
- 作業は新しいブランチで（例：`phase-map-fan`）。`git checkout main && git pull && git checkout -b phase-map-fan`。
- 各ステップ後に `npm test`（全green維持）と `npm run build`（成功）を必ず確認。
- 実機確認は**Vercelプレビュー**で（位置情報APIはHTTPS必須。LAN IP:5173では位置情報が取れない）。
- **この指示書の範囲を超えて実装しない**（§7「やらないこと」を厳守）。

---

## 1. この機能は何か（1行）

**吉方位タブの中で、家（基準点）を中心に各方位の吉凶を地図上の「扇」で重ねて見せる。** 既に段階1で出している8方位ランキングのスコアを、そのまま地図の扇に色で反映するだけ。新しい判定はしない。

---

## 2. 再利用するもの（呼ぶだけ・改変禁止）

- 段階1で実装済みの **8方位×スコア** のデータ（buildBoard→scoreEngineで宮ごとに出している点数を、方位名・点数・吉凶レベルに整形したもの）。地図はこの配列を受け取って描くだけ。
- 基準点の緯度経度（段階1の `navigator.geolocation` / 国土地理院ジオコーディングで取得済みのもの）。
- 自然時補正・時辰判定・盤組立は段階1のものをそのまま使う。**地図は計算をしない。**

> もし段階1が「ランキングの配列」を地図に渡せる形でexportしていない場合は、**その配列を切り出す薄いセレクタ/propのみ追加**してよい（エンジン本体は触らない）。

---

## 3. 確定仕様：扇の見せ方（モック `map_fan_proto.html` のとおり）

### 3-1. 全体構成
- 地図は Leaflet。基準点を中心に初期表示（**初期ズーム 13**）。中心に基準点ピン（金色 `#e6c34a`）。
- 8方位を **45度均等の扇**で描く（北=0度を真上、時計回り。北東45・東90・南東135・南180・南西225・西270・北西315）。方角は**定位**（坎=北…既に確定）。
- **全方位を常に表示**する（吉だけに絞らない）。吉/凶/中立を色で見分ける（旧「吉のみ/凶も見る」トグルは地図では使わない）。

### 3-2. 二段構えの扇（最重要・この機能の肝）
各方位は **2レイヤー**で描く：

**① 確定ゾーン（0〜500m）＝濃い・はっきり**
「ここまではしっかり取れる」範囲。中心から半径500mの扇。
- 吉（青系）・凶（赤系）は塗りも輪郭もはっきり。中立（グレー）は控えめ。
- 1位（ベスト方位）だけ特別強調：金色の太枠＋濃いめの塗り。

**② フェード帯（500m〜遠方）＝薄く、遠いほど消える**
「その方位は遠出でも同じ吉凶が続く」ことを示す。500mの外側を**同心の弧帯（リング状の扇）4段**で重ね、外ほど薄くする。
- 吉・凶のみ描く。**中立はフェードを出さない**（うるさくなるため）。
- 凶のフェードは吉よりやや控えめ（係数0.75）。
- 各帯に薄い同色の輪郭線（境界がうっすら見える）。

### 3-3. 確定値（モックから。定数化して後で調整可能に）
```
RADIUS_M     = 500          // 確定ライン（時盤の仮置き。先生確認後にJSON/定数差し替えで変更可）
SECTOR_DEG   = 45           // 8方位均等
初期ズーム    = 13

色（既存テーマと統一）：
  大吉 kichi-strong = #185FA5（濃い青）
  中吉 kichi-mid    = #378ADD
  小吉 kichi-weak   = #85B7EB（淡い青）
  中立 neutral      = #6f6c5e（グレー）
  凶   kyo          = #c2554f（赤）
  ベスト枠/中心ピン = #e6c34a（金）

確定ゾーン①（0〜500m）の塗り：
  ベスト:   weight 3,   opacity 0.95, fillOpacity 0.45（枠は金 #e6c34a）
  吉:       weight 1.5, opacity 0.7,  fillOpacity 0.32
  凶:       weight 1.5, opacity 0.7,  fillOpacity 0.30
  中立:     weight 1,   opacity 0.4,  fillOpacity 0.14, 破線(dashArray '4 3')

フェード帯②（吉・凶のみ。凶は各opに係数 base=0.75）：
  500–1000m :  fillOpacity 0.30*base
  1000–2500m:  fillOpacity 0.20*base
  2500–5000m:  fillOpacity 0.12*base
  5000–10000m: fillOpacity 0.07*base
  各帯の輪郭線: weight 0.7, opacity 0.4*base, 同色

500m確定ラインの円：金 #e6c34a, weight 1.5, opacity 0.7, 塗りなし, 破線 '3 5'
方位ラベル：白文字＋黒縁取り。方位名＋点数。点数の色は吉=薄青/凶=薄赤/中立=グレー。
  ラベル位置＝中心から RADIUS_M*0.62 の地点（確定ゾーン内）。
```

### 3-4. 弧帯（リング状の扇）の作り方
モックの `sectorPolygon(center, fromDeg, toDeg, outerM, innerM)` を踏襲：
- `innerM=0` → 中心からの普通の扇（確定ゾーン用）。
- `innerM>0` → 外周（from→to）を outerM で描き、内周（to→from の逆順）を innerM で描いてリングを閉じる（フェード帯用）。
- 方位点の算出は球面の `destPoint`（中心から方位角・距離mの緯度経度）。近距離なので簡易式で十分（モックの実装をそのまま移植）。

### 3-5. タイル（地図の地色）
- 派手なタイルだと薄い扇が埋もれるため、**淡色タイルを使う**。モックは CartoDB Positron（`https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png`, subdomains:'abcd', キー不要・無料）を採用してOKと確認済み。
- 代替候補：地理院タイル（`https://cyberjapandata.gsi.go.jp/xyz/pale/{z}/{x}/{y}.png`、淡色・国産・キー不要）。**最終的にどのタイルを使うかは先生確認の対象**（§8）。まずは CartoDB Positron で実装。
- ダークテーマ運用時のタイル選択は後回しでよい（まず淡色で見やすさ優先）。

---

## 4. データの渡し方（地図⇔ランキングの連動）

- 地図に出すのは **選択中の時辰（時盤）／日（日盤）の吉方位だけ**。時刻・日付・基準点・目的フィルタが変わったら、**ランキングの再計算結果をそのまま地図にも反映**（同じ配列を共有）。
- ベスト方位（ランキング1位）＝地図の金枠の扇＝1位カード、が常に一致すること。
- 地図はあくまで表示。並べ替え・点数化はしない。

---

## 5. 触ってよい / 触ってはいけない

### 触ってよい（新規 or 追加）
- 新規：地図コンポーネント（例 `DirectionMap`）、弧帯ポリゴン生成ユーティリティ（sectorPolygon / destPoint）、扇の色・不透明度の定数ファイル。
- 追加：上記の単体テスト、吉方位画面に地図を差し込むための最小の配線。
- 追加可：段階1のランキング配列を地図へ渡すためのセレクタ/prop（エンジンには触らない範囲で）。

### 触ってはいけない
- `src/kimon/` 配下のエンジン群（buildBoard, scoreEngine, kakkyoku, jukkanKokuou, banLevel, junri, timeKanshi）＝**呼ぶだけ**。
- 段階1の判定・ランキングのロジック＝**結果を受け取るだけ**、点数化の中身は変えない。
- データ（shoui_dict.json / chito_v2.csv / koyomi_v3.csv 等）＝**読むだけ**。
- 既存の盤UI（BoardGrid / PalaceCell）＝触らない。

---

## 6. テスト
- `sectorPolygon` が innerM=0 で中心起点の扇、innerM>0 でリングになること（頂点数・先頭/末尾点）。
- `destPoint` が方位角0/90/180/270で期待方向に座標を返すこと（北で緯度増、東で経度増 等）。
- 8方位の扇が漏れなく描かれること（吉/凶は確定ゾーン＋フェード4段＝5レイヤー、中立は確定ゾーンのみ）。
- ベスト方位に金枠スタイルが当たること。
- 既存テストは**全green維持**（エンジン非改変なので壊れない想定）。

---

## 7. やらないこと（範囲外）
- **道・経路の描画、目的地ピンの自動配置**はしない。アプリは「方角の扇＋距離の範囲」を示すまで。どの道・どの店へ行くかは使う人が地図を見て選ぶ（川・線路で行けない場合があるため、ピンポイント指定はしない）。
- 奇門三盤ルート（3スポット巡回の点線描画）は**将来機能**。今回作らない（設計図§10）。
- 期間スキャン・最強ランキング等の検索機能群は将来（設計図§10-2）。
- 距離の最終確定はしない（500mは仮置き。先生確認後に定数差し替え）。
- ダークテーマ専用タイルの作り込みは後回し。

---

## 8. 完成後に先生へ確認（チェックリスト追記分）
- 距離の確定：時盤の「500m以上＋5分滞在」を確定ラインの円に採用してよいか。日盤（遠出）の確定ライン半径をいくつにするか。
- フェードをどこまで伸ばすか（今は10km）。遠出（日盤）用にもっと伸ばすか。
- 地図タイル：淡色（CartoDB Positron / 地理院タイル pale）でよいか。和テーマとの相性。
- 中立（0点）方位を地図に出すか／グレーで控えめのままでよいか。

---

## 9. 進め方
1. 段階1のランキング配列を地図に渡せるか確認（必要なら薄いセレクタのみ追加）。
2. `DirectionMap` 新規作成：Leaflet初期化＋淡色タイル＋基準点ピン＋500m円。
3. 二段構えの扇（確定ゾーン＋フェード4段）を §3 の確定値で実装。モック `map_fan_proto.html` と見た目を突き合わせる。
4. 吉方位画面に差し込み、時刻/日付/基準点/目的フィルタ変更で再描画されることを確認。
5. `npm test` / `npm run build` → Vercelプレビューで実機（iPhone）確認 → PR。**マージはユーザー（かなこ）が手動**。

*見た目の正解は `map_fan_proto.html`。数値・色・段数はそこから取った確定値。先生確認事項（§8）は完成後にまとめて依頼し、距離やタイルは定数/JSON差し替えで後から直す（NGなら外す方式）。*
