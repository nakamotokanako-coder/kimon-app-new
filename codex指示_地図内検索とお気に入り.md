# Codex実装指示：地図内検索（施設・住所）＋ お気に入り（場所）

> 対象：吉方位タブの地図（扇表示）に「探す」機能を足す。設計図「設計図_逆引き方位検索.md」§2（場所検索＝国土地理院ジオコーディング）の流れに沿う。
> 見た目・動きの確定モック：**`map_search_proto.html`**（このファイルと同じ場所に置く）。実機（iPhone）で確認済み・OK。
> 前提：地図連動（扇表示）は実装済み・mainは安定。テスト全green維持が必須。
> 既存の扇地図コンポーネント（`DirectionMap` 等）に**追記する形**。地図そのものは作り直さない。

---

## 0. 大原則（必読・前指示書から継続）

- **既存の判定エンジンには一切手を入れない**。盤組立・点数計算・方位スコアは既存関数の結果を**受け取るだけ**。
- **AIは使わない**。
- **課金ゼロ・APIキー不要**を厳守。地図=Leaflet、施設検索=Overpass API、住所検索=国土地理院ジオコーディング（設計図§2で確定済み）。**Google Mapsは使わない**。
- 作業は新しいブランチで（例：`phase-map-search`）。`git checkout main && git pull && git checkout -b phase-map-search`。
- 各ステップ後に `npm test`（全green維持）と `npm run build`（成功）を必ず確認。
- 実機確認は**Vercelプレビュー**で（位置情報APIはHTTPS必須）。
- **この指示書の範囲を超えて実装しない**（§7「やらないこと」を厳守）。
- ブランチは push 前に `git log --oneline origin/main..ブランチ` で差分確認。

---

## 1. この機能は何か（1行）

**地図の上で「施設（コンビニ・駅…）」や「住所・地名」を探せるようにし、出てきた場所が家から見て何方位・吉か凶かをひと目で分かるようにする。さらに、よく行く場所をお気に入り登録できる。**
アプリは「探す目」を足すだけ。**どこへ行けとは勧めない**（思想：§7「目的地ピンの自動配置はしない／使う人が地図を見て選ぶ」を維持）。判定は中心からの方位角で機械的に行い、**既存の扇スコア配列を参照するだけ**。

---

## 2. 再利用するもの（呼ぶだけ・改変禁止）

- 扇表示が既に持っている **8方位×スコア×吉凶レベル** の配列（`DIRS` 相当）。施設・住所ピンの色と「何方位・点数」表示はこれを引くだけ。
- 基準点 `HOME`（緯度経度）＝GPS／場所検索で取得済みのもの。
- `destPoint` / 距離計算など扇地図のユーティリティ。**新規に方位角・距離の関数が要る場合のみ薄く追加**（モック参照：`bearing` / `distM` / `dirForPoint`）。

> モックでは方位判定を「中心からの方位角を45度刻みで8方位に丸める」で実装（`dirForPoint`）。**扇の方位割り当てと完全一致させること**（北=0度中心・時計回り・定位）。扇とピン色がズレたら破綻するので、可能なら扇側と同じ割り当て関数を共有する。

---

## 3. 確定仕様（モック `map_search_proto.html` のとおり）

### 3-1. 検索バー（1本の自由入力）
- 地図の上に検索窓＋「探す」ボタン。Enter／ボタンで実行。**1文字ごとの自動検索はしない**（API負荷・マナー対策）。
- よく使う種別はチップ（タップ）：`コンビニ / 駅 / カフェ / スーパー / 公園 / 神社`。タップで即検索。
- プレースホルダ：「施設も住所も（コンビニ / 駅 / 川口市○○ …）」。

### 3-2. 入口の振り分け（種別 or 住所）
打った言葉を判定して2系統に振り分ける（モック `runSearch`）：
- **種別語**（KEYMAPに当たる）→ §3-3 施設検索（今見えている範囲）
- **それ以外**（住所・地名・固有施設名）→ §3-4 住所ジャンプ
- 振り分けはモックの `KEYMAP`（語→OSMタグ）を踏襲。マッピング表は定数化し後から追加可能に。

### 3-3. 施設検索（種別・Overpass API）
- **検索範囲＝今見えている地図の bbox**（`map.getBounds()`）。固定しない。地図をスワイプして別の場所を見てから探せる。
- Overpass で node＋way を取得（`out center`）。最大60件程度。
- 各ヒットに対し `dirForPoint` で方位・吉凶を判定し、**その方位の色のピン**を立てる。ピン中の記号：吉=「+」／凶=「−」／中立=「・」。
- ピンのポップアップ：`名称・方位・吉凶レベル（点数）・家から約◯◯`。
- 地図の下に**一覧**：吉方位＆近い順で上位8件（モック `renderList`）。
- 0件時：「この範囲では見つからない／地図を動かす・別の言葉で／※OSM登録分のみ」と案内（モック文言）。

### 3-4. 住所ジャンプ（住所・地名・国土地理院ジオコーディング）
- **設計図§2の確定どおり国土地理院ジオコーディングAPIを使う**（無料・キー不要・CORS可）。
  - 例：`https://msearch.gsi.go.jp/address-search/AddressSearch?q={住所}`（緯度経度を返す）。先頭ヒットを採用。
  - ※モックは検証の都合でNominatimを使用。**本番は国土地理院に差し替える**こと（設計図準拠）。
- ヒットしたら：地図をその地点へ寄せ、ピンを立て、**家から見て何方位・吉凶・点数・距離**を出す（モック `dropPlace`）。`HOME` と目的地が両方入るよう `fitBounds`。
- **見つからないときの保険**：今見えている範囲の「名前部分一致」施設検索に自動フォールバック（モック `fallbackNameSearch`）。それでも0なら「地名は市区町村＋町名で、施設は地図を動かして」と案内。

### 3-5. 地続き仕様（重要）
- 住所で飛んだあと、続けて種別を打つと**飛んだ先の範囲**で施設検索が走る。「気になる地名へ飛ぶ→その辺の店を探す」が同じ窓で繋がる。
- 種別検索の結果ピンと、住所ジャンプのピンは**排他**（新しい検索で前のを消す。モック `clearPois` / `clearPlace`）。お気に入りのピンとは別管理。

### 3-6. お気に入り（場所）
- 施設・住所どちらのピンのポップアップにも「★ お気に入りに追加」。
- 追加した場所は地図下に**お気に入りリスト**（モック `renderFavs`）。各行：色ドット・名称・家から距離・**今日の盤での方位/吉凶/点数**・削除（×）。
- リストの行タップ → その場所へ飛んでピン表示（`showFav`→`dropPlace`）。
- **肝**：お気に入りは座標で保持。日・時刻・基準点が変わるたびに方位/吉凶を**再計算して表示**（固定の場所でも「今日は西で凶／明日は吉」が見える）。日付・時刻・GPS変更時に `renderFavs` を呼ぶ。
- 既に登録済みの場所はボタンを「★ 登録済み」（無効）に。重複防止キーは座標5桁（モック `favKey`）。

### 3-7. 保存先（本番仕様）
- **本番は `localStorage`（端末保存）で永続化**。キー例：`kimon_favs`。`saveFavs`/起動時ロードを実装（モックはメモリのみ＝環境制約のためコメント箇所に本実装を入れる）。
- 保存するのは最小限：`{name, lat, lon}` の配列。方位・吉凶は保存しない（毎回再計算する値なので）。
- ※将来アカウント＋クラウド同期に載せ替える余地を残す（今はやらない。§7）。

---

## 4. 色・見た目（既存テーマと統一・モックの確定値）

```
大吉 #185FA5 / 中吉 #378ADD / 小吉 #85B7EB / 中立 #6f6c5e / 凶 #c2554f / 金 #e6c34a
ピン：水滴形（border-radius:50% 50% 50% 0 を-45度回転）、白縁2px、方位色で塗る。
  住所ジャンプのピンは大きめ（26px）、施設ピンは小さめ（20px）。
お気に入り行・一覧行・状態テキストの配色はモックのCSSをそのまま踏襲。
フォントは基調ゴシック（Noto Sans JP）。明朝は使わない（前回フォント統一の教訓）。
```

---

## 5. 外部APIのマナー（必須・守らないと将来ブロックされる）

- **国土地理院ジオコーディング**：問い合わせは検索実行時のみ（1文字ごと禁止）。
- **Overpass API**：`[timeout:20]`、`out center` で件数を絞る（上限60程度）。連打抑止のためボタン/Enter実行に限定。公共インスタンス（overpass-api.de）はレート制限があるので、短時間の連続実行を避けるデバウンス（例：実行中は再実行をブロック）を入れる。
- 失敗時はユーザーに分かる文言を出す（モックの `setState` のcatch文言）。アプリは落とさない。

---

## 6. 触ってよい / 触ってはいけない

### 触ってよい（新規 or 追加）
- 新規：検索バーUI、施設検索（Overpass）/住所ジャンプ（国土地理院）の呼び出し、お気に入りストア（localStorage）、お気に入り/一覧の表示コンポーネント、方位角・距離ユーティリティ（扇と共有できればなお良い）。
- 追加：上記の単体テスト、扇地図への最小の配線（検索結果ピン・お気に入りピンのレイヤー追加）。

### 触ってはいけない
- `src/kimon/` 配下のエンジン群（buildBoard, scoreEngine, kakkyoku, jukkanKokuou, banLevel, junri, timeKanshi）＝**呼ぶだけ**。
- 扇の描画ロジック・スコア配列の中身＝**結果を受け取るだけ**。色や方位割り当ての定義は扇と共有（二重定義しない）。
- データ（shoui_dict.json / chito_v2.csv / koyomi_v3.csv 等）＝**読むだけ**。
- 既存の盤UI（BoardGrid / PalaceCell）＝触らない。

---

## 7. やらないこと（範囲外）

- **道・経路の描画、目的地までのルート案内はしない**（思想維持）。アプリは「方角・吉凶・距離」を示すまで。どの道で行くかは使う人が選ぶ。
- **おすすめ施設の自動提示はしない**。出すのはユーザーが打った語の結果だけ。
- お気に入りの**クラウド同期・アカウント機能は今回作らない**（localStorageまで）。
- 施設データの自前DB化・補完はしない（OSMにある分だけ。地方でまばらなのは仕様として許容）。
- カテゴリの大量追加はしない（モックのKEYMAP＋自由入力で足りる。要望が出たら足す）。

---

## 8. テスト

- `dirForPoint`：北/北東/東…の座標を渡して、扇と同じ方位インデックスを返すこと（特に境界 22.5度・67.5度…の丸め）。**扇の方位割り当てと一致**を保証するテストを最優先。
- `bearing` / `distM`：方位角0/90/180/270と既知2点間距離が妥当。
- 振り分け：「コンビニ」「駅」→施設検索ルート、「川口市○○」「東京駅」→住所ジャンプルートに入ること（KEYMAP判定の単体テスト）。
- お気に入り：add/del/重複防止（同座標は1件）/localStorage保存・ロードの往復。
- お気に入りの再計算：基準点 `HOME` を変えると同じ場所の方位/点数が変わること。
- 既存テストは**全green維持**（エンジン非改変なので壊れない想定）。

---

## 9. 完成後に先生へ確認（チェックリスト追記分）

- 方位の境界の丸め方（45度均等・北=0中心）でよいか。流派で八方位の境界の取り方に差があれば教えてもらう。
- お気に入りの方位表示は「時盤・日盤どちらで出すか」。地図タブの現在モードに追従でよいか（お散歩中なら時盤、遠出モードなら日盤）。
- 住所ジャンプ先の方位も「徒歩(時盤)/遠出(日盤)」どちらの吉凶で見せるべきか（基本は現在タブのモード追従の想定）。

---

## 10. 進め方

1. 検索バー＋チップのUIを扇地図の上に差し込む（既存レイアウトに追記）。
2. 施設検索（Overpass・bbox）を実装 → ピン色＝方位、一覧、0件案内。
3. 住所ジャンプ（**国土地理院**ジオコーディング）を実装 → 飛ぶ＋ピン＋方位/距離、見つからない時は名前検索へフォールバック。
4. お気に入り（localStorage）：追加/一覧/タップ表示/削除、日時・基準点変更時の再計算。
5. 方位割り当てを扇と共有・一致確認（テスト）。
6. `npm test` / `npm run build` → Vercelプレビューで実機（iPhone）確認 → PR。**マージはユーザー（かなこ）が手動**。

*見た目・動きの正解は `map_search_proto.html`。ただし住所検索は本番で国土地理院ジオコーディングに差し替え（設計図§2準拠）。お気に入りはモックのメモリ保存を localStorage に置き換えて永続化。先生確認事項（§9）は完成後にまとめて依頼する。*
