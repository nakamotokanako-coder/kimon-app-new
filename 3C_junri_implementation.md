# Phase 3-C 実装指示：◎順利・秘伝判定ロジック追加

**担当**: Claude Code（kimon-app ローカル）
**プロジェクト**: `C:\Users\nakam\OneDrive\ドキュメント\kimon-app`
**規模**: 中
**前提**: 3-A（凶門 -40）はマージ済み。最新 main から作業ブランチを切ること。

---

## 🎯 ゴール

各宮の点数計算結果に対し、「◎順利（秘伝）」判定を追加する。
該当した宮は評価ランクを **「◎◎効果最大」に格上げ** して表示する。

**先生監修で仕様は完全確定済み。下記の通りに実装すること。**

---

## 📐 確定仕様（先生回答済み・変更不可）

### Step 1: 「日干のある宮」を特定する

その日の **日干**（日干支の天干。例：戊申 → 戊）が、盤のどの宮に
配置されているかを **天盤の干** で探す。← 先生確定（地盤ではなく天盤）

```
日干が坎宮の天盤干にある → 「坎」の行を使う
日干が乾宮の天盤干にある → 「乾」の行を使う
```

#### 日干が天盤のどこにも無い場合（先生指示・5/11受領）

時盤は8宮しかないため、日干が天盤上に存在しないことがある。その場合：

1. 天盤に日干があれば → その宮（基本ケース）
2. 天盤に日干が無ければ → **旬首（junshu）の宮を代替として使う**
   （盤データの `chokufu` / 旬首が入っている宮を基準宮とする）
3. 複合表記（1つの宮に2つの干が並ぶ中宮処理ケース）の場合は
   **片方ずつ分けて判定**し、どちらかが日干ならその宮を日干の宮とする

> 💡 既存の「スーパー吉方位」関連ロジックで日干の宮を特定している箇所が
> あれば、それを流用・共通化するのが望ましい。無ければ新規ヘルパーを作る。

### Step 2: 対象宮テーブルを引く

特定した「日干のある宮」をキーに、◎順利になりうる対象宮セットを得る。

| 日干のある宮 | ◎順利になりうる宮 |
|---|---|
| 坎 | 坎・兌・乾 |
| 艮 | 艮・離 |
| 震 | 震・巽・坎 |
| 巽 | 震・巽・坎 |
| 離 | 離・震・巽 |
| 坤 | 坤・離 |
| 兌 | 兌・乾・艮・坤 |
| 乾 | 兌・乾・艮・坤 |

※ 兌・乾は同じ4宮セット（先生確定・意図通り）。

### Step 3: 各対象宮で◎順利条件を判定（先生確定）

対象宮それぞれについて、**以下2つを両方** 満たすとき「◎順利」該当：

1. その宮の八門が **吉門**（生門・休門・開門・景門）である
2. その宮の **合計点が 40点以上** である

### Step 4: 表示への反映（先生確定）

◎順利に該当した宮は、評価ランクを **「◎◎効果最大」に格上げ** する。
（通常の評価ランク判定より優先。◎順利該当 = ◎◎効果最大として扱う）

---

## 🛠 実装方針

### 編集対象（想定）
- `src/kimon/scoreEngine.js` … 既に宮ごとの合計点・吉門判定を持っているはず。
  そこに◎順利判定を追加する形が自然。
- 日干の宮特定ヘルパー … 既存があれば流用、無ければ新規。
- 対象宮テーブルは定数（オブジェクト）として持つ。

### 八卦キーの対応（既存コードに合わせる）
```
kan=坎 / gon=艮 / shin=震 / son=巽 / ri=離 / kun=坤 / da=兌 / ken=乾
```
対象宮テーブルもこのキーで定義すること。

### 評価ランクとの関係（既存の評価ランク判定がある場合）
- 評価ランク判定（3-B）がまだ未実装でも、3-C は単独で着手可。
- ◎順利フラグを宮データに持たせ、最終的な評価ランク決定時に
  「◎順利フラグが立っていれば◎◎効果最大」とする。
- 3-B 未実装なら、◎順利フラグだけ先に立てておき、表示格上げは
  評価ランク実装時に接続する設計でもよい（その場合はコメントで明示）。

---

## ⚠️ 守ってほしいこと

1. **点数計算ロジック自体は変更しない**（合計点の出し方は既存のまま）。
   3-C は「判定の追加」であって「点数の変更」ではない。
2. 対象宮テーブルの中身は上記表と完全一致させること（兌・乾の4宮セットに注意）。
3. 「天盤の干で日干の宮を探す」を厳守（地盤ではない）。
4. 日干が天盤に無い場合の旬首フォールバックを必ず実装する
   （無いと一部の盤で日干の宮が決まらず判定が破綻する）。
5. 既存テストを壊さないこと。

---

## 🧪 テスト・確認

### ユニットテスト（追加）
- `test/scoreEngine.test.js`（または相当ファイル）に◎順利判定のケースを追加：
  - 日干が天盤の坎宮にある盤で、兌宮が「吉門 + 40点以上」→ 兌宮に◎順利が立つ
  - 同じ盤で、対象外の宮（例：離宮）は◎順利が立たない
  - 対象宮だが「吉門だが40点未満」→ ◎順利は立たない
  - 対象宮だが「40点以上だが凶門」→ ◎順利は立たない
  - 日干が天盤に無い盤 → 旬首の宮を基準に判定される

### ブラウザ確認
- `npm run dev` で初期日付（2026-05-18）の盤を表示し、
  ◎順利が出る宮があれば表記＆「◎◎効果最大」になっているか目視。
- 日干が天盤に無いケースになる日付でもエラーが出ないこと。

### ビルド
- `npm run build` が成功すること。
- `npm test` が全 pass すること。

---

## 🤔 もし実装中に判断に迷ったら（停止して確認）

以下に該当したら、勝手に解釈せず**作業を止めてぶりちゃんに報告**すること：

- 既存 scoreEngine.js に「宮ごとの合計点」や「吉門判定」が
  想定と違う形でしか無く、◎順利判定に必要な値が取れない場合
- 日干の宮特定ヘルパーが既存に無く、旬首の宮データの参照方法が不明な場合
- 評価ランク判定（3-B）が既に実装されていて、格上げの接続方法が
  仕様と食い違う場合

---

## ✅ 完了条件

1. ◎順利判定ロジックが scoreEngine（または相当箇所）に追加されている
2. 日干の宮特定（天盤優先・旬首フォールバック・複合表記分解）が動く
3. 対象宮テーブルが先生確定の表と完全一致
4. 「吉門 + 40点以上」の両条件判定が正しい
5. 該当宮が「◎◎効果最大」に格上げ表示される（または3-Bへ接続できる形でフラグ保持）
6. 追加テスト + 既存テストが全 pass、build 成功、ブラウザ目視OK

完了後、変更ファイルと追加した判定の要約を報告してください。
