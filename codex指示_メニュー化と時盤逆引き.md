# Codex実装指示：メニュー化 ＋ 時盤逆引き方位検索

> 対象：設計図「設計図_逆引き方位検索.md」の段階0.5（メニュー化）＋段階1〜3（時盤逆引き・時間帯一覧・地図）。
> レイアウトの目標：モック「reverse_direction_search_mockup.html」（スマホ縦・円形方位盤・ボトムタブ）。
> 前提：PR #15（盤の元エクセル配置）はマージ済み・mainは安定最新。テスト326件green維持が必須。

---

## 0. 大原則（必読）

- **既存の判定エンジンには一切手を入れない**。盤組立・点数計算は既存関数を呼ぶだけ。先生ロジックの独自性・正確さを守るため。
- **AIは使わない**。判定は全て既存の自前エンジン。
- 作業は**新しいブランチ**で。`git checkout main && git pull && git checkout -b phase-reverse-direction`
- 各ステップ後に `npm test`（326件green維持）と `npm run build`（成功）を必ず確認。
- 実機確認は**Vercelプレビュー**で行う（位置情報APIはHTTPS必須。LAN IP:5173では位置情報が取れない）。
- **この指示書の範囲を超えて実装しない**（§6の「やらないこと」を厳守）。

---

## 1. 再利用する既存エンジン（呼ぶだけ・改変禁止）

| 役割 | 場所 |
|---|---|
| 盤組立（日盤・時盤、chito_v2.csvからlookup） | `buildBoard`（src/kimon/ 配下） |
| 時干支計算（五鼠遁日法・子刻23:00開始） | `timeKanshi`（src/kimon/） |
| 点数判定（40点でusable、目的別タグ=恋愛/金運/仕事/学力） | `src/kimon/scoreEngine.js:159` |
| 格局判定 | `src/kimon/kakkyoku.js:110` |
| 十干剋応判定 | `src/kimon/jukkanKokuou.js:41` |
| 盤レベル・門迫・空亡・五不遇時 | `src/kimon/banLevel.js:254` |

- まず**これらの現状を読んで報告**してから実装に入ること（特にscoreEngineが宮ごとに点数をどう返すか、目的別タグの現状、時格・五不遇時を点数に織り込んでいるか）。

---

## 2. 段階0.5：メニュー化（ボトムタブ）

### やること
アプリ全体を3メニューのボトムタブ構成にする。
- ボトムタブ：**盤 / 吉方位 / 設定**（画面下に固定）
- 「盤」＝既存のメイン画面（今ある盤・象意パネル・盤情報）をそのまま割り当て。**中身は変えない**。
- 「吉方位」＝新規（§3で作る）。
- 「設定」＝テーマ切替・基準点まわり（既存のテーマ切替をここに移動 or 参照）。

### 初期表示タブ（重要）
- **アプリ起動時のトップ画面は「盤」**（＝現状どおり今日の盤が最初に出る）。逆引きの「吉方位」をトップにしない。
- 既存ユーザーの使い勝手を変えないため、起動時の見え方は現状維持。逆引きはボトムタブで「吉方位」をタップして入る。

### 実装の指針
- ルーティング（画面切替）を導入。最小構成でよい（重いルーターライブラリは不要、状態で出し分けでも可）。デフォルトの選択タブ＝「盤」。
- ボトムタブは `position: fixed; bottom: 0`。**コンテンツ側に padding-bottom をタブ高さ＋余白ぶん（約110px）確保**し、最下部までスクロールしてもタブと重ならないようにする（モックで実証済みの値）。
- スマホ/タブレット専用。既存の `max-width` 中央寄せ方針を踏襲。

---

## 3. 段階1〜3：吉方位画面（時盤逆引き）

レイアウトはモック `reverse_direction_search_mockup.html` のとおり。上から：
ヘッダー（モード名＋現在時辰）→ モードタブ（時盤お散歩/日盤遠出/期間検索※将来）→ 基準点 → 円形方位盤 → 1位カード → 目的フィルタ＋吉のみ表示 → 本日の時間帯別ベスト。

### 3-1. 基準点（段階1・4）
- **現在地**：ブラウザ標準 `navigator.geolocation.getCurrentPosition` で緯度経度取得。**外部API不要・課金ゼロ**。
- **場所検索**：国土地理院ジオコーディングAPI（`https://msearch.gsi.go.jp/address-search/AddressSearch?q=...`、CORS許可済み・キー不要）。地名/施設名→座標。
- 位置情報が取れない時（拒否・タイムアウト・PC）は**地域リスト手動選択にフォールバック**。
- 取得した座標は**ブラウザ内で経度補正に使うのみ。外部送信しない**。

### 3-2. 自然時補正（段階1・時盤の肝）
- 補正式：**補正分 = (経度 − 135) × 4分**（先生の補正表と一致を検算済み）。
- 端末時刻に補正を足した「自然時」で時辰を判定 → その時辰の時盤を `buildBoard` で組む。
- 時辰区切りは子刻=23:00開始（既存 timeKanshi に準拠）。

### 3-3. 8方位ランキング（段階1）
- 組んだ時盤の**8宮（中宮除く）のスコアを scoreEngine から取得 → 大きい順にソート**。
- 各宮の方角は**定位**（坎=北・離=南・震=東・兌=西・艮=北東・巽=南東・坤=南西・乾=北西）。8方位は45度均等（確定済み）。
- ランキング1位を「現在のベスト方位」カードに表示（方位・点数・主な吉要素）。

### 3-4. 円形方位盤（段階1+3）
- モックの円盤を踏襲：真北を上、45度ずつ8区画。
- 色分け：**色相＋濃淡**。吉=青系（濃=大吉/淡=小吉）、中立=グレー、凶=赤系。各区画に点数表示。
- **1位は区画を強調**（金枠を太く＋発光）。中央は基準点。
- 区画の色は点数帯で決定（しきい値は仮でよい・後から調整可能なように定数化）。

### 3-5. 時間帯別ベスト一覧（段階2）
- 1日12時辰をループして各時辰の時盤を組み、各時辰のベスト方位＋点数を一覧表示。
- 現在の時辰はハイライト。「吉のみ表示」ONなら凶時辰は「該当なし」表示。

### 3-6. 目的別フィルタ（段階5の前倒し・データ駆動）
- 目的6つ：仕事・金運・恋愛・出会い・健康・投資。
- **対応を外部JSON `purposeFilters.json` に集約**（コード直書き禁止）。スコア＝scoreEngine基礎点＋選択目的に合致する吉要素ボーナス。
- 判定要素は八門・八神・十干剋応・格局の4本立て。
- **初期値は仮**（先生確認前の叩き台）。`shoui_dict.json` の意味文から目的キーワードで抽出した値を入れる。**後でJSON差し替えのみで確定値に更新できる構造**にすること。

### 3-7. 吉凶の扱い
- **データは全件保持**（凶を削除しない。削除すると点数計算・ランキングが壊れる）。
- 表示は**デフォルト「吉のみ」ON**。トグルで凶も表示（避けるべき方位の確認用）。

---

## 4. 触ってよいファイル / 触ってはいけないファイル

### 触ってよい（新規 or 追加）
- 新規：吉方位画面のコンポーネント、円盤コンポーネント、自然時補正のユーティリティ、`purposeFilters.json`、ルーティング/ボトムタブ。
- 追加：上記に対応するテスト。

### 触ってはいけない
- `src/kimon/` 配下のエンジン群（buildBoard, scoreEngine, kakkyoku, jukkanKokuou, banLevel, junri, timeKanshi）＝**呼ぶだけ**。
- `shoui_dict.json` / `chito_v2.csv` / `koyomi_v3.csv` などのデータ＝**読むだけ**。
- 既存の盤UI（BoardGrid / PalaceCell）の中身＝メニューに割り当てるだけで**ロジック・表示は変えない**。

---

## 5. テスト
- 自然時補正の式（東京+19分など主要地点）の単体テスト。
- 8方位ランキングが点数順に並ぶこと。
- 「吉のみ表示」フィルタが凶を除外すること。
- 既存326件は**全green維持**（エンジンに触らないので壊れないはず）。

---

## 6. やらないこと（重要・この段階の範囲外）

- 日盤モードの本実装（モックのプレビュー止まりでよい。段階6で別途）。
- 期間検索（将来・タブ枠のみ）。
- 地図のピン詳細・経路表示（地図はまず現在地中心＋8方位の簡易表示で可。Leaflet+OSM導入は段階3で最小限）。
- ランキング同点処理の作り込み（まず点数順＋順利で並べる。重み付けの精緻化は先生回答後）。
- 時格・五不遇時の扱いの最終確定（**先生確認待ち**。初期は点数に入れて表示し、最終調整は回答後）。
- 鑑定メモ・顧客管理。

---

## 7. 進め方
1. まず§1の既存エンジンを読んで現状報告（実装前）。
2. 段階0.5メニュー化 → test/build → Vercelプレビューで確認。
3. 段階1時盤逆引き（基準点・自然時補正・8方位ランキング・円盤）→ test/build → 実機（Vercelプレビュー）。
4. 段階2時間帯一覧 → 段階5目的フィルタ（JSON叩き台）→ 段階3地図（最小）。
5. 各段階でPR。**マージはユーザー（かなこ）が手動**。

*先生確認2点（方位の定位／時盤の凶の扱い）は並行してユーザーが先生に確認中。回答が来たら反映するが、実装はこの指示書のとおり先行してよい。*
