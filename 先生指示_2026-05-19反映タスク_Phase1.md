# 先生指示_2026-05-19反映タスク Phase 1 (拡張版)

**作成日**: 2026-05-19
**対象Phase**: Phase 1（UI修正 + 盤レベル判定表示 + 五不遇時実装）
**先生からの指示元**: 画像3枚 + テキスト回答 + 解答Excel + ユーザー追加指示

---

## 🎯 ゴール

奇門遁甲アプリの表示を先生流派の正統な見た目に近づける + 重要な盤レベル判定を可視化する。

スコア計算ロジックの本格修正は **Phase 2 以降** に持ち越し、ここでは「**先生の最重要指示を表示まで反映する**」ことに集中する。

---

## 📋 タスク一覧

| # | 内容 | 影響ファイル候補 | 種別 |
|---|---|---|---|
| 1 | 暦情報セクション削除 | `src/components/MetaPanel.jsx` | UI削除 |
| 2 | 九星セクション「月」「日」を完全名称に | `src/components/MetaPanel.jsx` | UI修正 |
| 3 | 盤グリッドの宮ラベルを五行表記に + 中宮は「中」のみ | `src/components/PalaceCell.jsx` | UI修正 |
| 4 | メタパネルに「盤レベル判定」セクション追加 | `src/components/MetaPanel.jsx` | UI追加 |
| 5 | 五不遇時の判定ロジック新規実装 | `src/kimon/banLevel.js`（新規） | ロジック追加 |

---

## 🔧 詳細仕様

### Task 1: 暦情報セクション削除

**現状**:
```
暦情報
─ 日付      2026-05-09 0:00
─ 節気      立夏
─ 陰陽      陽
─ 三元      上元
─ 節気局数  陽4局
─ 時局数    陽4局
```

**先生指示**: このセクション**まるごと削除**

**残すセクション**:
- ✅ 盤情報
- ✅ 干支（年・月・日・時）
- ✅ 九星（年・月・日）
- ✅ Task 4 で追加する **盤レベル判定**

**実装方針**:
- `MetaPanel.jsx` から該当JSXブロックを削除
- **データ計算ロジックは残す**（節気・節気局数・時局数は他で使われている可能性）

---

### Task 2: 九星セクションの月・日表記を完全名称に

**現状**: 月=「五」、日=「五」のように略表記

**先生指示**: 「五黄土星」のような正式な九星名にする

**マッピングルール**:

| 数字 | 完全名称 |
|---|---|
| 一 | 一白水星 |
| 二 | 二黒土星 |
| 三 | 三碧木星 |
| 四 | 四緑木星 |
| 五 | 五黄土星 |
| 六 | 六白金星 |
| 七 | 七赤金星 |
| 八 | 八白土星 |
| 九 | 九紫火星 |

```javascript
const KYUSEI_FULL_NAMES = {
  '一': '一白水星', '二': '二黒土星', '三': '三碧木星',
  '四': '四緑木星', '五': '五黄土星', '六': '六白金星',
  '七': '七赤金星', '八': '八白土星', '九': '九紫火星',
};
function toKyuseiFullName(num) {
  return KYUSEI_FULL_NAMES[num] ?? num;
}
```

---

### Task 3: 盤グリッドの宮ラベル変更

**現状**:
```
坎 1 子, 艮 8 丑寅, 震 3 卯, 巽 4 辰巳,
離 9 午, 坤 2 未申, 兌 7 酉, 乾 6 戌亥, 中 5
```

**先生指示**: 九星番号を削除し、宮名に五行を付与。**中宮のみ例外で「中」のみ表示**。

| 現状 | 修正後 |
|---|---|
| 坎 1 | **坎水** |
| 艮 8 | **艮土** |
| 震 3 | **震木** |
| 巽 4 | **巽木** |
| 離 9 | **離火** |
| 坤 2 | **坤土** |
| 兌 7 | **兌金** |
| 乾 6 | **乾金** |
| 中 5 | **中**（数字・五行なし） |

```javascript
const PALACE_FIVE_ELEMENTS = {
  '坎': '水', '艮': '土', '震': '木', '巽': '木',
  '離': '火', '坤': '土', '兌': '金', '乾': '金',
  // 中宮は意図的に含めない
};
function withFiveElement(palaceName) {
  const el = PALACE_FIVE_ELEMENTS[palaceName];
  return el ? `${palaceName}${el}` : palaceName;
}
```

---

### Task 4: メタパネルに「盤レベル判定」セクション追加

**現状**: 盤レベル判定（伏吟・反吟・門迫・空亡・五不遇時）が**表示されていない**。

**先生指示**: 盤全体で発生している盤レベル判定を明示的にユーザーに見せる。

**追加するセクション**:

```
盤レベル判定
─ 伏吟      [該当時のみ表示: 例「干伏吟」]
─ 反吟      [該当時のみ表示: 例「星反吟」]
─ 門迫      [該当時のみ表示: 例「坎宮+生門」]
─ 空亡      [該当時のみ表示: 例「寅宮・卯宮」]
─ 五不遇時  [該当時のみ表示: 例「甲日×庚時」]
```

**表示ルール**:
- 該当しない場合: その項目は非表示
- 全部該当しない場合: セクション自体「特になし」または非表示
- 該当する判定は**赤字 or 警告色**で目立たせる

**実装方針**:

#### 4-1. 既存検出ロジックの確認
- `buildBoard.js` または `scoreEngine.js` で既に検出しているはず
- 検出結果を `board.banLevel = { fukugin, hangin, monpaku, kuubou, gofuguuji }` のような構造で渡す
- もし検出が無い項目は **Phase 2 で追加実装** とし、今回は既存検出分だけ表示

#### 4-2. UI 例

```jsx
{board.banLevel && (
  <Section title="盤レベル判定">
    {board.banLevel.fukugin && <Row label="伏吟" value={board.banLevel.fukugin} alert />}
    {board.banLevel.hangin && <Row label="反吟" value={board.banLevel.hangin} alert />}
    {board.banLevel.monpaku && <Row label="門迫" value={board.banLevel.monpaku} alert />}
    {board.banLevel.kuubou && <Row label="空亡" value={board.banLevel.kuubou} alert />}
    {board.banLevel.gofuguuji && <Row label="五不遇時" value={board.banLevel.gofuguuji} alert />}
  </Section>
)}
```

---

### Task 5: 五不遇時の判定ロジック実装（新規）

**仕様**: 日干と時干の特定組み合わせで凶時刻。先生いわく「**盤自体使わない方が無難**」。

**判定テーブル**:

| 日干 | 凶時干 | 日干 | 凶時干 |
|---|---|---|---|
| 甲 | 庚 | 己 | 乙 |
| 乙 | 辛 | 庚 | 丙 |
| 丙 | 壬 | 辛 | 丁 |
| 丁 | 癸 | 壬 | 戊 |
| 戊 | 甲 | 癸 | 己 |

**注意**: **時盤の時のみ判定対象**（年盤・月盤・日盤では適用しない）。

#### 5-1. 新規ファイル: `src/kimon/banLevel.js`

```javascript
// src/kimon/banLevel.js

const GOFUGUUJI_TABLE = {
  '甲': '庚', '乙': '辛', '丙': '壬', '丁': '癸', '戊': '甲',
  '己': '乙', '庚': '丙', '辛': '丁', '壬': '戊', '癸': '己',
};

/**
 * 五不遇時を判定
 * @param {string} dayKan - 日干
 * @param {string} hourKan - 時干
 * @param {string} boardType - 盤種（'時' のみ判定対象）
 * @returns {string|null} 該当時は「甲日×庚時」、該当しない時は null
 */
export function detectGofuguuji(dayKan, hourKan, boardType) {
  if (boardType !== '時') return null;
  if (!dayKan || !hourKan) return null;
  if (GOFUGUUJI_TABLE[dayKan] === hourKan) {
    return `${dayKan}日×${hourKan}時`;
  }
  return null;
}
```

#### 5-2. buildBoard.js から呼び出し

```javascript
import { detectGofuguuji } from './banLevel.js';

// boardType==='時' の時のみ
const gofuguuji = detectGofuguuji(dayKan, hourKan, boardType);
board.banLevel = {
  ...existingBanLevel,
  gofuguuji,
};
```

#### 5-3. テスト追加: `test/banLevel.test.js`

```javascript
import { describe, it, expect } from 'vitest';
import { detectGofuguuji } from '../src/kimon/banLevel.js';

describe('detectGofuguuji', () => {
  it('甲日×庚時 で検出', () => {
    expect(detectGofuguuji('甲', '庚', '時')).toBe('甲日×庚時');
  });
  it('癸日×己時 で検出', () => {
    expect(detectGofuguuji('癸', '己', '時')).toBe('癸日×己時');
  });
  it('甲日×丙時 では検出しない', () => {
    expect(detectGofuguuji('甲', '丙', '時')).toBeNull();
  });
  it('日盤では検出しない', () => {
    expect(detectGofuguuji('甲', '庚', '日')).toBeNull();
  });
  it('日干・時干が空はnull', () => {
    expect(detectGofuguuji('', '庚', '時')).toBeNull();
    expect(detectGofuguuji('甲', '', '時')).toBeNull();
  });
});
```

#### 5-4. スコア減点について

先生「**盤自体使わない方が無難**」と明言だが、**具体的な減点値はPhase 2で先生に確認**。
Phase 1 では「**検出して表示するのみ**」、スコアには反映しない。

---

## ⚠️ 確認事項（実装中に判断が必要な場合）

### Q1: 伏吟・反吟・門迫・空亡の現状検出状態

`buildBoard.js` または `scoreEngine.js` で既に検出しているはず。
- どの種類が実装済みかを確認
- 検出結果がメタパネルに渡されているかを確認
- 結果が `board.banLevel` のような構造でない場合、その構造に整える
- **検出ロジック自体の修正は Phase 2** に持ち越し（今回は表示追加のみ）

### Q2: 五不遇時のスコア減点値

Phase 1 では**未確定**として扱う。Phase 2 で先生に確認後に実装。
今回は表示のみ。

### Q3: 中宮ラベル「中」の文字色・サイズ
- 他の宮との統一性は保つ
- ただし、中宮セルは内側の盤情報がない（「—」表示）ため、見た目が浮く可能性
- 必要なら少し薄めの色にして「使えない宮」感を出してもよい（任意判断）

---

## 🧪 検証方法

### 動作確認

1. `npm run dev` でローカル起動
2. 以下の日時で目視確認:

| 確認用日時 | 確認内容 |
|---|---|
| 2026-05-09 00:00 時盤 | UI全般（五黄土星表記、宮ラベル、暦情報削除） |
| 五不遇時が発生する任意の日時 | 五不遇時表示 |

### チェックリスト

| 項目 | 期待結果 |
|---|---|
| 「暦情報」セクションが消えている | ✅ |
| 九星セクションの月・日が「五黄土星」等の完全名称 | ✅ |
| 盤グリッドの宮ラベル「坎水」「艮土」など五行付き | ✅ |
| 中宮セルのラベルが「中」のみ | ✅ |
| 「盤レベル判定」セクションが表示される | ✅ |
| 該当する盤レベル判定が表示される（赤色等） | ✅ |
| 既存機能（盤種切替、テーマ切替、スコア）が壊れていない | ✅ |

### テスト

- 既存テスト211件が **全てpass**
- 新規テスト（五不遇時）追加で **216件以上**

### Build

- `npm run build` が成功すること

---

## 📦 コミット規約

```
feat: UI修正と盤レベル判定表示（先生指示 2026-05-19 Phase 1）

UI修正:
- 暦情報セクションを削除
- 九星表記を完全名称に（月・日も「五黄土星」等）
- 盤グリッドの宮ラベルを五行表記に変更
  - 坎1→坎水, 艮8→艮土, 震3→震木, 巽4→巽木
  - 離9→離火, 坤2→坤土, 兌7→兌金, 乾6→乾金
  - 中宮は「中」のみ表示

盤レベル判定:
- メタパネルに「盤レベル判定」セクションを追加
  - 伏吟・反吟・門迫・空亡・五不遇時を表示
- 五不遇時の判定ロジックを新規実装（banLevel.js）
  - 日干×時干 10通りの組み合わせで凶時刻を検出
  - 時盤のみで判定
  - スコア減点値はPhase 2で先生に確認後に実装予定

Tests: 211+ passing
Refs: 先生指示_2026-05-19反映タスク_Phase1.md
```

push後はVercel自動デプロイで本番反映。

---

## 🔜 次のPhase（参考）

このPhase 1完了後、以下のPhaseが予定されています:

- **Phase 2**: 盤レベル判定の完全実装
  - 伏吟❷❸（星伏吟・門伏吟）の追加
  - 反吟2種の判定修正
  - 門迫4パターン限定への修正
  - 空亡 -10 の実装
  - 五不遇時の減点値確定
- **Phase 3**: 十干剋応・格局の判定漏れ調査＆修正
  - 奇入太陰、太孛入刑、日奇入霧 などの追加
  - 凶の十干剋応を格局欄にも併記
- **Phase 4**: ランタイム計算系（歳格・月格・日格・伏干・雲干）

Phase 1 完了後、Claude Code は「Phase 1 完了報告」を出して**待機**してください。

---

## 📞 質問・確認

Claude Code が実装中に判断に迷う箇所があれば、**ユーザー（ぶりちゃん）に確認を求めて**ください。

以上。
