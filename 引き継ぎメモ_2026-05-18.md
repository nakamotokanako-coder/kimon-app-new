# 引き継ぎメモ（2026-05-18）

**対応者**: Claude Code（kimon-app ローカル）
**元指示**: `先生指示_2026-05-18反映タスク.md`
**結果**: 全タスク完了。`npm test` 211 pass / `npm run build` 成功。

---

## ✅ 今回反映した内容

### Phase 1: 日局数の新データ運用開始

- `data/day_kyokusu.csv`（最終版・2026-01-01〜2044-01-31・6605日）を正式採用。
- 新ローダー `src/kimon/loadDayKyokusu.js` を作成（`loadKoyomi.js` と同じ流儀、O(1) Map）。
  - UTF-8 BOM を除去してパース。
  - `lookupDayKyokusu(date)` / `dayKyokusuRange()` をエクスポート。
- `src/kimon/buildBoard.js`：`boardType==='日'` の局数取得を
  **旧 `koyomi.kyokusu` → 新 `lookupDayKyokusu(date)`** に差し替え。
  - 干支（eto）は引き続き `koyomi.eto_day` を使用。
- **範囲外日付は明示エラー**（先生推奨方針／ぶりちゃん確認済み）:
  - 文言: `日盤は2026-01-01〜2044-01-31の期間のみ鑑定できます（指定日: {date}）`
  - App.jsx は既存の error 表示で受ける（変更不要）。
- App.jsx の初期日付を `2025-12-05` → **`2026-05-18`**（範囲内）に変更。

#### 注意（重要）
- `koyomi_v3.csv` 自体が **2044-01-31 まで**のため、上限を超えた日付は
  buildBoard では koyomi 側エラーが先に出る（日盤範囲エラーには到達しない）。
  day_kyokusu 単体の上限境界は `loadDayKyokusu` のユニットテストで担保。
- 旧 `koyomi_v3.csv` の `kyokusu` 列は **温存**（時盤・他用途で参照の可能性）。
  → 使用箇所の全洗い出しは別タスク（下記 残課題A）。

### Phase 2: 刑格の二重表記対応（5/12「解決済み」は撤回）

- **撤回**: 5/12 時点で「刑格は十干剋応と重複のため格局から削除」としていたが、
  先生指示により **別概念（算出の種類が異なる）として復活**。
- `data/kakkyoku.json`：
  - `kyo.刑格`（凶格 / score -10 / reading けいかく）を追加。
  - `meta.separated_from_jukkanKokuou` から「刑格」を除外（8→7格局）。
  - `meta.version` を `2026-05-18` に更新、分離注記を更新。
- `src/kimon/kakkyoku.js`：天盤=庚 × 地盤=己 で `刑格`(kyo) を検出する分岐を追加。
  - `tenbanHas`/`chibanHas`（splitKan ベース）使用 → 複合表記対応。
    - 例: 天「庚」×地「己丁」→ 該当 ／ 天「丙己」×地「己」→ 非該当。
- **ダブル減点が正しい挙動**:
  - 十干剋応「官符刑格」(×, -10) ＋ 格局「刑格」(-10) ＝ **合計 -20**
  - 天盤庚自体の -20 と合わせ、該当宮 subtotal は **-40**（テストで担保）。
- `scripts/generate-kakkyoku-csv.mjs` 再実行 →
  `data/chito_v2_with_kakkyoku.csv` 再生成。
  - 格局列に `×刑格` が 244 箇所出現、十干剋応「官符刑格」と共存を確認。

#### 付随変更
- `src/kimon/kakkyoku.js` の JSON import を
  `import ... with { type: 'json' }` に統一（`jukkanKokuou.js` と同流儀）。
  - これにより生成スクリプトを **素の `node` で実行可能**に
    （`vite-node` 未インストールでも動く）。テスト/ビルドは従来どおり pass。

---

## 🧪 追加テスト

- `test/buildBoard.test.js`
  - 2026-01-01（陽3局乙亥）日盤 全8宮一致。
  - 2026-06-21（夏至切替 → 陰7局丙寅）。
  - 範囲外（2025-12-31）明示エラー＋指定日含有。
  - `loadDayKyokusu` 単体: 境界内/下限外/上限外/対応期間。
- `test/kakkyoku.test.js`：刑格 検出・複合表記・非該当・スコア。
- `test/scoreEngine.test.js`：刑格ダブルカウント（jukkan -10 / kakkyoku -10 / subtotal -40）。

---

## 📌 残課題（持ち越し）

- **A. 旧 `kyokusu` 列の整理**: koyomi_v3.csv の `kyokusu` 列の使用箇所を全洗い出し、
  仕様確定後に削除判断（別タスク）。年盤・月盤は 5/18 時点で削除済。
- **A-1. UIレイアウト**調整（持ち越し）。
- **A-2. 残り格局の修正**（持ち越し）。
- **過去日付（2026-01-01 より前）の鑑定**: 先生「続きなし」。需要薄。
  UI で「日盤は 2026-01-01 以降のみ」アナウンスは現状エラー表示で代替。

---

## 確認した検証ポイント

- 2026-01-01: day_kyokusu=陽3局 / koyomi eto_day=乙亥 → key `陽3局乙亥`（chito_v2 存在）。
- 2026-06-21: 夏至で陽→陰、陰7局（指示書の法則と一致）。
- 2026-05-18（App 初期値）: 陽5局 / eto_day 壬辰 → key `陽5局壬辰`（chito_v2 存在）。
- `npm test`: 6 files / 211 tests 全 pass。
- `npm run build`: 成功（chunk-size 警告は従来からの既知、エラーではない）。

**未コミット**（push は未実施 / 指示があれば対応）。
