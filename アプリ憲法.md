# 奇門遁甲アプリ（kimon-app）アプリ憲法

> **これは「不変の決まり」を集約した1枚。** 日々の出来事は引き継ぎメモへ。ここには"何回読んでも変わらない"ことだけを書く。
> 次チャットは **本ファイル ＋ 直近の引き継ぎメモ1枚** を読めばフル装備で動ける。
> 最終更新：2026-05-27（Codexが実コードと照合して補強・訂正）
> 出典：SPEC.md / 奇門遁甲講座2025_まとめ.md / 一枚要約 / 各引き継ぎメモ / 設計図_逆引き方位検索.md

---

## 0. 絶対原則（これだけは絶対に守る）

1. **先生のデータが唯一の正。** 理論計算・ネットの定説とズレたら、必ず先生提供データを優先（SPEC.md §7）。◯◯＝流派監修・暦データ提供の共同開発者。【要確認】リポジトリ内では実名を確認できず、旧資料にも先生名の伏せ字表記が残っている。
2. **判定は「lookup」、ロジックを再発明しない。** 盤の全要素（天地盤干・八神・九星・八門・十干剋応・格局）は先生のExcel（数式45,705個・約120万セル）で計算済み。アプリは日付・時辰をキーに**引くだけ**。格局や十干剋応の成立条件を自前で判定するコードは書かない。
3. **占術エンジンには触らない。** `src/kimon/` 配下（buildBoard / scoreEngine / kakkyoku / jukkanKokuou / banLevel / junri / timeKanshi）は**呼ぶだけ**。新機能はこれらを呼ぶ薄いラッパー＋UIで作る。点数計算ロジックを変えると全検算と数百件テストが崩れる。
4. **データCSV・shoui_dict.json は読むだけ**。書き換えない。
5. **キャラ**：関西弁・タメ口のフランクな事業ブレストの相棒（かなこの希望）。**Codexへの指示は必ずコードブロック内**に書く（コピペしやすいように）。

---

## 1. プロダクト概要

- **何か**：プロ占術師が、日々・毎時の奇門遁甲の盤（吉凶方位）を手計算せずブラウザで確認できるツール。「◯◯の流派の盤組立・吉凶判定をWeb化したもの」。【要確認】実名はリポジトリ内で確認できず。
- **二層構造の構想**：
  - **プロ版**（kimon-app・先生監修データ・全機能）＝買い切り3万円想定 ← **今ここに集中**
  - 一般版（簡易「今日の吉方位」）＝月額580〜980円想定 ← 構想のみ・未着手
- **出発点**：先生のExcel「奇門遁甲Pro5_5.xlsx」（21シート）を全解析したもの。

---

## 2. リポジトリ・環境・運用フロー（不変）

| 項目 | 値 |
|---|---|
| 正本リポジトリ | `nakamotokanako-coder/kimon-app-new`（origin: `https://github.com/nakamotokanako-coder/kimon-app-new.git`。Private・Vercel本番接続） |
| 旧リポジトリ（触らない） | `nakamotokanako-coder/kimon-app` |
| 本番URL | https://kimon-app-new.vercel.app/ |
| ローカル正本 | `C:\dev\kimon-app`（現在ブランチは `feat-kakkyoku-search`）。旧パス `C:\Users\nakam\OneDrive\...\kimon-app` の記述は古い。現行作業・実コード照合の正本は `C:\dev\kimon-app`。 |
| スタック | React 18 + Vite 5 + Vitest 4 + Leaflet 1.9 + PapaParse（`package.json` 確認） |
| npm scripts | `dev`=`vite` / `build`=`vite build` / `preview`=`vite preview` / `test`=`vitest run` / `test:watch`=`vitest` |
| デプロイ | git push → Vercel 自動デプロイ |

### 開発フロー（毎回これ）
```
Claude Web が指示書を書く（できれば本番ソースを読んでから）
  → C:\dev\kimon-app 直下に指示書を置く
  → Codex が実装
  → かなこが実機確認（GPS等はVercelプレビューをiPhoneで）
  → PR（base=main）
  → ★かなこが手動マージ（自動マージしない）
  → 本番デプロイ反映を確認
```

---

## 3. ★デグレ防止・過去の事故（必ず読む）

- **PR前に必ず base が `main` か確認。** Codexが別ブランチ上で作業していることがある（#27の事故）。`git switch -c <新> origin/main` でmainから切り直す（#29で回避成功）。
- **マージは必ず手動。** かなこが実機確認してから。動作未確認でマージしない。
- **GPS/位置情報はHTTPS必須**。ローカルでは取得不可 → 実機確認はVercelプレビューURLをiPhoneで。
- **GPSはデフォルトOFF・手動起動が鉄則**（先行アプリのタイムアウト8秒バグの教訓）。
- **地図の全画面モード `.is-fullscreen` と `invalidateSize` effect は触らない**＝過去2回のデグレ箇所。現行 `src/reverseDirection/DirectionMap.jsx` では、扇・ピン再描画後の `window.setTimeout(() => map.invalidateSize(), 0)` が532行、全画面切替後の `window.setTimeout(() => map.invalidateSize(), 80)` が552行、`.is-fullscreen` 付与が557行。
- **地図に手を入れる機能は専用レイヤーに分離**（現在地マークは `liveLayerRef`、扇は `layerGroupRef`。分離を壊すとチラつく）。
- 指示書は**実コードを読んでから書く**と精度が段違い。

---

## 4. 占術エンジン仕様（先生確定・不変）

### 4-1. 盤組立（buildBoard）
- 実ファイル：`src/kimon/buildBoard.js`。公開関数は `buildBoard({ date, hour, boardType })`。
- 現行実装の `boardType` は **`日` / `時` のみ**。`年` / `月` は `buildBoard.js` では未対応で、指定すると `boardType は "日" または "時" のみ` エラー。
- **完全展開盤テーブルからlookup**：`key = kyokusu + eto` で `lookupChito(key)` を呼び、`chito_v2_with_kakkyoku.csv` の1行から8宮の `tenban/chiban/kyusei/hasshin/hachimon` を展開する。
- 暦DB：`koyomi_v3.csv`。日局数は `day_kyokusu.csv`（2026-01-01〜2044-01-31）を `lookupDayKyokusu(date)` で参照。時盤局数は `koyomi.time_kyokusu` を使う。
- 時干支：`src/kimon/timeKanshi.js` の `calcTimeKanshi(dayKan, hour)`。`hourIdx = Math.floor(hour / 2) % 12` なので、アプリの時盤スロット `0` は `23-1時` として扱う。
- 時盤の自然時補正は逆引き方位側 `src/reverseDirection/reverseDirection.js` の `getLongitudeCorrectionMinutes(longitude) = Math.round((longitude - 135) * 4)` と `applyNaturalTime()` で処理。扇の方位は `src/reverseDirection/mapFan.js` 側。【要確認】真北基準・45°×8等分の最終仕様はコードコメント上は明示が薄い。
- 中宮処理3パターン（旬首中宮／時干中宮／甲中宮）はCSV側で処理済みとされるが、現行 `buildBoard.js` は `futatsu_no_tokoro` 列を読んでいない。【要確認】同列は現行CSVヘッダにも存在しない。

### 4-2. 点数判定（scoreEngine）— 宮ごとの配点
| 要素 | 配点 |
|---|---|
| 三奇六儀 | 乙・丙・丁=+10 / 庚=−20 / 他=0 |
| 八門 | 休・生・開=+40 / 景=+20 / **傷・杜・死・驚=−40** |
| 天蓬九星 | 天冲・天輔・天心・天任=+10 / 他=0 |
| 八神 | 直符・太陰・六合・九地・九天=+20 / 螣蛇・勾陳・朱雀=−20 |
| 十干剋応 | 〇=+10 / ×=−10 / △=0 |
| 格局 | 吉=+10 / 凶=−10 |
| 空亡（該当宮のみ） | −10 |
| 盤レベル（全宮一律） | 干伏吟・星伏吟・門伏吟・星反吟・門反吟・五不遇時を各-10。複数該当は加算（例：3つで-30） |

- 実ファイル：`src/kimon/scoreEngine.js`。主要関数は `scorePalace()` / `scoreBoard()`、閾値は `USABLE_THRESHOLD = 40`。
- 十干剋応は `src/kimon/jukkanKokuou.js` の `scoreJukkanKokuou()` で計算。複合干で複数該当する場合は **合計ではなく平均**（`sum / results.length`）。これは§9の未解決事項にも残す。
- 格局は `src/kimon/kakkyoku.js` の `detectPalaceKakkyoku()` を呼ぶ。吉格は +10、凶格は -10。
- **usable（使える方位）= 合計40点以上**。`scorePalace().usable` が全機能共通の足切り基準。

### 4-3. 盤レベル判定（banLevel.js）
- 実ファイル：`src/kimon/banLevel.js`。主要関数は `detectBanLevel(board)` / `scoreBoardBanLevel(banLevel)` / `scorePalaceBanLevel(palaceName, palaceData, kuubouStr)`。
- 判定対象宮は `PALACE_NAMES = ['kan','gon','shin','son','ri','kun','da','ken']` の8宮のみ。**中宮は判定対象外**。
- `detectBanLevel()` の戻り値キー：`kan_fukugin` / `sei_fukugin` / `mon_fukugin` / `sei_hangin` / `mon_hangin` / `monpaku_palaces` / `kuubou_palaces` / `kuubou_text` / `gofuguuji`。
- 伏吟3種：干伏吟（全8宮で天盤先頭干=地盤先頭干）／星伏吟（九星が定位通り）／門伏吟（八門が定位通り）。各-10、独立カウント。
- 反吟2種：星反吟・門反吟（全8宮で定位の逆配置）。各-10。
- 五不遇時：`boardType === '時'` のみ、日干×時干10通り。該当時は `甲日×庚時` のような文字列、非該当時は `null`。盤全体で-10。
- 門迫＝4パターン限定（該当宮のみ-10）：**坎+生門 / 震+開門 / 離+休門 / 兌+景門**。凶門は門迫にならない。
- 空亡＝`meta.kuubou` の地支が宮の地支に1文字でも含まれれば該当宮のみ-10。

### 4-4. ◎順利・秘伝（junri.js・先生監修済み）
- 実ファイル：`src/kimon/junri.js`。主要関数は `findDayKanPalace(palaces, meta)` / `detectJunri(board, palaceScores)`。
- 起点宮は、その日の**日干が天盤に配置された宮**を8宮から検索して特定。複合表記の天盤干は1文字ずつ分解する。
- 日干が天盤に無い場合は、`meta.tenban_junshu_p`（日本語宮名）を英字キーに変換して代替。さらに無ければ `meta.junshu` を天盤からスキャン。どれも無ければ `null`。
- 対象宮テーブル（日干の宮→順利になりうる宮）：坎→坎兌乾／艮→艮離／震・巽→震巽坎／離→離震巽／坤→坤離／兌・乾→兌乾艮坤。
- 条件：対象宮が「吉門（休門・生門・開門・景門）」かつ「合計点 >= 40」→ `is_junri = true`。順利は点数自体を変更しない。
- `detectJunri()` の戻り値キー：`day_kan_palace` / `targets` / `junri_palaces`。
- `scoreBoard()` の戻り値トップレベル：`day_kan_palace` / `junri_palaces`。各宮スコアには `is_junri` が付与される。`targets` は `scoreBoard()` トップレベルには返らない。

### 4-5. 評価ランク（上から優先・★上位2つは先生確認待ち）
【要確認】現行コードには、以下の日本語ランク（XX超危険／◎◎効果最大など）を一元的に返す関数は見当たらない。実装済みなのは `scoreBoard()` の点数・`usable`・`is_junri`、逆引き側 `getScoreTone(score)` の色調分類（40以上 great / 20以上 good / 1以上 weak / 0 neutral / -20以下 bad-strong / それ以外 bad）。下記は仕様メモとして残す。
```
XX超危険  : 庚・凶門・五不遇時・伏吟反吟が3つ すべて揃う  ← Q2定義 保留中
✕危険     : 庚または凶門がある
△注意    : それ以外（「凶門があるが庚がない」で確定。※五不遇時の扱いだけ未確認）
◎◎効果最大 : 40点以上・吉門・秘伝(順利)該当
◎とても良い : 吉門で80点以上
〇良い     : 40点以上
－無難     : 吉門だが40点以下
```

---

## 5. ★格局・十干剋応データの地図（今日の発見・最重要）

データファイル：**`data/chito_v2_with_kakkyoku.csv`**。`src/kimon/loadChito.js` は `import csv from '../../data/chito_v2_with_kakkyoku.csv?raw'` でこのCSVを読んでいる。無印 `data/chito_v2.csv` は現行ロード対象ではない。
各宮サフィックス：kan=坎 / gon=艮 / shin=震 / son=巽 / ri=離 / kun=坤 / da=兌 / ken=乾。現行CSVヘッダは8宮それぞれに `tenban_` / `chiban_` / `kyusei_` / `hasshin_` / `hachimon_` / `jukkan_kokuou_` / `kakkyoku_` を持つ。

| 列 | 中身 |
|---|---|
| `kakkyoku_◯◯` | 格局（九遁9つ＋玉女守門＋三奇得使/昇殿＋凶格など） |
| `jukkan_kokuou_◯◯` | 十干剋応81通り。**★青龍返首・飛鳥跌穴はここに入る**（格局列ではない！） |

- セルは**複合表記**：`◯雲遁;×青龍逃走`、`◯丁奇得使;◯玉女守門;×丁奇入墓` のように `◯/×` ＋ `;` 区切り。
- **格局を名前で探すときは「両列を部分一致」**。現行 `src/reverseDirection/kakkyokuSearch.js` の `findSpecialKakkyokuMatches()` も `kakkyoku.includes(name) || jukkanKokuou.includes(name)` で判定する。完全一致だと複合表記に当たらない。
- 「飛鳥跌穴が格局列に無い＝バグ」ではない。**仕様**（天地盤だけで決まる格は十干剋応側）。
- 実データ検算（2026-05-27、1080行・8宮セルを部分一致で集計）：`jukkan_kokuou_*` に 青龍返首123・飛鳥跌穴119。`kakkyoku_*` に 玉女守門97・雲遁56・鬼遁38・風遁36・龍遁24・神遁20・地遁19・人遁12・天遁11・虎遁4。なお `人遁` は `jukkan_kokuou_*` 側にも部分一致で120件ヒットするため、検索仕様上は両列検索が必須。

### 先生の「特別な効果がある12格局」（格局検索ビューDの対象）
実装ファイル：`src/reverseDirection/kakkyokuSearch.js` / `src/reverseDirection/KakkyokuSearchView.jsx`。`SPECIAL_KAKKYOKU_GROUPS` に集約。
旬首2大吉格：青龍返首・飛鳥跌穴（→十干剋応列）
九遁：天遁・地遁・人遁・雲遁・風遁・龍遁・虎遁・神遁・鬼遁（→格局列）
人の和合：玉女守門（→格局列）

---

## 6. 機能の現状（2026-05-27時点・実コード照合）

### 実装済み・本番稼働
- 盤組立（日盤・時盤）：`src/kimon/buildBoard.js` / `src/kimon/buildHourBoard.js`。年盤・月盤は現行 `buildBoard()` では未対応。
- 五鼠遁日法：`src/kimon/timeKanshi.js`。
- 点数エンジン：`src/kimon/scoreEngine.js`。盤レベル判定：`src/kimon/banLevel.js`。◎順利：`src/kimon/junri.js`。十干剋応：`src/kimon/jukkanKokuou.js`。格局判定：`src/kimon/kakkyoku.js`。
- UI：5×5グリッド盤（`src/components/BoardGrid.jsx` / `src/components/PalaceCell.jsx`）。外周に方位＋十二支、中央3×3が宮。**盤デザイン刷新済み**（Noto化・aspect-ratio 1/1.28で端末非依存サイズ固定・八門ベタ塗り 吉#4472C4/凶#FF0000・順利は青枠のみ）。
- 象意パネル：`src/components/ShouiPanel.jsx`、データ読込は `src/kimon/loadShouiDict.js` / `data/shoui_dict.json`。
- **逆引き方位検索＋地図連動**：`src/reverseDirection/ReverseDirectionView.jsx` / `reverseDirection.js` / `DirectionMap.jsx` / `mapFan.js` / `mapSearch.js`。時盤お散歩／日盤遠出、扇表示、現在地マーク＋追っかけ線、距離・方位表示、GPS手動ON。
- 最強ランキング（日盤・期間スキャン・5段階ソート）：`src/reverseDirection/SaikyoRankingView.jsx` / `strongestRanking.js`。
- 格局検索（ビューD・時盤専用）：`src/reverseDirection/KakkyokuSearchView.jsx` / `kakkyokuSearch.js`。`ReverseDirectionView.jsx` の `mode === 'kakkyoku'` で表示。
- テスト **373 passed（16ファイル）**（`npm test` 実行、2026-05-27）。`npm run build` 成功（Viteの大きいchunk警告あり）。

### 未完・進行中
- 評価ランク上位（XX超危険）＝先生確認待ち（Q2/Q4五不遇時）。現行コードに日本語ランク決定関数は未実装。
- 点数エンジンのバグ修正（`jukkanKokuou.js` の複合干平均処理）＝先生回答待ち。【要確認】現行実装は平均（`sum / results.length`）で、これが意図値か未確定。
- 検索ビューB（奇門三盤ルート＝玉の輿ツアー）・ビューC（方角固定の日探し）＝未着手。
- 目的別フィルタ（金運/恋愛/仕事…）＝`purposeFilters.json` でデータ駆動の設計のみ。

### 凍結中
- 販売準備（ライセンス保護・販売LP・契約書）、管理画面/鑑定業務管理、引越し用判定（歳破・五黄殺・年三殺）、気口、App Store化（Phase 4・Capacitor想定）。

---

## 7. UI・デザインの決まり（不変）

- スマホ/タブレット専用（PC幅は中央にスマホ幅で表示）。max-width 430〜480px。
- 配色：吉=青、凶=赤。八門ベタ塗り 吉 `#4472C4`／凶 `#FF0000` 白抜き。
- フォント：全要素を `Noto Sans JP`（ゴシック）に統一。明朝（`Noto Serif JP`）は使わない。見出し・宮名・大きい数字も `Noto Sans JP` に揃える。
- Webフォントは `index.html` の `<link>` で読み込む。CSS の `@import` は本番で効かないことがあったため、`<link>` に一本化した。
- 和テーマ世界観：黒地（#0f0d08）＋金（#b8923a / 見出し #e6c34a）。
- 表示は**デフォルト吉のみ**、プロ向け「凶も見る」トグルを残す。
- ナビ：ボトムタブ3つ（盤／吉方位／設定）。「吉方位」の中で画面内タブ（時盤お散歩／日盤遠出／最強ランキング／格局検索…）。共通部品（基準点・目的フィルタ・吉凶トグル）は1回だけ実装。

---

## 8. 用語の正規化メモ
- 「天衝」と「天冲」は同字異体（検証時どちらかに正規化）。
- 天禽星は天芮と同宮、**先生流派では書かない**。
- 三奇=乙丙丁／六儀=戊己庚辛壬癸／甲尊=甲。八門=休生傷杜景死驚開。八神=直符・螣蛇・太陰・六合・勾陳(玄武)・朱雀(白虎)・九地・九天。
- クレジット表記必須：「共同開発：◯◯」「暦データ：◯◯提供」。【要確認】実名はリポジトリ内で確認できず。

---

## 9. 未解決の先生確認リスト（横断・随時更新）
- 評価ランク Q2「XX超危険」の定義／Q4 五不遇時を△注意に含むか。
- 点数エンジン `jukkanKokuou.js` 平均処理のバグ修正方針。
- 門迫を中宮にも表示するか。
- 現在地ナビの色・「吉扇で緑」の基準（ベストのみ/吉系すべて）・距離表示の可否。
- 格局検索：グループ分け／OR検索／検索期間既定30日／usable基準（§格局検索の指示書参照）。
- 共同事業の確認書（契約書化）＝ビジネス面の最優先。
