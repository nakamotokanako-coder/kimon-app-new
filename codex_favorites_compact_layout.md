# Codex指示：お気に入り一覧が画面を圧迫する問題を全箇所で解消する

> 目的：お気に入りが大量になると、**地図「吉方位」タブだけでなく、お気に入り一覧を表示している他の箇所でも縦スペースを圧迫する**。これをまとめて解消する。
> 方針：**場所によって「小窓スクロール」と「折りたたみ」を使い分ける**。
>  - **地図「吉方位」タブ**（よく使う主戦場）＝地図を大きく固定＋お気に入りは**固定高さの小窓内スクロール**（常に見えてスクロールできる）。
>  - **それ以外の箇所**（設定・他タブ等、ついでに出る所）＝**折りたたみ**（普段は閉じてタップで開く）を基本に、使われ方で判断。
> 性質：**レイアウト（CSS＋開閉state）中心の低リスク改修**。スコア計算・データ・お気に入りの保存/編集/削除ロジックには触らない。
> 重要前提：直近で入れた「お気に入り2段表示＋鉛筆＋編集モーダル／名前変更・削除」（コミット bfc2856）は**そのまま維持**。今回はその外側の入れ物（レイアウト）だけ整える。

---

## 0. まず全箇所を洗い出す（実装前に必ず・最重要）
> ※このリポは「同じ機能でも窓ごとにコードが別」になりがち（過去に施設検索の直叩きが別ファイルに残っていた等）。**推測で直さず、まず実態を列挙する。**

1. お気に入り一覧を**表示している箇所を全部**特定する：
   - `rg -n "お気に入り|favorite|favourites|favorites" src` を実行。
   - ヒットを精査し、「お気に入りの**一覧をレンダリングしている**コンポーネント／JSX」がどこに何箇所あるかをリスト化する（保存/読込ヘルパー自体ではなく、**画面に縦に並べている箇所**）。
   - 各箇所について：どのタブ/画面か（地図・設定・盤・他）、現状スクロール制御や高さ制限があるか、を1〜2行でメモ。
2. **このメモ（箇所リスト）を実装前にコミットメッセージ or PR説明に残す**（後で「全部直したか」を照合できるように）。
3. もし**お気に入り一覧が地図タブの1箇所しか無い**場合は、§2（地図タブ）だけ実施すれば完了。§3（他箇所）はスキップしてその旨を報告。

---

## 1. 共通ヘルパー（重複を作らない）
- 「小窓スクロール枠」「折りたたみ枠」を**それぞれ1つの共通コンポーネント/CSSクラス**として作り、各箇所で使い回す。
  - 箇所ごとに別々のスクロール/開閉ロジックを書かない（直し忘れ・挙動不一致の原因になる）。
- 折りたたみの開閉stateは各箇所ローカルでよい（localStorageに保存する必要は今回なし。やるなら任意）。

---

## 2. 地図「吉方位」タブ：地図を大きく固定＋お気に入りは小窓スクロール
（対象：`src/reverseDirection/` 配下、DirectionMap.jsx等）

### 2-1. 地図セクションを大きく一定サイズで固定
- 地図セクションに**安定した高さ**を与える（下の内容に押されて縮まない）。
  - 目安：**画面高の約45〜55%（例 `height: 50vh` 相当＋`min-height` 併記）**。値は実機で微調整可（コードに「実機調整可」コメント）。
- 地図の高さが**お気に入り件数に左右されない**こと（0件でも20件でも地図は同じ高さ）。
- Leafletはコンテナ高さ変更後に表示が崩れることがある。崩れたら高さ確定後に `map.invalidateSize()` を呼ぶ。

### 2-2. お気に入りを小窓内スクロール
- お気に入り一覧を**固定の最大高さ＋縦スクロール**に：`max-height`（行3〜4件分が見える高さ、目安 `240px`・実機調整可）＋ `overflow-y: auto`（モバイルは `-webkit-overflow-scrolling: touch`）。
- 件数が増えても**お気に入りセクションの高さは一定**＝地図や下の検索結果を押し下げない。
- 見出し「お気に入り」は**スクロール領域の外（上）に固定**（見出しごと流れて消えない）。見出し横に件数を小さく出してよい（任意・例「お気に入り（5）」）。

---

## 3. その他の箇所：折りたたみ（普段閉じてタップで開く）
（§0で見つかった、地図タブ以外のお気に入り表示箇所すべて）

- 各箇所のお気に入り一覧を**折りたたみ**にする：
  - 見出し行（例「お気に入り（5）」＋▽/▷の開閉アイコン）をタップで開閉。
  - **デフォルトは閉じる**（普段は場所を食わない）。開くと一覧が出る。
- 開いたときの一覧が長くなりすぎる場所は、**折りたたみの中身にも小窓スクロール（max-height＋overflow）を併用**してよい（開いても画面を埋め尽くさない）。
- 各箇所の使われ方を§0メモで見て、「ここは常時表示の方が自然」と判断したら小窓スクロール側に寄せても可。**判断理由をPR説明に1行残す**。

---

## 4. 体裁の微調整（任意・やりすぎない）
- 1件の高さが高い場合のみ、行の上下paddingをわずかに詰める程度はOK。
- 長い名前（例「特別養護老人ホーム『もみの樹園』」）で行が膨らむのが気になる場合、**1段目の表示名を1行省略**（`text-overflow: ellipsis` + `white-space: nowrap`、または行数制限）にしてよい（任意・実機判断）。住所2段目は従来どおり。
- ただし**2段表示・鉛筆アイコン・方位バッジ（値/色）の見た目は維持**。入れ物を整えるだけ。

---

## 5. 触ってよい / 触ってはいけない
### 触ってよい
- 各箇所のお気に入り一覧の**レイアウト・高さ・スクロール・折りたたみ開閉**（CSS＋開閉state）。
- 地図セクションの高さ固定。
- 見出しの固定表示・件数表示・表示名の1行省略・行paddingの微調整（任意）。
- 小窓/折りたたみの共通コンポーネント追加。

### 触ってはいけない
- 方位スコア計算・方位バッジの値/色（吉青/凶赤）・距離計算。
- お気に入りのデータ構造（lat/lon/住所/label/スコア等のフィールド名・型）・localStorageキー・保存/読込・**名前変更/削除ロジック（bfc2856で入れた機能は維持）**。
- 地図の扇/ピン・検索（施設/住所）ロジック・`/api/overpass`・src/kimon/ エンジン群。
- お気に入りの**地図上ピン表示は今回やらない**（次回の別PR）。

---

## 6. テスト
- 既存テスト全green維持（レイアウト中心なのでテスト増減は基本なし。スナップショットがあれば必要に応じ更新）。
- 折りたたみの開閉state・小窓スクロールはロジックが薄いので新規テストは任意（入れるなら「閉が初期値」「開で一覧が出る」程度）。
- `npm test` green ＋ `npm run build` 成功を確認。

---

## 7. 進め方
1. `git checkout main && git pull` → `git checkout -b feat-favorites-compact-layout`（mainから新規ブランチ）。
2. **§0で全箇所を洗い出し**（箇所リストをPR説明用にメモ）→ §1共通枠 → §2地図タブ → §3他箇所 → §4任意。
3. `npm test`（全green）＋ `npm run build`（成功）。
4. push前に `git log --oneline origin/main..feat-favorites-compact-layout` で本機能のコミットだけか確認。
5. **名指し add**（変更ファイルのみ。`git add .` 禁止＝先生資料の未追跡ファイルを巻き込まない）。
6. push → PR（**説明に§0の箇所リストと、各箇所に小窓/折りたたみどちらを当てたかを記載**）→ **Vercelプレビューで実機目視（スマホ縦）**：
   - 地図タブ：地図が大きく固定。お気に入りを増やしても地図の高さ不変（小窓内スクロール）。見出しは上に固定。
   - 他箇所：お気に入りが折りたたまれていて場所を食わない。タップで開閉できる。
   - どの箇所でも 2段表示・鉛筆・編集/削除・方位バッジ（値/色）が従来どおり。
   - 検索結果セクションも従来どおり。
7. 全部OKで手動マージ → **Vercel概要で本番展開コミットが最新か確認**（古ければ「プロダクションへの昇格」＋本番URLを Ctrl+Shift+R）。

---

## 注意（環境）
- 地図タブの検索は `/api/overpass`（Vercel Function）経由のため、**ローカル `npm run dev` では施設/住所検索が動かない**。検索を伴う実機確認は必ず**Vercelプレビュー**で行う（既存お気に入りがあればローカルでもレイアウト/折りたたみの確認は可能）。
