# 🎯 Codex タスク：象意パネルを「用語チップ＋タップ展開（案A）」に改修

**作成**: 2026-05-24 / Claude Web
**実行**: Codex（kimon-app ローカル `C:\dev\kimon-app`）
**対象**: `src/components/ShouiPanel.jsx` ＋ そのスタイル（`src/styles.css` の象意パネル該当部）
**前提**: display_text は全131件追加済み（コミット 20e4d3d）。現状はアコーディオン1段表示（コミット 3e8b316）。
**最重要制約**: 盤（BoardGrid / PalaceCell）には一切触らない。データ（shoui_dict.json）も変更しない。

---

## 📨 背景（先生指摘への対応）

先生から「象意のアコーディオンが分かりにくい（どれがタップできるか分からない）」「スマホで横に長くて右スクロールしないと読めない」と指摘。
対応として、用語表示を**案A（用語チップ＋タップで解説展開）**に変更する。先生確認済み（2026-05-24）。

### 先生確定事項
- 象意パネルは**盤とは別**のパネルでよい（質問2 → A）
- 用語チップの初期状態は**全部閉じた状態**、タップで開く（質問3 → A）

---

## ✅ 改修内容

### UI（現状アコーディオン → チップ＋タップ展開）
宮ごとに、その宮に出る用語（十干剋応名・格局名）を**色付きチップ**として並べ、
チップをタップするとその用語の `display_text`（解説）が**下に展開**する。

- **チップ**：用語名＋開閉キャレット（▼閉/▲開）。`cursor: pointer`、タップ領域 44px 以上
  - 色：**吉＝青 #4472C4 / 凶＝赤 #FF0000**（白文字）
  - 吉凶の判定は既存ロジックを流用：
    - 十干剋応＝ sign（〇=吉/×=凶/△=中立）。中立(△)の色は現状の扱いを踏襲（現状黄なら黄のまま。先生は「青/赤」を吉/凶に指定。△の指定はないので現状維持）
    - 格局＝ kakkyoku.json の kichi_kyo（吉/凶）
- **初期状態**：全チップ閉じた状態（解説は非表示）
- **タップ展開**：その用語の `display_text` を下に表示
  - 解説は**折り返し表示**（`white-space: normal; overflow-wrap: anywhere;`）で**横スクロールを出さない**
  - 親要素に `max-width: 100%; min-width: 0;`
  - 用語名（見出し）＋解説本文。見出しに吉青/凶赤の色アクセント（左ボーダー等）
- 複数チップを同時に開けてよい（アコーディオンのように排他にしなくてよい）

### フォントを Noto Sans JP（ゴシック）に統一 ★追加（2026-05-25）
- 盤デザイン刷新で盤側を Noto ゴシックに統一済み。**象意パネルだけ明朝のまま取り残されている**ため、ここも揃える。
- 象意パネル内の**すべての文字**を `Noto Sans JP`（ゴシック）にする：
  - 用語チップの文字、タップ展開した解説本文、宮ヘッダー（宮名・方位・八神・八門・点数）、表のラベル等すべて。
- 盤側で宮名だけ明朝にしている例外は、**象意パネルには適用しない**（象意パネルは全部ゴシックでよい）。
- 既に `Noto Sans JP` が読み込まれていればそれを使う。無ければ盤側と同じ font-family 指定を流用する。

### 維持する
- 宮ごとのまとまり（宮名・方位・八神・八門・点数のヘッダー）は維持
- 名称引きヘルパー（getJukanShoui / getKakkyoku 等）と同名複数(no)の引き分けロジックはそのまま流用
- `display_text` をそのまま解説に使う（空ならフォールバックで modern+practical）
- import パス・既存プロパティ名・既存の色クラス（shoui-kichi 等）は現状を尊重

### やらないこと
- 盤（BoardGrid / PalaceCell）の変更（象意パネルのみ）
- shoui_dict.json の変更
- 占断内容・名称引きロジックの変更

---

## 🎨 配色・スタイル指針

- 吉チップ：`background:#4472C4; color:#fff;`
- 凶チップ：`background:#FF0000; color:#fff;`
- 中立(△十干剋応)：現状の扱いを踏襲（変更指示なし）
- チップは角丸（pill）、キャレット（▼/▲）で開閉が分かるように
- 解説ボックス：薄い背景＋左ボーダー（吉=青/凶=赤）、本文は折り返し
- スマホ幅（〜420px）でもチップが折り返して並び、解説が横スクロールしないこと
- **フォント：象意パネル全体を `Noto Sans JP`（ゴシック）に。明朝は使わない（盤と統一）**

参考イメージ：別途共有の「象意パネル 案A 適用イメージ」画像のとおり。

---

## 🔍 チェック項目

- [ ] 各宮の用語が色付きチップで並ぶ（吉=青#4472C4 / 凶=赤#FF0000、白文字）
- [ ] 初期状態で全チップが閉じている（解説非表示）
- [ ] チップをタップすると display_text が下に展開、再タップで閉じる
- [ ] キャレット（▼/▲）で開閉状態が分かる
- [ ] スマホ幅（375/390/414px）で解説が折り返し、**横スクロールが出ない**
- [ ] 同名複数の用語（華蓋蓬星[9,73]等）が no で正しく引き分けられ、解説が混ざらない
- [ ] 盤（BoardGrid/PalaceCell）の表示が一切変わっていない
- [ ] **象意パネルの文字がすべて Noto Sans JP（ゴシック）になっている（明朝が残っていない）**
- [ ] PC幅でも破綻しない
- [ ] `npm test` 全green / `npm run build` 成功 / `npm run dev` 目視OK

---

## 📌 完了条件

1. 象意パネルが案A（チップ＋タップ展開）になり、先生指摘（分かりにくい・横スクロール）が解消
2. 盤・データは無変更
3. テスト全green / build成功 / dev目視（複数幅）OK
4. commit（例: `feat(shoui): term chips with tap-to-expand glossary (plan A)`）
5. PR → マージ

---

## 💡 補足

- 既存の辞書整合テスト（display_text の存在・空文字なし・改行なし）は維持。
- チップの吉凶色は「表示色」であり、判定ロジック（sign / kichi_kyo）は既存のものをそのまま使う。色の値だけ #4472C4 / #FF0000 を使う。
