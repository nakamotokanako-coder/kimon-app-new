# 設計図：逆引き方位検索（時盤・日盤）

> 機能①「逆引き方位検索」＋②「地図連動」＋③「時間帯タイムライン」の共通設計。
> 作成：Claude Web / 2026-05-26。判定ロジックは先生エンジン（自前）を使用しAIは使わない。
> ステータス：論点詰め完了 → この設計図をもとにイメージHTML作成 → Codex実装の流れ。

---

## 0. この機能は何か

**「行き先を決めて日時を探す」の逆。今いる場所（または指定地点）から見て、いつ・どの方位が一番吉かをランキングで出す。**

既存の判定エンジン（buildBoard・scoreEngine）が宮ごとに点数を出しているので、**全方位×時間帯を総当たりで点数化して並べ替えるだけ**で実現できる。エンジン本体には手を入れない＝先生ロジックの独自性・正確さを保つ。

---

## 1. 2つのモード（盤の使い分け）

奇門遁甲の作法に沿って、盤の種類で用途を分ける（流派一般の理解と一致）。

| モード | 盤 | 用途 | 単位 | 基準 |
|---|---|---|---|---|
| **お散歩吉方位** | 時盤 | 日々の短距離移動・吉運の累積 | 時刻（2時間ごと） | 自然時補正が必要 |
| **遠出吉方位** | 日盤 | 出張・旅行・商談など1日がかりの遠方 | 日付 | 自然時補正は基本不要 |

- 時盤＝累積型開運。ちょっとした外出で吉運を積む。
- 日盤＝特定の目的地へ向かう日の吉方位。遠出は1日がかりなので日付で指定。
- ※本格作法では「日盤で遠出する日も、出発の瞬間は時盤の吉方位を5〜10分取ってから出発」とされる。日盤モードに出発時刻の時盤を補助表示するかは**先生確認事項**（§7）。

---

## 2. 基準点（どこから見た方角か）

**現在地（GPS）／場所検索のどちらでも基準点に設定できる。** 「今いる場所からお散歩」「会社から出発」「自宅基準」すべて対応。

- 現在地：ブラウザ標準 Geolocation API。**無料・APIキー不要・サーバー不要。**
- 場所検索：国土地理院ジオコーディングAPI（地名・施設名→座標）。**無料・キー不要・CORS許可済みでフロントから直接呼べる。** 代替候補：Geolonia Community Geocoder（日本住所特化・商用可）。
- **課金は一切発生しない**（Google Mapsを使わない限り）。

---

## 3. 自然時補正（時盤モードのみ・最重要）

時盤は2時間ごとに切り替わるため、土地ごとの自然時補正が必須。

- 補正式：**補正分 = (その地点の経度 − 135) × 4分**
  - 例：東京(経度139.69) → +18.8分 ≒ +19分（先生の補正表と一致を検算済み）
- **必要なのは経度だけ**。GPS／場所検索で取れた座標の経度をそのまま式に入れる → 補正表に載っていない土地でも自動算出。
- 補正後の「自然時」で時辰（子刻23-1時…）を判定 → その時辰の時盤を組む。
- 時辰の境界前後は補正で隣の盤になりうるので、補正を必ず先に適用する。
- 日盤モードは1日単位のため時刻補正は基本不要（日付境界・節入りの扱いのみ確認＝§7）。

---

## 4. 画面構成

### 4-1. 時盤モード（お散歩）
```
┌─────────────────────────┐
│ [基準点] 現在地 ▼ / 場所検索 🔍   │  ← GPS or 検索
│ 自然時補正：東京 +19分（自動）        │
├─────────────────────────┤
│ 🗺 地図（Leaflet+OSM）              │  ← ②地図連動
│   現在地中心・今の最大吉方位を       │
│   45度8方位でピン表示                │
├─────────────────────────┤
│ 🎯 今この時刻のベスト方位            │
│   例：南東 +30点（生門・六合）       │
├─────────────────────────┤
│ ⏰ 本日の時間帯別ベスト一覧          │  ← ③タイムライン
│   23-1時  北   +20                  │
│   1-3時   南東 +30 ★最大            │
│   ...（12時辰）                      │
├─────────────────────────┤
│ 🔖 目的フィルタ：                    │
│   [仕事][金運][恋愛][出会い]         │
│   [健康][投資]                      │
│   ☑ 吉のみ表示（デフォルトON）       │
└─────────────────────────┘
```

### 4-2. 日盤モード（遠出）
```
┌─────────────────────────┐
│ [基準点] 現在地 ▼ / 場所検索 🔍   │
│ [日付] 2026-06-01 📅                │
├─────────────────────────┤
│ 🗺 地図：その日の最大吉方位ピン      │
├─────────────────────────┤
│ 🏆 この日の方位ランキング           │
│   1位 東   +40 (飛鳥跌穴)           │
│   2位 南東 +30                      │
│   3位 ...                           │
├─────────────────────────┤
│ 🔖 目的フィルタ／☑吉のみ            │
│ （補助）出発時刻の時盤吉方位 ※確認中  │
└─────────────────────────┘
```

---

## 4-3. メニュー構成（アプリ全体のナビゲーション）

機能が増えてきたため、全部を1画面に縦積みせずメニューで分ける。**スマホ/タブレット専用**前提。

### 構成：3メニュー（ボトムタブ）＋「吉方位」の中でタブ切替
```
┌─ ボトムタブ（毎日使う3つ・親指が届く画面下に固定）──┐
│  [盤]            [吉方位]          [設定]            │
│  今日の盤・象意   逆引き・地図      テーマ・基準点      │
└──────────────────────────────────┘
        │
        └─「吉方位」画面の中で枝分かれ（画面内タブ）
            ├ 時盤 お散歩 ：今の時刻＋本日の時間帯一覧
            ├ 日盤 遠出   ：日付指定で方位ランキング
            └ 期間検索（将来）：日付範囲でベストを探す
              ※基準点（GPS／場所検索）・目的フィルタ・吉/凶切替は3タブ共通
```

### 設計判断の根拠
- **3メニューに絞る**：毎日触るのは「盤」と「吉方位」、設定はたまに。頻度差をそのままタブ数に反映。
- **形＝ボトムタブ**：プロが毎日使うツール。画面下固定はよく使う画面へ最速で飛べる。3つなら押し間違いも少ない。
- **時盤・日盤を1画面に集約**：基準点・目的フィルタ・吉/凶切替が共通。別メニューにすると同じ部品を2か所に作り重複・直し忘れの原因に。画面内タブなら共通部分は1回だけ実装。
- **期間検索（3-G）も同枠**：「日付範囲で方位を探す」＝吉方位探しの仲間。将来タブを1つ足すだけ。
- **将来拡張**：鑑定メモ・顧客管理（アイデア⑥）はボトムタブ4つ目として追加可能。

### 実装時期（重要）
- **PR #15（盤の元エクセル配置）マージ後に着手**。メニュー化はアプリの土台（ルーティング）を変える改修のため、盤まわりを触っている今やるとコンフリクトする。順序は「PR #15マージ → メニュー化 → 逆引き機能を吉方位タブに実装」。

---

## 5. 目的別フィルタ

### 5-1. 仕組み
ランキングのスコア ＝ **既存scoreEngineの基礎点 ＋ 選択中の目的に合致する吉要素のボーナス**。
判定要素は **八門・八神・十干剋応・格局の4本立て**。

### 5-2. データ駆動（後から差し替え可能な設計）
目的→吉要素の対応を**外部JSON（purposeFilters.json）に集約**。コードに直書きしない。
→ 先生の確定回答が来たら**JSONを差し替えるだけ**。エンジンもUIも触らない。

```json
{
  "金運": {
    "門": ["生門","開門"],
    "神": [],
    "十干剋応": ["飛鳥跌穴","天遁"],
    "格局": ["飛鳥跌穴","天遁","地遁","神遁"],
    "警告_凶": ["太白入熒","日奇被刑"]
  },
  "恋愛": { "門": ["開門"], "神": ["六合","太陰"], "十干剋応": ["奇儀順遂","天乙会合"], "格局": ["玉女守門","乙奇得使"] }
  // ...仕事・出会い・健康・投資
}
```

### 5-3. 初期値は半自動生成（先生確認前の叩き台）
`shoui_dict.json`（十干剋応81件・格局50件）の意味文（original/modern/practical）に
「財・婚姻・昇進・人望」等の目的語が入っている → **目的キーワードで該当象意を機械抽出**して初期値を生成。
- 先生は「ゼロから決める」のではなく「機械が出した叩き台を○×で直す」だけ → 負担激減。
- **必ず吉凶フィルタと併用**：キーワードだけだと凶象意も拾う（例：金運で「太白入熒」＝財の凶格）。
  十干剋応は `kikkyo` フィールドで吉抽出、格局は kakkyoku.js の吉凶判定と突き合わせ。

### 5-4. 目的別 仮対応（一般通説ベース・要先生確認）
| 目的 | 八門 | 八神 | 代表的な吉格局/十干剋応 |
|---|---|---|---|
| 仕事 | 生門・開門 | 直符・九天 | 人遁・奇儀順遂・青龍返首 |
| 金運 | 生門・開門 | — | 飛鳥跌穴・天遁 |
| 投資 | 生門・休門 | 九地 | 飛鳥跌穴・天遁・地遁・神遁 |
| 恋愛 | 開門 | 六合・太陰 | 玉女守門・乙奇得使・天乙会合 |
| 出会い | 開門 | 六合 | 人遁・玉女守門・青龍耀明 |
| 健康 | 生門・休門 | — | 謀事成就ほか（要精査） |

---

## 6. 吉凶の扱い（データは全件保持・表示でフィルタ）

**データから凶を物理削除しない。** 理由：凶の減点(-10)が消えるとランキングの順位が成立しなくなる／盤セル象意・象意パネル・326テストが壊れる。

- 内部：全データ（吉・中立・凶）で全方位を正しく点数化。
- 表示：**デフォルトは吉のみ**（流派作法＝使える門は上吉門＋景門のみ、とも一致）。
- 切替：**「凶も見る」をプロ向けに残す**（避けるべき方位の確認に使う）。一般版は吉固定でも可。

---

## 7. 先生への確認事項

1. **方位の定位**：各宮の方角は定位（坎=北、離=南…）でよいか。盤を回す/星の回座は不要との理解でよいか。 ※ほぼ「定位でOK」想定。
2. **8方位の角度**：45度均等で確定（既に指示済み）。→ 地図ピンの角度に使用。
3. **目的別フィルタの定義**：§5の半自動初期値を叩き台に、各目的の吉要素を○×で確定。出会い・健康・投資は既存タグに無いため特に要確認。
4. **ランキングの同点処理**：点数が+10/-10/0の3段階に寄るため同点多発。順利・盤レベル・目的ボーナスをどう加味して順位をつけるか（※盤セル4件問題と同じ重み付け）。
5. **日盤の出発時刻の時盤併用**：遠出日に出発時刻の時盤吉方位を補助表示するか。
6. **日盤の日付境界**：深夜0時前後／節入りの扱い。
7. **距離・滞在ガイド**：時盤=短距離・5〜10分滞在、日盤=遠方、等の目安を表示するか。数値の確認。
8. **時盤限定の凶**（時格・五不遇時）：scoreEngineがどこまで点数に織り込んでいるか。五不遇時の盤を「使わない」扱いにするか。

---

## 8. 段階的実装プラン

| 段階 | 内容 | 先生確認 | 課金 |
|---|---|---|---|
| **0. 基盤** | scoreEngineの目的タグ・時格/五不遇時の現状を実装確認 | — | 0 |
| **0.5 メニュー化** | PR#15マージ後。3メニュー（盤/吉方位/設定）＋吉方位内タブの骨格を作る | — | 0 |
| **1. 時盤逆引き(今すぐ)** | GPS→経度→自然時補正→時辰判定→buildBoard→8方位ランキング | 不要(吉のみ) | 0 |
| **2. 時間帯一覧** | 12時辰ループ→時刻×方位のベスト一覧（③タイムライン） | 不要 | 0 |
| **3. 地図連動** | Leaflet+OSM、現在地中心、45度8方位ピン（②） | 角度のみ | 0 |
| **4. 場所検索** | 国土地理院API、基準点を任意地点に | — | 0 |
| **5. 目的フィルタ** | purposeFilters.json接続、半自動初期値、吉/凶切替 | 定義の確定 | 0 |
| **6. 日盤モード** | 日付指定→その日のランキング、出発時刻の時盤補助 | 境界・併用 | 0 |

- 全段階を通して**既存エンジン（buildBoard/scoreEngine/kakkyoku/jukkanKokuou）を再利用**。新エンジンは書かない。
- 位置情報はHTTPS必須 → 実機確認は**Vercelプレビュー**で行う（LAN IPでは位置情報が取れない点に注意）。
- 位置情報はブラウザ内で経度補正に使うのみ。外部送信しない（先生データ保護方針と一致）。

---

## 9. 技術スタック（すべて無料・キー不要）
- 位置情報：Geolocation API（ブラウザ標準）
- 地図：Leaflet + OpenStreetMap（または国土地理院タイル）
- 場所検索：国土地理院ジオコーディングAPI / Geolonia Community Geocoder
- 既存：React + Vite + Vitest、PapaParse、先生エンジン群

---

*次アクション：この設計図をもとに「時盤モードの画面イメージHTML」を作成 → かなこさん確認 → 先生確認事項を送付 → Codex実装。*
