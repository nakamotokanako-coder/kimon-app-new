# Codex実装指示：最強ランキング 改善（実機フィードバック反映）

> 対象：既存ブランチ **phase-ranking**（db3c030 で実装済み・push前）への追加修正。
> 実機（Vercelプレビュー）でかなこ確認の結果、下記4点を改善する。**エンジン（src/kimon/）は触らない**。
> 表示と期間ロジックだけの修正。テスト348件green維持。

---

## 0. 大原則
- **phase-ranking ブランチで続けて作業**（新ブランチは切らない）。最後に push 前 `git log --oneline origin/main..phase-ranking` で差分確認。
- `src/kimon/` 配下のエンジンは**一切触らない**。点数計算・盤組立は呼ぶだけ。
- 修正対象は `src/reverseDirection/`（ランキング画面）と `src/styles.css` のみ想定。
- 各修正後に `npm test`（348green維持）と `npm run build`（成功）。

---

## 1. 期間プリセットを「本日起点・期間の長さ」方式に統一（最重要）

### 問題
現状「今月」を選ぶと6月の日付が出る。だが今日は5月。「今月＝5月のこと？」と暦の月と混同して誤解を生む。
原因：プリセットが「暦の区切り」と「今日からN日」の考え方が混ざっている。

### 修正
**全プリセットを「本日を起点とした期間の長さ」に統一**し、ラベルもそれに合わせる。

| 旧ラベル | 新ラベル | 範囲（本日起点・未来方向） |
|---|---|---|
| 今週 | **1週間** | 本日 〜 本日+7日 |
| 今月 | **1ヶ月** | 本日 〜 本日+1ヶ月 |
| 3ヶ月 | **3ヶ月** | 本日 〜 本日+3ヶ月 |
| 半年 | **半年** | 本日 〜 本日+6ヶ月 |
| 1年 | **1年** | 本日 〜 本日+1年 |

- 「今週」「今月」という暦区切りの語をやめ、**期間の長さ**で言い切る（誤解防止）。
- 起点は常に**本日**。暦の月初・週初ではない。

### ★過去日付は絶対に含めない（要確認）
- ランキングは「**これから行く日を選ぶ**」用途。過去日は「もう行けない日」なので**本日より前の日付は一切出さない**。
- 実装確認：スキャン開始日 = 本日（`new Date()` の0時）。本日含む未来のみ。
- **Codexへ：現状の実装が過去日付を含んでいないか確認して報告すること**（本日起点・未来方向のみになっているか）。もし過去が混入していたら本日起点に修正。

---

## 2. 起点・終点を画面に明示

期間が「本日から」であることが分かるよう、総合トップの見出し付近に**起点と終点**を表示する。

- 例：「**本日 5/26 〜 6/25**」（1ヶ月選択時）／「**本日 5/26 〜 2027/5/26**」（1年選択時）。
- 既存の件数表示（「31件」「365件」）と並べてよい。
- 目的：いつからいつまでを見ているか一目で分かるようにする（特に長期で年をまたぐ時に効く）。

---

## 3. 年またぎは「年」を表示（日付の見せ方）

### 問題
1年表示で1位が「東 1/17(日)」のように月日だけだと、**今年の1月か翌年の1月か分からない**。
本日5/26起点の1年なら、その1/17は翌年（2027年）だが、年が無いと誤解する。

### 修正：年をまたぐ日付だけ年を付ける（A案）
- **同じ年内の日付**：従来どおり「6/11(木)」（年なし・スッキリ）。
- **起点と異なる年の日付**：「**2027年 1/17(日)**」のように**年を前置**。
- ランキングカード・月別ベストの両方に適用。
- 月別ベストの見出し（「1月」など）も、年が変わる月は「**2027年 1月**」と年を併記。

実装の目安：表示しようとする日付の年 ≠ 起点（本日）の年 のとき年を付ける。
将来「本日起点」以外（任意起点）になっても、起点年と比較する作りにしておけば破綻しない。

---

## 4. 点数カラムを右端固定（レイアウト崩れ修正）

### 問題
ランキング下部で、行ごとに点数（+90点など）の横位置がズレる。バッジの数や方位名の長さでカード内のレイアウトが動くため。

### 修正
- **点数を右端の固定幅カラムに分離**し、中身（方位名の長さ・バッジ数）に関係なく常に同じ位置に揃える。
- 点数カラムは固定幅（例：72px相当）＋右寄せ＋縦中央寄せ。方位名・バッジ群は左側の可変幅エリアに収める（はみ出しは折り返し or 省略）。
- モック `saikyo_ranking_mock.html` の `.score`（`flex:0 0 auto; text-align:right`）が参照。これを固定幅で徹底する。
- 数値の桁数（+90 / +100 / +110）が変わっても右端が揃うこと。

---

## 5. テスト
- 期間プリセットが「本日起点＋指定長さ」で範囲を返すこと（1週間=本日+7日 など）。
- **過去日付を含まないこと**（本日より前が結果に出ない）。
- 年またぎ表示：起点と同年は年なし、異なる年は年付き。
- 既存348件green維持（エンジン非改変）。

---

## 6. やらないこと
- エンジン（src/kimon/）の変更。点数の値そのものは別途エンジン修正で直る（本指示の範囲外）。
- 盤↔日盤(遠出) の基準日同期チェック（**別件・別ブランチ**で後日）。
- 過去日付の検索機能（将来のビューC=期間検索で検討。本指示では未来のみ）。

---

## 7. 進め方
1. 現状の期間ロジックを読んで、過去日付を含むか／起点が本日か報告。
2. §1期間プリセット統一 → §2起点表示 → §3年またぎ → §4点数固定 の順に修正。
3. `npm test` / `npm run build` → Vercelプレビューで実機確認（特に：1ヶ月で6月にはみ出さないか・1年で「2027年」が付くか・点数が揃うか・過去日が出ないか）。
4. push前に `git log --oneline origin/main..phase-ranking` で差分確認 → push → PR → **マージはかなこ手動**。

*参照モック：`saikyo_ranking_mock.html`。点数固定・5段階ソートはモック準拠。*
