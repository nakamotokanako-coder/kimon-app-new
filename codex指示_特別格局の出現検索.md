# Codex指示書：特別格局の出現検索（ビューD・時盤専用）

> 作成：Claude Web / 2026-05-27
> 前提メモ：設計図_逆引き方位検索.md、引き継ぎメモ_2026-06-03、saikyo_ranking_mock.html
> 完成イメージ：`kakkyoku_search_mock.html`（このリポジトリ外・別途共有。**見た目・操作感はこれに準拠**）
> 判定は**先生エンジン（buildBoard / scoreEngine / kakkyoku / jukkanKokuou）をそのまま再利用**。新エンジンは書かない。

---

## 0. これは何か（一行で）

先生が指定した **特別な効果を持つ12個の吉格局** を複数チェックで選び、**それが「使える方位」として出現する日時・方位を、本日起点で一覧表示**する検索機能。**時盤専用**（お散歩・短距離）。日盤は対象外。

### 対象の12格局（`shoui_dict.json` の `kakkyoku_index_by_name` に全て定義済み）
旬首2大吉格：青龍返首(1)・飛鳥跌穴(2)
九遁：天遁(3)・地遁(4)・人遁(5)・雲遁(6)・風遁(7)・龍遁(8)・虎遁(9)・神遁(10)・鬼遁(11)
人の和合：玉女守門(18)

---

## 1. かなこ＆先生が確定した仕様（★必ず守る）

1. **「使える方位(usable)だけ」を出す。** 格局がその宮に出ていても、伏吟・反吟・門迫・凶門・庚の凶格などで使えない宮は**結果から除外**する。usable 判定は **scoreEngine の既存ロジックをそのまま使う**（このビュー独自の足切りは新設しない）。
2. **複数チェックで選べる**（1個だけでもOK）。選んだ格局は **OR**（どれか1つでもその宮に出ていればヒット）。
3. **並べ替えは2モード切替**：「日付が早い順」（既定）と「スコアが高い順」。**本日を起点**にする（過去日は出さない）。
4. **時盤のみ**。九遁・玉女守門は時盤格局のため、日盤検索は提供しない。
5. **自然時補正は必須**（時盤だから）。基準点の経度から `補正分 = (経度 − 135) × 4分` で算出し、補正後の自然時で時辰を判定 → その時辰の盤を組む。設計図§3のとおり。既存の逆引き機能に同じ補正処理があれば**それを再利用**すること（重複実装しない）。

---

## 2. 計算ロジック（新エンジン不要）

```
入力：基準点(緯度経度) / 検索期間（既定 本日+30日）
for 各日 in [本日 .. 本日+期間]:
  for 各時辰 in 12時辰(子〜亥):
    補正後の自然時で時辰を確定 → buildBoard で時盤を構築
    for 各宮(8方位):
      宮の格局リスト = kakkyoku 判定結果
      if (選択された格局のどれか) ∈ 宮の格局リスト:
        if scoreEngine で その宮が usable:
          結果に push { 日付, 時辰, 方位, ヒット格局[], 八門, スコア }
並べ替え（date / score）→ 表示
```

- buildBoard / scoreEngine / kakkyoku は**既存のものをそのまま呼ぶ**。引数の作り方は既存の逆引き（時盤）実装に合わせる。
- 計算量：30日 × 12時辰 × 8方位 = 2880盤評価。盤構築がやや重いが、**検索ボタン押下時に1回だけ**走る同期処理で問題ない規模。重ければ時辰ループ単位で `requestIdleCallback` / `setTimeout(0)` で分割してプログレス表示してよい（任意）。
- **中宮（坤宮寄宮）の扱いは既存ロジックに従う**。8方位＝8宮で回す（中宮は方位なしなので結果対象外）。

---

## 3. データ（格局マスタとpractical象意）

結果カードに表示する「practical（使い所）」は `shoui_dict.json` の格局オブジェクトの `practical` を使う。**JSONは変更しない**。12格局の表示名・グループ分けは下記のとおり（モックの `KAKU` 定義と同一）。

| グループ | 格局 |
|---|---|
| 旬首2大吉格 | 青龍返首 / 飛鳥跌穴 |
| 九遁 | 天遁 / 地遁 / 人遁 / 雲遁 / 風遁 / 龍遁 / 虎遁 / 神遁 / 鬼遁 |
| 人の和合 | 玉女守門 |

- グループ見出しは「旬首2大吉格 / 九遁 / 人の和合」。**※このグループ分けは先生未確認**（§7参照）。とりあえずこの3分類で実装し、先生確認後に差し替えられるよう**配列定義1箇所にまとめる**こと。

---

## 4. 画面（モック `kakkyoku_search_mock.html` 準拠）

「吉方位」ボトムタブ内の**画面内タブの1つ**として追加する（設計図§4-3のメニュー構成。時盤お散歩・日盤遠出・最強ランキングと並ぶ位置）。タブ名は **「格局を探す」** 程度で（最終名はかなこ確認）。

画面構成（上から）：
1. ヘッダー：タイトル「特別格局の出現検索」＋サブ「狙った大吉格が、いつ・どの方位に出るかを探す（時盤）」
2. 基準点バー：現在地／場所検索（**既存の逆引き機能の基準点UIを再利用**）＋「時盤・お散歩」バッジ
3. 自然時補正の表示：「自然時補正：東京 +19分（経度から自動算出）」
4. 格局チェッパー：3グループ、チップ複数選択、「すべて選択／クリア」、選択数カウント
5. 検索ボタン：選択数0なら disabled（文言「格局を選んでください」）、1個以上で「この◯個が出る日時を検索」
6. 結果ヘッダ＋並べ替えトグル（日付が早い順／スコアが高い順）
7. 結果カード：方位・日付(曜日)・時辰(例「子(23-1時)」)・スコア・ヒット格局バッジ・八門バッジ・practical象意
8. 結果0件のときは空状態メッセージ（「選んだ格局は、この期間内に使える方位として出現しませんでした。期間や格局を変えてみてください。」）

### 結果カードのタップ挙動
- カードタップで**その日時・方位の時盤の地図（扇表示）へ遷移**。既存の日盤マップ／逆引きマップへの遷移と同じ作法に合わせる（既存の遷移ルートがあればそれを使う。無ければ今回は遷移なしでも可・要相談）。

---

## 5. フォント（★今回の修正点・必ず守る）

- 本アプリは**盤デザイン刷新時にNoto化済み**：本文＝`'Noto Sans JP'`、見出し・タイトル・宮名・大きい数字＝`'Noto Serif JP'`（明朝）。**この機能も同じ font-family を使い、アプリ全体と揃える**。
- 既存の index.html / グローバルCSS に Noto の Webフォント読み込み（Google Fonts の `<link>` か `@import`）が**既にあるはず**。**それに乗ること。新たに別フォントを足さない。**
- 万一どこにも Noto の読み込みが無い場合のみ、`index.html` の `<head>` に下記を追加する（重複追加はしない）：
  ```html
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;500;700&family=Noto+Serif+JP:wght@600;700&display=swap" rel="stylesheet">
  ```
- このビューの CSS で font-family をハードコードするときも、本文 `'Noto Sans JP', 'Hiragino Kaku Gothic ProN', sans-serif` / 見出し `'Noto Serif JP', serif` を使う。**モックと同じ指定**。

---

## 6. 触ってよい範囲 / 触らない範囲

### 触ってよい
- 新規コンポーネント（例 `src/reverseDirection/KakkyokuSearch.jsx` など、既存の逆引き配下に追加）
- 「吉方位」画面のタブ定義に1タブ追加
- `src/styles.css` の本機能該当部の追記
- 検索ロジック用の薄いユーティリティ（既存エンジンを呼ぶだけのラッパー）

### ★絶対に触らない
- `buildBoard` / `scoreEngine` / `kakkyoku.js` / `jukkanKokuou.js` 本体（呼ぶだけ。中身を変えない）
- `shoui_dict.json` / `chito_v2.csv` / `koyomi*.csv`（データは読むだけ）
- 盤（BoardGrid / PalaceCell）、象意パネル、現在地マーク／追っかけ線まわり
- 全画面モード（`.is-fullscreen`）の `invalidateSize` effect（過去2回のデグレ箇所）

---

## 7. 先生確認事項（このPRと並行して送る・回答待ちでブロックしない）

実装は下記の暫定仕様で進め、回答が来たら**定義1箇所の差し替え**で対応できるようにしておく。

1. **格局のグループ分け**：「旬首2大吉格／九遁／人の和合」の3分類でよいか。一括（特別な吉格12）でも可。
2. **OR検索でよいか**：複数選択時は「どれか1つでも出ていればヒット」で良いか（AND＝全部同時に出る、は想定していない）。
3. **同一宮に複数の指定格局が同時ヒットした場合**：バッジを並べて1件として数える、で良いか（モックは並べる想定）。
4. **検索期間の既定値**：本日から30日でよいか。プリセット（7/14/30日）を出すか。
5. **usable の基準**：「使える方位だけ出す」＝scoreEngine の既存 usable 判定で足切り、という理解でよいか。九遁・玉女守門に固有の追加条件（例：特定の宮・八神とのセット成立条件）があれば教えてほしい。
6. **九遁の成立条件**：現在の `kakkyoku.js` が九遁を正しく判定できているか（天盤・地盤・八門・八神・宮の条件。講座まとめ第5章の表どおりか）の確認。← ここが判定の要。

---

## 8. テスト・確認

- `npm test` 全green維持（既存テストを壊さない）。検索ロジックのユーティリティに単体テストを足す（例：選択格局のOR判定、usable除外、date/scoreソート）。
- `npm run build` 成功。
- 実機（iPhone・**Vercelプレビュー**）で：チップ複数選択／検索／結果表示／並べ替え切替／空状態／フォントがNotoで出ているか／既存タブ（時盤お散歩・日盤・ランキング）にデグレが無いか。
- **PR前にブランチとbaseを必ず確認**（base=main から切る。前回 #27 の事故＆ #29 の教訓）。

---

## 9. 進め方

1. 既存の逆引き（時盤）実装を読み、基準点UI・自然時補正・buildBoard 呼び出し・kakkyoku 判定結果の取得方法を把握。
2. 検索ロジック（§2）を既存エンジンのラッパーとして実装＋単体テスト。
3. UI（§4・モック準拠）を実装。フォントは§5を厳守。
4. 「吉方位」画面にタブ追加。
5. Vercelプレビューで実機確認 → base=main でPR → かなこ手動マージ。
